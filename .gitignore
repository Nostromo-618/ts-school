node_modules
dist
.DS_Store
*.log
.vite
playwright-report
.playwright-mcp
test-results
coverage
tsconfig.tsbuildinfo

# Local Gemma / LiteRT mirrors (pnpm models:fetch) — never ship in dist
.models

# Generated diagnostic maps are committed (src/curriculum/generated/).
# Legacy public/ts-lib from the removed in-browser worker may still exist locally.
public/ts-lib
.harness-dist
.ci-labs
