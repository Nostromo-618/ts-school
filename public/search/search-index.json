{
  "version": 1,
  "documentCount": 208,
  "tracks": [
    "foundations",
    "types",
    "functions",
    "structures",
    "type-level",
    "runtime-boundary",
    "async",
    "node-migration",
    "tooling",
    "testing"
  ],
  "documents": [
    {
      "id": "abortsignal-and-cancellation",
      "title": "AbortSignal and cancellation",
      "category": "Async, errors & Result",
      "tab": "intermediate",
      "route": "/lessons/async/abortsignal-and-cancellation",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "AbortSignal",
        "AbortController",
        "cancellation",
        "timeout",
        "signal",
        "async",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Threading a signal through an async call chain, typing the abort reason, and why cancellation is a parameter rather than a return value.. A request that the caller no longer wants keeps running, and there is no type-level pressure to accept a signal. No cancellation plumbing. Pass `AbortSignal` into APIs that support it.. Pass `AbortSignal` into APIs that support it.. AbortError should be handled distinctly from other failures.. Cancel on timeout and on navigation away.. No cancellation plumbing.. Thread `AbortSignal` through. load returns string, not number.. await fetch(url);. // Ambient stand-ins — DOM AbortSignal is not in the es2022 lib set. interface AbortSignal { readonly aborted: boolean; } declare class AbortController { readonly signal: AbortSignal; abort(reason?: unknown): void; } type FetchInit = { signal?: AbortSignal }; declare function fetchText(url: string, init?: FetchInit): Promise<string>; export async function load( url: string, signal: AbortSignal, )…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Threading a signal through an async call chain, typing the abort reason, and why cancellation is a parameter rather than a return value.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A request that the caller no longer wants keeps running, and there is no type-level pressure to accept a signal. No cancellation plumbing. Pass `AbortSignal` into APIs that support it.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "abstract-classes",
      "title": "Abstract classes and members",
      "category": "Objects, classes & modules",
      "tab": "intermediate",
      "route": "/lessons/structures/abstract-classes",
      "icon": "ph-cube",
      "keywords": [
        "abstract",
        "base class",
        "template method",
        "subclass",
        "override",
        "structures",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A base that cannot be instantiated and members a subclass must supply — the one inheritance feature that carries real checking weight.. A base class with a method that throws 'not implemented' pushes a compile-time contract into a runtime failure. Base class constructed directly. Abstract forces subclasses to implement members.. abstract forces subclasses to implement members.. Prefer interfaces when you only need a shape.. Use abstract classes when you share implementation.. Base class constructed directly.. abstract classes cannot be instantiated.. class Animal { speak() {} } new Animal();. abstract class Animal { abstract speak(): string; } class Dog extends Animal { speak(): string { return \"woof\"; } } const d: Animal = new Dog(); const bad = new Animal();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A base that cannot be instantiated and members a subclass must supply — the one inheritance feature that carries real checking weight.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A base class with a method that throws 'not implemented' pushes a compile-time contract into a runtime failure. Base class constructed directly. Abstract forces subclasses to implement members.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "accessors-and-computed-members",
      "title": "Accessors and computed members",
      "category": "Objects, classes & modules",
      "tab": "intermediate",
      "route": "/lessons/structures/accessors-and-computed-members",
      "icon": "ph-cube",
      "keywords": [
        "getter",
        "setter",
        "accessor",
        "computed property",
        "readonly",
        "structures",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "get and set with different types, computed property names, and how the checker treats a getter-only property as `readonly`.. A setter that accepts a string and a getter that returns a Date is a useful API and an awkward one to type. Computed key access without relating key to value. Computed keys work with literal types and `keyof`.. Computed keys work with literal types and `keyof`.. Getters/setters can enforce invariants at the boundary.. Prefer methods when side effects are involved.. Computed key access without relating key to value.. `typeof` key ties the read to string. Not number.. const key = 'id'; obj[key] = 1;. type User = { id: string; name: string }; const key = \"id\" as const; function read(u: User, k: typeof key): string { return u[k]; } const u: User = { id: \"1\", name: \"Ada\" }; const id: string = read(u, \"id\"); const bad: number = read(u, \"id\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "get and set with different types, computed property names, and how the checker treats a getter-only property as `readonly`.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A setter that accepts a string and a getter that returns a Date is a useful API and an awkward one to type. Computed key access without relating key to value. Computed keys work with literal types and `keyof`.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "adding-typescript-to-an-existing-project",
      "title": "Adding TypeScript to a project that works",
      "category": "Node.js migration",
      "tab": "beginner",
      "route": "/lessons/node-migration/adding-typescript-to-an-existing-project",
      "icon": "ph-hard-drives",
      "keywords": [
        "adopt",
        "tsconfig",
        "incremental",
        "devDependency",
        "node-migration",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Install typescript, add a `tsconfig`, and keep shipping JS while the checker watches over your shoulder.. Teams often treat adopting TypeScript as a big-bang rename: every file must become `.ts` before anyone gets value. That belief blocks the first win. You can keep the runtime entry in JavaScript, add a pinned `typescript` (and a minimal `strict` `tsconfig`), and start checking beside the code that already works — without freezing the roadmap for a month.. Add typescript as a pinned devDependency (this site dual-installs typescript@7 plus typescript-strada@6.0.3 for createProgram).. Commit a minimal `strict` `tsconfig` before mass renames.. Keep node running compiled or tsx/ts-node only in trusted local/dev paths.. Keep the runtime entry; add checking beside it.. No TypeScript error here — that's the point: naming Env still leaves Number(`undefined`) as a runtime NaN risk.. Require PORT before Number() so NaN cannot escape.. Must you rename all files to `.ts` on day one?. // package.json scripts still run node on .js entrypoints. function boot(env) { return Number(env.PORT); } boot({ PORT: \"3000\" });. type Env = { PORT?: string }; function boot(env: Env): number { return Number(env.PORT); } // PORT might be missing — Number(undefined) is NaN. const port: number = boot({}); if (Number.isNaN(port)) { throw new Error(\"PORT required\"); }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Install typescript, add a `tsconfig`, and keep shipping JS while the checker watches over your shoulder.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Teams often treat adopting TypeScript as a big-bang rename: every file must become `.ts` before anyone gets value. That belief blocks the first win. You can keep the runtime entry in JavaScript, add a pinned `typescript` (and a minimal `strict` `tsconfig`), and start checking beside the code that already works — without freezing the roadmap for a month.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "allowjs-and-checkjs",
      "title": "allowJs and checkJs",
      "category": "Node.js migration",
      "tab": "beginner",
      "route": "/lessons/node-migration/allowjs-and-checkjs",
      "icon": "ph-hard-drives",
      "keywords": [
        "allowJs",
        "checkJs",
        "JSDoc",
        "@ts-check",
        "node-migration",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Type-check existing `.js` with JSDoc while `allowJs` keeps those files in the program — the bridge before renames.. New `.ts` files sit next to untyped `.js`, and the old bugs quietly re-enter through the door you left open. Without `checkJs`, JavaScript neighbors are invisible to the checker — so the migration feels unfinished even after you 'added TypeScript.' JSDoc can be a lightweight contract while files wait to be renamed.. `allowJs` includes `.js` in the project; `checkJs` type-checks them.. // @ts-check at the top of a file enables checking even without `checkJs` globally.. JSDoc @param/@returns is enough to unlock many migrations without a rename yet.. With `checkJs`, JSDoc becomes a lightweight contract.. Same error you want `checkJs` to surface in `.js` files.. Pass a number to double.. What does `checkJs` do?. // @ts-check /** @param {number} n */ export function double(n) { return n * 2; } double(\"2\");. // The TS equivalent of a checkJs finding: export function double(n: number): number { return n * 2; } double(\"2\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Type-check existing `.js` with JSDoc while `allowJs` keeps those files in the program — the bridge before renames.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "New `.ts` files sit next to untyped `.js`, and the old bugs quietly re-enter through the door you left open. Without `checkJs`, JavaScript neighbors are invisible to the checker — so the migration feels unfinished even after you 'added TypeScript.' JSDoc can be a lightweight contract while files wait to be renamed.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "annotations-vs-inference",
      "title": "Annotations against inference",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/annotations-vs-inference",
      "icon": "ph-compass",
      "keywords": [
        "inference",
        "annotation",
        "let",
        "const",
        "explicit types",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "When to write the type yourself and when to let TypeScript `infer it` — and the quiet ways inference can widen further than you meant.. Annotating everything makes noise; annotating nothing hides intent at API boundaries. The skill is knowing where inference is enough and where a written type is documentation and a contract.. Prefer inference for locals whose initializer already states the type clearly.. Annotate function parameters, public returns, and values that cross module boundaries.. When inference produces a union you did not want, fix the initializer or add an annotation — do not silence with `any`.. Without types, string | number collapses into 'whatever'.. Annotate or parse when inference widens past the API.. Parse envPort to a number before calling createServer.. Where should you almost always write type annotations?. function createServer(port) { return { listen: () => port }; } // Env vars are strings; mixing with a numeric default is a classic footgun. function listen(envPort) { const port = envPort ?? 3000; return createServer(port); } listen(\"3000\");. function createServer(port: number) { return { listen: () => port }; } function listen(envPort: string | undefined) { // Inferred as string | number — not assignable to number. const port = envPort ?? 3000; return createServer(port); } listen(\"3000\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "When to write the type yourself and when to let TypeScript `infer it` — and the quiet ways inference can widen further than you meant.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Annotating everything makes noise; annotating nothing hides intent at API boundaries. The skill is knowing where inference is enough and where a written type is documentation and a contract.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "any-and-implicit-any",
      "title": "any, and the implicit any leak",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/any-and-implicit-any",
      "icon": "ph-compass",
      "keywords": [
        "any",
        "implicit any",
        "noImplicitAny",
        "escape hatch",
        "unsound",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`any` switches the checker off for everything it touches, and untyped parameters hand it to you without asking. Where it leaks in and how to see it.. One `any` at the edge of a module silently disables checking for everything that touches it. Implicit `any` from missing annotations does the same with less honesty — the hole has no name.. Explicit `any` opts out of checking for that value and everything derived from it.. `noImplicitAny` (on under `strict`) forces you to notice untyped parameters instead of inventing `any` for you.. Prefer `unknown` at boundaries, then narrow — `any` is an escape hatch, not a default.. No types means no argument about whether userId is a string.. `TS7006`: parameter 'body' implicitly has an `any` type.. Annotate body as { userId: string } and leave handle({ userId: 42 }) so Check matches the authored solution (that call is the remaining diagnostic).. What does `noImplicitAny` change?. function handle(body) { // Trust the client: body.userId is \"whatever\". return body.userId.toUpperCase(); } handle({ userId: 42 });. // Under strict, an untyped parameter is an error — not a silent any. function handle(body) { return body.userId.toUpperCase(); } handle({ userId: 42 });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`any` switches the checker off for everything it touches, and untyped parameters hand it to you without asking. Where it leaks in and how to see it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "One `any` at the edge of a module silently disables checking for everything that touches it. Implicit `any` from missing annotations does the same with less honesty — the hole has no name.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "array-narrowing",
      "title": "Narrowing arrays and their elements",
      "category": "Types & narrowing",
      "tab": "intermediate",
      "route": "/lessons/types/array-narrowing",
      "icon": "ph-shapes",
      "keywords": [
        "Array.isArray",
        "filter",
        "find",
        "predicate",
        "noUncheckedIndexedAccess",
        "types",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`Array.isArray`, .find returning T | `undefined`, and why .filter(Boolean) does not remove `null` from the type unless you help it.. arr[0] is typed T even on an empty array, so the safest-looking line in the file is the one that throws. Assuming items[0] exists. Indexing may yield `undefined` — narrow before using methods.. Indexing may yield `undefined` — narrow before using methods.. `noUncheckedIndexedAccess` makes this the default; model it even without the flag.. Empty arrays are the classic production crash.. Assuming items[0] exists.. first returns string | `undefined`.. function first(items) { return items[0].toUpperCase(); }. function first(items: string[]): string | undefined { const head = items[0]; if (head === undefined) return undefined; return head.toUpperCase(); } const boom: string = first([]);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`Array.isArray`, .find returning T | `undefined`, and why .filter(Boolean) does not remove `null` from the type unless you help it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "arr[0] is typed T even on an empty array, so the safest-looking line in the file is the one that throws. Assuming items[0] exists. Indexing may yield `undefined` — narrow before using methods.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "arrays-and-tuples",
      "title": "Arrays and tuples",
      "category": "Types & narrowing",
      "tab": "beginner",
      "route": "/lessons/types/arrays-and-tuples",
      "icon": "ph-shapes",
      "keywords": [
        "array",
        "tuple",
        "readonly",
        "destructuring",
        "types",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "T[] for a homogeneous list, [string, number] for a fixed shape, and why the difference matters the moment you destructure.. An array of anything is an array of nothing in particular, so a list of pairs and a list of strings type-check identically. Destructuring assumes numbers; strings still 'work' until toFixed.. number[] is a list of numbers of `unknown` length; [number, number] is a pair.. After destructuring a tuple, each binding keeps its element type.. Prefer `readonly` [number, number] when the pair should not be mutated or pushed to.. Destructuring assumes numbers; strings still 'work' until toFixed.. A tuple fixes length and element types; string[] will not do.. Pass a numeric pair so pointLabel type-checks.. What does [string, number] guarantee that (string | number)[] does not?. function pointLabel(pair) { const [x, y] = pair; return x.toFixed(1) + \",\" + y.toFixed(1); } pointLabel([\"10\", \"20\"]);. function pointLabel(pair: [number, number]): string { const [x, y] = pair; return x.toFixed(1) + \",\" + y.toFixed(1); } pointLabel([\"10\", \"20\"]);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "T[] for a homogeneous list, [string, number] for a fixed shape, and why the difference matters the moment you destructure.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "An array of anything is an array of nothing in particular, so a list of pairs and a list of strings type-check identically. Destructuring assumes numbers; strings still 'work' until toFixed.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "assertion-functions",
      "title": "Assertion functions",
      "category": "The runtime boundary",
      "tab": "intermediate",
      "route": "/lessons/runtime-boundary/assertion-functions",
      "icon": "ph-shield-check",
      "keywords": [
        "asserts",
        "assertion function",
        "invariant",
        "narrowing",
        "runtime-boundary",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`asserts value is T` narrows after a successful return — useful for invariants, dangerous if used instead of parsing untrusted input.. Throwing when a check fails does not change TypeScript's view of the value unless you mark the function as an assertion. Runtime throw without an `asserts` signature — types ignore the check.. `asserts value is T` means: if the function returns, value is T; if not, it threw.. Use assertions for internal invariants you control — not as a substitute for parsing untrusted JSON.. A lying assertion function (returns without checking) is trusted by the checker; that is a critical footgun.. Runtime throw without an `asserts` signature — types ignore the check.. After `assertUser`, `raw` is User. Calling `greet` without asserting still errors.. After `assertUser(raw)` where assertUser is `asserts value is User`, what is raw?. function assertUser(value) { if (!value || typeof value.id !== \"string\") { throw new Error(\"not a user\"); } } function main(raw) { assertUser(raw); // Engine still treats raw as whatever it was — no narrowing. return raw.id.toUpperCase(); }. type User = { id: string }; function assertUser(value: unknown): asserts value is User { if ( typeof value !== \"object\" || value === null || typeof (value as { id?: unknown }).id !== \"string\" ) { throw new Error(\"not a user\"); } } function greet(user: User): string { return user.id.toUpperCase(); } export function main(raw: unknown): string { assertUser(raw); return greet(raw); } // Without the a…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`asserts value is T` narrows after a successful return — useful for invariants, dangerous if used instead of parsing untrusted input.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Throwing when a check fails does not change TypeScript's view of the value unless you mark the function as an assertion. Runtime throw without an `asserts` signature — types ignore the check.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "assertions-and-narrowing-in-tests",
      "title": "Assertions that narrow",
      "category": "Testing with types",
      "tab": "intermediate",
      "route": "/lessons/testing/assertions-and-narrowing-in-tests",
      "icon": "ph-test-tube",
      "keywords": [
        "assertion",
        "narrowing",
        "toBeDefined",
        "non-null",
        "expect",
        "testing",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "expect(x).toBeDefined() proves nothing to the compiler. Which assertions narrow, which do not, and how to bridge the gap without casting.. Every line after a truthiness assertion still sees the nullable type, so tests fill up with bangs. Jest expects do not narrow TypeScript types. Runtime expects do not narrow TS types unless you wrap them.. Runtime expects do not narrow TS types unless you wrap them.. `asserts` x is T bridges test `asserts` into control flow.. Alternatively assign after a guard.. Jest expects do not narrow TypeScript types.. Assertion functions narrow after expectDefined. id is string.. Does expect(user).toBeDefined() narrow user for TypeScript?. expect(user).toBeDefined(); expect(user.id).toBe('1');. type User = { id: string }; declare function expectDefined<T>(x: T | null | undefined): asserts x is T; declare const user: User | undefined; expectDefined(user); const id: string = user.id; const bad: number = user.id;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "expect(x).toBeDefined() proves nothing to the compiler. Which assertions narrow, which do not, and how to bridge the gap without casting.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Every line after a truthiness assertion still sees the nullable type, so tests fill up with bangs. Jest expects do not narrow TypeScript types. Runtime expects do not narrow TS types unless you wrap them.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "assignability-rules",
      "title": "The assignability rules",
      "category": "Types & narrowing",
      "tab": "advanced",
      "route": "/lessons/types/assignability-rules",
      "icon": "ph-shapes",
      "keywords": [
        "assignability",
        "subtyping",
        "any",
        "unknown",
        "never",
        "reduction",
        "types",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "What the checker actually asks when it asks whether A fits B: top and bottom types, union and intersection reduction, and the special cases for `any`.. Type X is not assignable to type Y is the most common error in TypeScript, and almost nobody can state the rule it is enforcing. Dynamic assignment has no structural subtype check.. Assignability is structural: members of the target must be present (with compatible types) on the source.. `any` is assignable to and from almost everything — it punches through the lattice.. `unknown` is the safe top; `never` is the bottom; unions/intersections reduce before the check.. Dynamic assignment has no structural subtype check.. `unknown` cannot flow into string without narrowing.. Accept `unknown` and return a string by narrowing with `typeof`.. Is `unknown` assignable to string?. // JS: assignability is “did it run?” — anything goes. let x = 1; x = \"now a string\"; x = { oops: true };. // unknown is a safe top: nothing without narrowing. let u: unknown = 1; const s: string = u; // never is bottom: assignable to everything, nothing assignable to it (except never). function fail(): never { throw new Error(\"nope\"); } const n: number = fail(); void n;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "What the checker actually asks when it asks whether A fits B: top and bottom types, union and intersection reduction, and the special cases for `any`.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Type X is not assignable to type Y is the most common error in TypeScript, and almost nobody can state the rule it is enforcing. Dynamic assignment has no structural subtype check.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "async-await-typing",
      "title": "async and await",
      "category": "Async, errors & Result",
      "tab": "beginner",
      "route": "/lessons/async/async-await-typing",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "async",
        "await",
        "Promise",
        "return type",
        "async",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "async functions always return `Promise<T>`; await unwraps Promise and propagates the inner type — including unions.. An async function forgets to return on one branch; callers await `undefined` and crash. Implicit `undefined` return becomes a resolved `undefined`. Async function f(): `Promise<T>` means every return path must produce T (or throw).. async function f(): `Promise<T>` means every return path must produce T (or throw).. await expression has the inner type of the `Promise`.. Try/catch around await still types the caught value as `unknown` under useUnknownInCatchVariables.. Implicit `undefined` return becomes a resolved `undefined`.. `Promise<User>` rejects a bare return (`undefined`).. Allow a `null` result in the return type.. What does an async function always return?. async function loadName(id) { if (id === \"missing\") return; return { name: \"Ada\" }; } const user = await loadName(\"missing\"); user.name;. type User = { name: string }; async function loadName(id: string): Promise<User> { if (id === \"missing\") return; return { name: \"Ada\" }; }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "async functions always return `Promise<T>`; await unwraps Promise and propagates the inner type — including unions.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "An async function forgets to return on one branch; callers await `undefined` and crash. Implicit `undefined` return becomes a resolved `undefined`. Async function f(): `Promise<T>` means every return path must produce T (or throw).",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "async-iterators-and-generators",
      "title": "Async iterators and generators",
      "category": "Async, errors & Result",
      "tab": "intermediate",
      "route": "/lessons/async/async-iterators-and-generators",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "async iterator",
        "generator",
        "for await",
        "yield",
        "streaming",
        "async",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "AsyncIterable<T>, for await, and the three type parameters of a generator — the shape behind every streaming API in Node.. Generator<T, TReturn, TNext> has three parameters and almost every example on the internet uses only the first. Async iteration without element types. AsyncGenerator<T> types yielded values.. AsyncGenerator<T> types yielded values.. for-await works on async iterables.. Prefer streams for large IO; generators for composed async sequences.. Async iteration without element types.. AsyncGenerator yields number. sum returns number.. for await (const chunk of stream) { sink(chunk); }. async function* ticks(n: number): AsyncGenerator<number, void, void> { for (let i = 0; i < n; i++) yield i; } export async function sum(n: number): Promise<number> { let total = 0; for await (const x of ticks(n)) total += x; return total; } const bad: string = await sum(3);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "AsyncIterable<T>, for await, and the three type parameters of a generator — the shape behind every streaming API in Node.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Generator<T, TReturn, TNext> has three parameters and almost every example on the internet uses only the first. Async iteration without element types. AsyncGenerator<T> types yielded values.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "awaited-and-unwrapping",
      "title": "Awaited and nested promises",
      "category": "Async, errors & Result",
      "tab": "intermediate",
      "route": "/lessons/async/awaited-and-unwrapping",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "Awaited",
        "thenable",
        "unwrap",
        "nested promise",
        "utility type",
        "async",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`Awaited<T>` flattens as far as await would. Where nested promises come from and why the naive unwrap type is wrong.. A cache that stores promises hands back `Promise<Promise<T>>` and the type that describes it has to flatten recursively. Nested promises confuse readers. Async functions wrap returns in `Promise`.. async functions wrap returns in `Promise`.. `Awaited<T>` unwraps nested thenables in types.. Avoid returning `Promise<Promise<T>>` manually.. Nested promises confuse readers.. `Awaited` unwraps Promise layers. N is number.. async function inner() { return 1; } async function outer() { return inner(); }. async function inner(): Promise<number> { return 1; } async function outer(): Promise<number> { return inner(); } type N = Awaited<ReturnType<typeof outer>>; const n: N = 1; const bad: N = \"1\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`Awaited<T>` flattens as far as await would. Where nested promises come from and why the naive unwrap type is wrong.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A cache that stores promises hands back `Promise<Promise<T>>` and the type that describes it has to flatten recursively. Nested promises confuse readers. Async functions wrap returns in `Promise`.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "branded-and-nominal-types",
      "title": "Branded types",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/branded-and-nominal-types",
      "icon": "ph-function",
      "keywords": [
        "branded type",
        "nominal",
        "opaque",
        "UserId",
        "unique symbol",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Adding an unforgeable marker to a structural type so UserId and OrderId stop being interchangeable strings.. Every id in the system is a string, so passing an order id where a user id was expected is a type-correct data breach. Structural string identity cannot tell user from order.. Branding is an intersection with a unique phantom property — erased at runtime, nominal at compile time.. Only trusted constructors (or assertions) should mint brands; a bare `as UserId` anywhere reopens the hole.. unique symbol brands are stronger than string-literal brand keys that can collide across packages.. Structural string identity cannot tell user from order.. Brands make swapped ids a type error.. Define Email as string & { `readonly` __email: `void` } and a function send(to: Email). Show a valid call using a cast mint.. Are UserId and OrderId assignable to each other if both are string & { __brand: … } with different brand strings?. // JS: ids are strings — mix them freely. function refund(orderId, userId) { console.log(\"refund\", orderId, \"for\", userId); } const userId = \"user_1\"; const orderId = \"order_9\"; refund(userId, orderId); // swapped — still \"works\". declare const userBrand: unique symbol; declare const orderBrand: unique symbol; type UserId = string & { readonly [userBrand]: void }; type OrderId = string & { readonly [orderBrand]: void }; function asUserId(s: string): UserId { return s as UserId; } function asOrderId(s: string): OrderId { return s as OrderId; } function refund(orderId: OrderId, userId: UserId) { void orderId; void userId; } …",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Adding an unforgeable marker to a structural type so UserId and OrderId stop being interchangeable strings.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Every id in the system is a string, so passing an order id where a user id was expected is a type-correct data breach. Structural string identity cannot tell user from order.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "branded-validated-types",
      "title": "Types only a validator can mint",
      "category": "The runtime boundary",
      "tab": "advanced",
      "route": "/lessons/runtime-boundary/branded-validated-types",
      "icon": "ph-shield-check",
      "keywords": [
        "branded type",
        "smart constructor",
        "validation",
        "Email",
        "opaque",
        "runtime-boundary",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Brand the output of a parser so an Email can only be produced by the function that checked it — validation you cannot forget to call.. Validation happens at the edge and the value travels for another twenty functions, any of which may construct a fresh unvalidated one. Nothing stops calling send with an unchecked string.. Smart constructors return branded types; public APIs accept only the brand.. Keep the brand key unexported so callers cannot forge values with object literals easily.. Pair with runtime parsing — the brand is a compile-time receipt for a check that already ran.. Nothing stops calling send with an unchecked string.. Only parseEmail mints Email — raw strings are rejected.. Define UserId brand and parseUserId(s: string): UserId | `null` requiring s.startsWith(\"user_\").. What should be the only way to obtain an Email?. // JS: validate once, then pass raw strings everywhere. function isEmail(s) { return s.includes(\"@\"); } function send(to) { console.log(\"mail\", to); } const raw = \"not-an-email\"; if (isEmail(raw)) send(raw); send(raw); // also works — validation was optional. declare const emailBrand: unique symbol; type Email = string & { readonly [emailBrand]: void }; function parseEmail(input: string): Email | null { if (!input.includes(\"@\")) return null; return input as Email; } function send(to: Email) { void to; } const raw = \"not-an-email\"; const parsed = parseEmail(raw); if (parsed) send(parsed); send(raw);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Brand the output of a parser so an Email can only be produced by the function that checked it — validation you cannot forget to call.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Validation happens at the edge and the value travels for another twenty functions, any of which may construct a fresh unvalidated one. Nothing stops calling send with an unchecked string.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "bundlers-and-transpile-only",
      "title": "Who is actually checking your types?",
      "category": "Tooling & the strictness ladder",
      "tab": "advanced",
      "route": "/lessons/tooling/bundlers-and-transpile-only",
      "icon": "ph-wrench",
      "keywords": [
        "esbuild",
        "swc",
        "vite",
        "transpile only",
        "type check",
        "pipeline",
        "tooling",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "esbuild, swc, Vite, and Node's type stripping all delete types without reading them. Where the real gate goes in a modern pipeline.. The build is fast because it `never` type-checked anything, and the only thing that did was the editor on one developer's machine. Transpile-only ≠ typechecked.. Separate “emit JS” from “prove types” — both are required, different tools.. Run `tsc` --`noEmit` (or vue-`tsc`) in CI even when Vite/esbuild build the artifacts.. `isolatedModules`/`verbatimModuleSyntax` keep transpile-only tools honest about syntax.. Transpile-only ≠ typechecked.. Dev transpile pipelines often omit typecheck — CI must not.. Type Gate = \"typecheck\" | \"lint\" and require both in a CI list.. Does Vite’s default TS transform typecheck?. // Fast bundlers strip TypeScript-looking syntax without proving types. // A broken call still ships if nothing runs tsc.. type PipelineStep = \"transpile\" | \"typecheck\" | \"test\"; const viteDev: PipelineStep[] = [\"transpile\"]; const ci: PipelineStep[] = [\"typecheck\", \"test\", \"transpile\"]; function hasTypecheck(steps: PipelineStep[]): boolean { return steps.includes(\"typecheck\"); } const ok = hasTypecheck(ci); const claim: true = hasTypecheck(viteDev); void ok;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "esbuild, swc, Vite, and Node's type stripping all delete types without reading them. Where the real gate goes in a modern pipeline.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The build is fast because it `never` type-checked anything, and the only thing that did was the editor on one developer's machine. Transpile-only ≠ typechecked.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "call-and-construct-signatures",
      "title": "Callable and constructable types",
      "category": "Functions & generics",
      "tab": "intermediate",
      "route": "/lessons/functions/call-and-construct-signatures",
      "icon": "ph-function",
      "keywords": [
        "call signature",
        "construct signature",
        "new",
        "callable",
        "factory",
        "functions",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Objects that are also functions, and types that describe a class rather than its instances: (…): T and new (…): T.. A factory that takes a class and returns instances of it cannot be typed with a plain function type. new on an `unknown` constructor. New (...) => T describes constructible values.. new (...) => T describes constructible values.. (...) => T describes callables — do not confuse them.. Factories that accept classes need construct signatures.. new on an `unknown` constructor.. Ctor signature requires string. number fails.. function make(C, arg) { return new C(arg); }. type Ctor<T> = new (name: string) => T; class User { constructor(readonly name: string) {} } function make<T>(C: Ctor<T>, name: string): T { return new C(name); } const u = make(User, \"Ada\"); const bad = make(User, 1);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Objects that are also functions, and types that describe a class rather than its instances: (…): T and new (…): T.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A factory that takes a class and returns instances of it cannot be typed with a plain function type. new on an `unknown` constructor. New (...) => T describes constructible values.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "catch-gives-you-unknown",
      "title": "catch gives you unknown",
      "category": "Async, errors & Result",
      "tab": "intermediate",
      "route": "/lessons/async/catch-gives-you-unknown",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "catch",
        "unknown",
        "useUnknownInCatchVariables",
        "error",
        "throw",
        "async",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "JavaScript lets you throw anything, so a caught value is `unknown` under useUnknownInCatchVariables. What to do with it before assuming it is an Error.. err.message on a caught value crashes with a different error whenever something threw a string. Assuming catch binding is Error. Catch bindings are `unknown` in modern TS configs.. catch bindings are `unknown` in modern TS configs.. Narrow with `instanceof` Error before reading message.. Never type catch as `any` to silence this.. Assuming catch binding is Error.. Under useUnknownInCatchVariables / `strict`, e is `unknown` — no .message.. Narrow e with `instanceof` Error before reading .message (else `String`(e)). Match the solution text.. try { await run(); } catch (e) { log(e.message); }. declare function run(): Promise<void>; declare function log(msg: string): void; export async function main(): Promise<void> { try { await run(); } catch (e) { log(e.message); } }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "JavaScript lets you throw anything, so a caught value is `unknown` under useUnknownInCatchVariables. What to do with it before assuming it is an Error.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "err.message on a caught value crashes with a different error whenever something threw a string. Assuming catch binding is Error. Catch bindings are `unknown` in modern TS configs.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "ci-gates-for-types",
      "title": "Type checking in CI",
      "category": "Tooling & the strictness ladder",
      "tab": "advanced",
      "route": "/lessons/tooling/ci-gates-for-types",
      "icon": "ph-wrench",
      "keywords": [
        "ci",
        "gate",
        "ratchet",
        "error budget",
        "cache",
        "pipeline",
        "tooling",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Where the check belongs in a pipeline, caching it, and ratcheting an error budget so a partially migrated codebase can only improve.. A codebase with 400 known errors either blocks every pull request or checks nothing, unless the gate counts rather than passes. Without a type gate, regressions are invisible in CI.. Put `tsc` --`noEmit` early in CI; cache .tsbuildinfo when using incremental/project references.. Ratchets encode a budget; tighten maxErrors over time.. Do not let suppressions grow unbounded — count @ts-expect-error too.. Without a type gate, regressions are invisible in CI.. Ratchet gates compare counts — over budget is false.. Write passes(errors: number, max: number): boolean.. Best CI strategy for a half-migrated repo?. // JS CI often runs tests only — type errors never appear. console.log(\"shipped\");. type GateResult = { errors: number; maxErrors: number }; function passes(gate: GateResult): boolean { return gate.errors <= gate.maxErrors; } const migrating: GateResult = { errors: 380, maxErrors: 400 }; const ok = passes(migrating); const broken: GateResult = { errors: 401, maxErrors: 400 }; const claim: true = passes(broken); void ok;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Where the check belongs in a pipeline, caching it, and ratcheting an error budget so a partially migrated codebase can only improve.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A codebase with 400 known errors either blocks every pull request or checks nothing, unless the gate counts rather than passes. Without a type gate, regressions are invisible in CI.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "class-member-visibility",
      "title": "private, protected, and #private",
      "category": "Objects, classes & modules",
      "tab": "beginner",
      "route": "/lessons/structures/class-member-visibility",
      "icon": "ph-cube",
      "keywords": [
        "private",
        "protected",
        "hash private",
        "encapsulation",
        "structures",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "TypeScript's `private`/`protected` are compile-time only; `#fields` are true runtime privacy — know which boundary you need.. A session object stores an auth token behind an underscore — `_token` — and the team treats that as \"private.\" It is not. Any helper that receives the object can read `session._token`, log it, stash it on `window`, or ship it in a debug payload. Look at the left pane: underscore privacy is a social contract, not enforcement. JavaScript will run that access anyway, so a token that was supposed to stay inside `Session` leaks the moment someone is curious, rushed, or malicious.. TS `private`/`protected` erase — malicious JS can still read the property.. `#token` is a runtime-private field checked by the engine.. Use `#` or `WeakMap` patterns when secrecy matters; use `private` for API hygiene.. Underscore privacy is a social contract, not enforcement.. `private` blocks external access at compile time.. Read the token through `debug()` instead of `s.token`.. Which privacy is enforced at runtime in modern JS?. function Session(token) { this._token = token; } Session.prototype.debug = function () { return this._token; }; new Session(\"secret\")._token;. class Session { constructor(private token: string) {} debug(): string { return this.token; } } const s = new Session(\"secret\"); s.token;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "TypeScript's `private`/`protected` are compile-time only; `#fields` are true runtime privacy — know which boundary you need.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A session object stores an auth token behind an underscore — `_token` — and the team treats that as \"private.\" It is not. Any helper that receives the object can read `session._token`, log it, stash it on `window`, or ship it in a debug payload. Look at the left pane: underscore privacy is a social contract, not enforcement. JavaScript will run that access anyway, so a token that was supposed to …",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "classes-intro",
      "title": "Classes",
      "category": "Objects, classes & modules",
      "tab": "beginner",
      "route": "/lessons/structures/classes-intro",
      "icon": "ph-cube",
      "keywords": [
        "class",
        "constructor",
        "implements",
        "parameter properties",
        "structures",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "TypeScript classes add parameter properties and implements — while remaining ordinary constructor functions at runtime.. Factory functions return ad-hoc objects; nothing ensures two factories build the same fields. mail vs email — the classic factory typo. Public/private/protected in the constructor parameter list `declare` and assign fields.. public/private/protected in the constructor parameter list `declare` and assign fields.. implements Interface checks the instance shape; it does not change emit.. Prefer plain objects + functions unless you need identity, `instanceof`, or inheritance.. mail vs email — the classic factory typo.. Parameter properties create typed instance fields.. Read u.email instead of u.mail.. What does `constructor(public id: string)` create?. function makeUser(id, email) { return { id, mail: email }; } const u = makeUser(\"1\", \"a@b.co\"); u.email.toLowerCase();. class User { constructor( public id: string, public email: string, ) {} } const u = new User(\"1\", \"a@b.co\"); u.mail.toLowerCase();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "TypeScript classes add parameter properties and implements — while remaining ordinary constructor functions at runtime.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Factory functions return ad-hoc objects; nothing ensures two factories build the same fields. mail vs email — the classic factory typo. Public/private/protected in the constructor parameter list `declare` and assign fields.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "commonjs-to-esm",
      "title": "CommonJS to ESM",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/commonjs-to-esm",
      "icon": "ph-hard-drives",
      "keywords": [
        "commonjs",
        "esm",
        "nodenext",
        "type module",
        "extensions",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "module: nodenext, \"type\": \"module\", mandatory file extensions in relative imports, and what happens to require along the way.. `ERR_MODULE_NOT_FOUND` for a file that plainly exists is usually the ESM extension rule — and the error message almost never says so. CommonJS `require` forgives bare relative paths; ESM does not. Migrations stall on this one surprise more than on type errors.. Under ESM / nodenext, relative imports need an explicit `.js` extension (even from `.ts` sources).. Replace require/module.exports with import/export and node: builtins.. Derive paths from `import.meta`.url instead of __dirname.. CJS require without extensions — Node forgives you.. Model nodenext extension rules with literal types. Extensionless path is not \"./config.js\".. Why does import \"./config\" fail under Node ESM?. const readConfig = require(\"./config\"); module.exports = { readConfig };. type ConfigLoader = () => unknown; declare function loadConfig(specifier: string): ConfigLoader; export const readConfig = loadConfig(\"./config.js\"); // Extensionless relative path is the nodenext footgun: const badSpecifier: \"./config.js\" = \"./config\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "module: nodenext, \"type\": \"module\", mandatory file extensions in relative imports, and what happens to require along the way.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "`ERR_MODULE_NOT_FOUND` for a file that plainly exists is usually the ESM extension rule — and the error message almost never says so. CommonJS `require` forgives bare relative paths; ESM does not. Migrations stall on this one surprise more than on type errors.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "conditional-types-intro",
      "title": "Conditional types",
      "category": "Type-level programming",
      "tab": "intermediate",
      "route": "/lessons/type-level/conditional-types-intro",
      "icon": "ph-function",
      "keywords": [
        "conditional type",
        "extends",
        "branch",
        "ternary",
        "generic",
        "type-level",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "T extends U ? X : Y is an if statement in the type system. Where it is genuinely the right tool, and where a union would have done.. A function whose return type depends on an argument's type needs either three overloads or one conditional type. Runtime `typeof` checks only. T extends U ? X : Y branches in the type system.. T extends U ? X : Y branches in the type system.. They power many library utilities.. Distribute over naked type parameters — learn that next.. Runtime `typeof` checks only.. Conditional type yields false for number — true is not assignable.. What does type IsString<T> = T extends string ? true : false do?. function unwrap(x) { return x; }. type IsString<T> = T extends string ? true : false; type A = IsString<\"hi\">; type B = IsString<number>; const ok: A = true; const bad: B = true;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "T extends U ? X : Y is an if statement in the type system. Where it is genuinely the right tool, and where a union would have done.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A function whose return type depends on an argument's type needs either three overloads or one conditional type. Runtime `typeof` checks only. T extends U ? X : Y branches in the type system.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "const-assertions",
      "title": "as const",
      "category": "Types & narrowing",
      "tab": "intermediate",
      "route": "/lessons/types/const-assertions",
      "icon": "ph-shapes",
      "keywords": [
        "as const",
        "const assertion",
        "readonly",
        "literal",
        "widening",
        "types",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A const assertion freezes an object or array into its most specific `readonly` literal type — the cheapest way to build a lookup table the checker understands.. A config object's values widen to string the moment it is declared, so nothing downstream can depend on what is actually in it. Mutable string[] invites drift. `as const` freezes literal types and makes tuples `readonly`.. `as const` freezes literal types and makes tuples `readonly`.. Indexed access (`typeof` routes)[number] builds a union of members.. Use it for route tables, event names, and config maps.. Mutable string[] invites drift.. `as const` routes do not include /admin.. What does `as const` primarily buy you on an array literal?. const routes = [\"/\", \"/about\"]; routes.push(\"/admin\");. const routes = [\"/\", \"/about\"] as const; type Route = (typeof routes)[number]; const go = (r: Route) => r; go(\"/\"); go(\"/admin\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A const assertion freezes an object or array into its most specific `readonly` literal type — the cheapest way to build a lookup table the checker understands.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A config object's values widen to string the moment it is declared, so nothing downstream can depend on what is actually in it. Mutable string[] invites drift. `as const` freezes literal types and makes tuples `readonly`.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "const-type-parameters",
      "title": "const type parameters",
      "category": "Functions & generics",
      "tab": "advanced",
      "route": "/lessons/functions/const-type-parameters",
      "icon": "ph-function",
      "keywords": [
        "const type parameter",
        "as const",
        "literal",
        "inference",
        "api design",
        "functions",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "<const T> makes a call site behave as though the caller wrote `as const` — the way a library preserves literal types without asking its users to.. Every user of your builder API has to remember `as const`, and the ones who forget get string instead of their route names. Without literal inference, route names widen immediately.. const type parameters apply a const assertion to the inferred argument type.. Library authors use them so callers need not write `as const` at every call.. Combine with `readonly` arrays/tuples when you want immutable literal tables.. Without literal inference, route names widen immediately.. <const T> preserves literal tuple types from the call site.. Write keys<const T extends `Record<string, unknown>>`(obj: T): (`keyof` T)[] that returns `Object.keys` cast appropriately, and call it on { a: 1 }.. What does `<const T>` change about inference?. // JS: route tables are plain arrays of strings. function routes(names) { return names; } const r = routes([\"home\", \"about\"]); // just strings. function routes<const T extends readonly string[]>(names: T): T { return names; } const r = routes([\"home\", \"about\"]); // r is readonly [\"home\", \"about\"] type First = (typeof r)[0]; const ok: First = \"home\"; const bad: First = \"settings\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "<const T> makes a call site behave as though the caller wrote `as const` — the way a library preserves literal types without asking its users to.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Every user of your builder API has to remember `as const`, and the ones who forget get string instead of their route names. Without literal inference, route names widen immediately.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "contextual-typing",
      "title": "Contextual typing",
      "category": "Functions & generics",
      "tab": "beginner",
      "route": "/lessons/functions/contextual-typing",
      "icon": "ph-function",
      "keywords": [
        "contextual typing",
        "callback inference",
        "map",
        "functions",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "When the surrounding type tells TypeScript what a callback parameter is — so you can omit the annotation and still stay safe.. You annotate every callback parameter out of fear, then the annotations drift from the array element type after a refactor. Strings have no toFixed — fails only when the line runs.. When a function expects (x: T) => U, an unannotated callback parameter becomes T.. You usually do not need to restate the parameter type inside .map/.filter callbacks.. If inference fails, annotate the parameter — or the empty array as T[].. Strings have no toFixed — fails only when the line runs.. Contextual typing still catches the wrong method.. Use a string method appropriate for id.. In [1,2].map((n) => ...), what is n's type?. const ids = [\"a\", \"b\", \"c\"]; ids.map((id) => id.toFixed(0));. const ids = [\"a\", \"b\", \"c\"]; // id is contextually typed as string from string[]. ids.map((id) => id.toFixed(0));",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "When the surrounding type tells TypeScript what a callback parameter is — so you can omit the annotation and still stay safe.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "You annotate every callback parameter out of fear, then the annotations drift from the array element type after a refactor. Strings have no toFixed — fails only when the line runs.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "contract-tests-between-packages",
      "title": "Catching a breaking type change",
      "category": "Testing with types",
      "tab": "advanced",
      "route": "/lessons/testing/contract-tests-between-packages",
      "icon": "ph-test-tube",
      "keywords": [
        "contract test",
        "api surface",
        "breaking change",
        "semver",
        "ci",
        "testing",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Type-level contract tests in CI, API-surface snapshots, and turning an accidental breaking change into a failed build rather than a consumer's bug report.. Semver says a type change is breaking, and nothing in the release process can tell whether one happened. Without type contracts, breakages arrive as production errors.. Keep a tiny consumer package or type tests that import the public API in CI.. API Extractor / attw / semver-check tools snapshot the surface.. Treat type-only breaks as major versions when you publish libraries.. Without type contracts, breakages arrive as production errors.. Consumer assignments fail when the contract params drift.. Define type Api = { ping(): \"pong\" } and a const api: Api.. Why run contract tests in CI for a library?. // JS breaking changes are runtime surprises for callers. function greet(name) { return \"hi \" + name; } // Tomorrow: greet({ first }) — callers still pass strings.. // Shared public contract: export type Greeter = (name: string) => string; const g: Greeter = (name) => `hi ${name}`; const broken: Greeter = (name: number) => `hi ${name}`; void g;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Type-level contract tests in CI, API-surface snapshots, and turning an accidental breaking change into a failed build rather than a consumer's bug report.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Semver says a type change is breaking, and nothing in the release process can tell whether one happened. Without type contracts, breakages arrive as production errors.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "control-flow-analysis",
      "title": "How control-flow analysis works",
      "category": "Types & narrowing",
      "tab": "advanced",
      "route": "/lessons/types/control-flow-analysis",
      "icon": "ph-shapes",
      "keywords": [
        "control flow",
        "CFA",
        "narrowing",
        "flow node",
        "aliased condition",
        "types",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The checker walks your code as a graph and computes a type per reference. Understanding that graph explains every narrowing that surprised you.. Narrowing looks like magic until it stops working, and then there is nothing to reason about unless you know what it is actually doing. Control flow in JS does not refine declared types — there are none.. Each reference gets a type from reaching definitions along the control-flow graph.. Discriminant checks, `typeof`, and equality create edges that refine types in true/false successors.. When CFA “fails,” ask what assignment or call could have invalidated the predicate since it was proven.. Control flow in JS does not refine declared types — there are none.. CFA narrows in branches; wrong assignments still fail outside them.. Write narrow(x: `unknown`): string that returns x if `typeof` x === \"string\", else \"\".. What does control-flow analysis compute?. // JS: if-checks are just branches — no type changes. function len(x) { if (typeof x === \"string\") return x.length; return x; }. function label(x: string | number) { if (typeof x === \"string\") { return x.toUpperCase(); } return x.toFixed(1); } function broken(x: string | number) { const isStr = typeof x === \"string\"; if (isStr) { // Aliased conditions are tracked in modern TS — but mutation breaks them. return x.toUpperCase(); } return x.toFixed(1); } const n: number = label(\"hi\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The checker walks your code as a graph and computes a type per reference. Understanding that graph explains every narrowing that surprised you.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Narrowing looks like magic until it stops working, and then there is nothing to reason about unless you know what it is actually doing. Control flow in JS does not refine declared types — there are none.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "currying-and-partial-application",
      "title": "Keeping inference through composition",
      "category": "Functions & generics",
      "tab": "intermediate",
      "route": "/lessons/functions/currying-and-partial-application",
      "icon": "ph-function",
      "keywords": [
        "curry",
        "compose",
        "pipeline",
        "middleware",
        "inference",
        "functions",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Curried functions, pipelines, and middleware chains all lose type information at the joins unless the signatures are written to carry it through.. compose(a, b, c) is where every functional Node codebase discovers the limits of inference. Curried add with no numeric guarantee. Generics can thread types through partial application.. Generics can thread types through partial application.. Prefer simple functions until currying clarifies an API.. Inference across multiple arrows can need annotations.. Curried add with no numeric guarantee.. Inner function returns number, not string.. function add(a) { return (b) => a + b; }. function add<A extends number>(a: A): (b: number) => number { return (b) => a + b; } const plus1 = add(1); const n: number = plus1(2); const bad: string = plus1(2);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Curried functions, pipelines, and middleware chains all lose type information at the joins unless the signatures are written to carry it through.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "compose(a, b, c) is where every functional Node codebase discovers the limits of inference. Curried add with no numeric guarantee. Generics can thread types through partial application.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "custom-error-classes",
      "title": "Custom error classes",
      "category": "Async, errors & Result",
      "tab": "intermediate",
      "route": "/lessons/async/custom-error-classes",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "Error",
        "extends",
        "instanceof",
        "setPrototypeOf",
        "stack",
        "async",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Extending Error, keeping `instanceof` working across transpilation targets, and adding typed fields without breaking the stack trace.. Subclassing Error and compiling down to ES5 quietly breaks `instanceof`, so the catch block that handles your error never runs. Throwing plain objects. Extend Error and set name for debuggability.. Extend Error and set name for debuggability.. `instanceof` works within the same realm.. Include cause when wrapping lower-level failures.. Throwing plain objects.. Custom Error subclasses carry typed fields. handle returns number.. throw { code: 404, message: 'missing' };. class AppError extends Error { constructor( readonly code: number, message: string, ) { super(message); this.name = \"AppError\"; } } function handle(err: unknown): number { if (err instanceof AppError) return err.code; return 500; } const n: number = handle(new AppError(404, \"missing\")); const bad: string = handle(new AppError(404, \"missing\"));",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Extending Error, keeping `instanceof` working across transpilation targets, and adding typed fields without breaking the stack trace.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Subclassing Error and compiling down to ES5 quietly breaks `instanceof`, so the catch block that handles your error never runs. Throwing plain objects. Extend Error and set name for debuggability.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "declaration-emit",
      "title": "Declaration emit",
      "category": "Tooling & the strictness ladder",
      "tab": "advanced",
      "route": "/lessons/tooling/declaration-emit",
      "icon": "ph-wrench",
      "keywords": [
        "declaration",
        "d.ts",
        "isolatedDeclarations",
        "declaration map",
        "portability",
        "tooling",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "How .d.ts files are generated, why 'the inferred type cannot be named' happens, declaration maps, and the isolatedDeclarations shortcut.. A type that compiles fine cannot be written into a declaration file, and the error names a file you have `never` opened. Without .d.ts, the public API is whatever you export in JS.. declaration: true writes .d.ts; `declarationMap` helps editors jump to source.. isolatedDeclarations requires enough annotations for emit without typechecking inference.. Export the types you return — do not leak private aliases into public signatures.. Without .d.ts, the public API is whatever you export in JS.. Explicit Public return types keep .d.ts self-contained.. Export type Point = { x: number; y: number } and function origin(): Point.. Typical cause of “inferred type cannot be named”?. // JS has no declaration emit — consumers read the source or nothing. export function make() { return { hidden: true }; }. type Hidden = { secret: number }; // Inferred return references Hidden — bad for declaration emit portability. export function make() { const value: Hidden = { secret: 1 }; return value; } // Prefer an exported alias for the public surface: export type Public = { secret: number }; export function makePublic(): Public { return { secret: 1 }; } const x: string = makePublic();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "How .d.ts files are generated, why 'the inferred type cannot be named' happens, declaration maps, and the isolatedDeclarations shortcut.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A type that compiles fine cannot be written into a declaration file, and the error names a file you have `never` opened. Without .d.ts, the public API is whatever you export in JS.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "declaration-files-intro",
      "title": "Where types come from: .d.ts files",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/declaration-files-intro",
      "icon": "ph-compass",
      "keywords": [
        ".d.ts",
        "ambient",
        "@types",
        "DefinitelyTyped",
        "declare",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Declaration files describe JavaScript that already exists — ambient modules, @types packages, and why a missing .d.ts feels like the library is untyped.. A missing or wrong `.d.ts` makes a working library look untyped — or worse, typed incorrectly. Ambient declarations are promises about runtime; broken promises become `any` or false confidence.. .d.ts files are TypeScript's description of existing JavaScript — they emit nothing.. Prefer packages with bundled types; otherwise install @types/name from DefinitelyTyped.. You can `declare` a minimal ambient module locally when upstream types are missing.. JavaScript packages often ship without type information.. `TS2307` until the package ships types or you add @types/slugify.. Replace the unresolved import with a local ambient function declaration.. What does a .d.ts file contribute at runtime?. // Runtime works; editors know nothing about slugify's API. const slugify = require(\"slugify\"); slugify(\"Hello World\", { lower: true });. // No @types in this sandbox — the import cannot be resolved. import slugify from \"slugify\"; export const path = slugify(\"Hello World\", { lower: true });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Declaration files describe JavaScript that already exists — ambient modules, @types packages, and why a missing .d.ts feels like the library is untyped.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A missing or wrong `.d.ts` makes a working library look untyped — or worse, typed incorrectly. Ambient declarations are promises about runtime; broken promises become `any` or false confidence.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "declaration-merging",
      "title": "Declaration merging",
      "category": "Objects, classes & modules",
      "tab": "intermediate",
      "route": "/lessons/structures/declaration-merging",
      "icon": "ph-cube",
      "keywords": [
        "declaration merging",
        "augmentation",
        "global",
        "interface",
        "namespace",
        "structures",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Two interfaces with the same name become one. The feature behind global augmentation, and the reason a stray declaration can change a type you never touched.. An interface you did not write gained a property because a dependency declared one with the same name. Redeclarations silently fight each other. Interfaces merge; type aliases do not.. Interfaces merge; type aliases do not.. Merging is useful for augmentation — dangerous for app models.. Prefer one declaration unless you are extending a library.. Redeclarations silently fight each other.. Merged interface requires both fields.. // two interfaces with same name in JS just overwrite. interface User { id: string; } interface User { name: string; } const u: User = { id: \"1\", name: \"Ada\" }; const bad: User = { id: \"1\" };",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Two interfaces with the same name become one. The feature behind global augmentation, and the reason a stray declaration can change a type you never touched.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "An interface you did not write gained a property because a dependency declared one with the same name. Redeclarations silently fight each other. Interfaces merge; type aliases do not.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "decorators",
      "title": "Decorators",
      "category": "Objects, classes & modules",
      "tab": "advanced",
      "route": "/lessons/structures/decorators",
      "icon": "ph-cube",
      "keywords": [
        "decorator",
        "experimentalDecorators",
        "metadata",
        "class",
        "framework",
        "structures",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Standard decorators and their context types, how they differ from the legacy experimental ones, and why frameworks still disagree about which to use.. Two decorator designs exist in the wild, they are not compatible, and the `tsconfig` flag that picks between them is easy to inherit by accident. Decorators are ordinary functions; typing context.kind matters in TS.. experimentalDecorators enables the old TypeScript design; new code should prefer the standard model when the toolchain supports it.. Context objects discriminate on kind: class, method, getter, setter, field, accessor.. Do not mix legacy and standard decorators in one project — pick one emit story.. Decorators are ordinary functions; typing context.kind matters in TS.. Decorator wrappers must preserve the method’s return type.. Write identityDecorator that returns the method value unchanged and apply it to ping(): string.. What does experimentalDecorators select?. // JS stage-3 decorators wrap definitions — frameworks differ on details. function logged(value, context) { if (context.kind === \"method\") { return function (...args) { console.log(\"call\", context.name); return value.call(this, ...args); }; } }. // Standard decorator = a function wrapping another function (simplified). type MethodDecorator = <T extends (...args: never[]) => unknown>( value: T, context: { kind: \"method\"; name: string | symbol }, ) => T; const logged: MethodDecorator = (value, context) => { void context.name; return value; }; function ping(): string { return \"pong\"; } const decorated = logged(ping, { kind: \"method\", name: …",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Standard decorators and their context types, how they differ from the legacy experimental ones, and why frameworks still disagree about which to use.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Two decorator designs exist in the wild, they are not compatible, and the `tsconfig` flag that picks between them is easy to inherit by accident. Decorators are ordinary functions; typing context.kind matters in TS.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "default-type-parameters",
      "title": "Default type parameters",
      "category": "Functions & generics",
      "tab": "intermediate",
      "route": "/lessons/functions/default-type-parameters",
      "icon": "ph-function",
      "keywords": [
        "default type parameter",
        "generic default",
        "api design",
        "unknown",
        "functions",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "T = `unknown` gives a generic a sensible zero value, which is how a library stays usable without every call site spelling out its types.. Adding a type parameter to a published type is a breaking change unless it has a default. Box with no default element type. Default type parameters kick in when inference cannot.. Default type parameters kick in when inference cannot.. Useful for option bags and empty collections.. Document defaults — callers may not notice them.. Box with no default element type.. Default T=string when unargued. a.value is string, not number.. function box(value) { return { value }; }. function box<T = string>(value: T): { value: T } { return { value }; } const a = box(\"hi\"); const b = box(1); const c = box(); const bad: number = a.value;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "T = `unknown` gives a generic a sensible zero value, which is how a library stays usable without every call site spelling out its types.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Adding a type parameter to a published type is a breaking change unless it has a default. Box with no default element type. Default type parameters kick in when inference cannot.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "deliberate-unsoundness",
      "title": "Where TypeScript is unsound on purpose",
      "category": "Foundations",
      "tab": "advanced",
      "route": "/lessons/foundations/deliberate-unsoundness",
      "icon": "ph-compass",
      "keywords": [
        "soundness",
        "bivariance",
        "covariance",
        "assertion",
        "trade-off",
        "foundations",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Method parameter bivariance, array covariance, assertions, and `any`. Each hole is a deliberate trade for usability — knowing which is which is the point.. TypeScript sometimes allows operations that are not fully sound — by design, for pragmatism. Treating the type system as a proof assistant leads to confusion when those holes show up.. Soundness holes are documented trade-offs, not bugs — treat them as places needing human review.. Prefer `readonly` T[] / ReadonlyArray when covariance would hurt.. `any` and assertions are explicit unsoundness; keep them at boundaries with comments.. Array aliasing is the classic covariance hole — JS has it raw.. Double assertions bypass the checker — know when you opted out.. Type a `readonly` animal list as ReadonlyArray<Animal> and show a Dog array assignable to it without push.. Why are method parameters bivariant by default?. // JS is unsound by default — types do not exist. const dogs = [{ bark() {} }]; const animals = dogs; animals.push({ meow() {} }); // corrupts the \"dog\" array dogs[1].bark();. type Animal = { tag: \"animal\" }; type Dog = Animal & { bark(): void }; // Arrays are covariant in TypeScript (soundness hole for usability). const dogs: Dog[] = [{ tag: \"animal\", bark() {} }]; const animals: Animal[] = dogs; animals.push({ tag: \"animal\" }); dogs[1].bark(); // Assertions are another deliberate escape hatch: const x = \"hi\" as unknown as number; const y: string = x;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Method parameter bivariance, array covariance, assertions, and `any`. Each hole is a deliberate trade for usability — knowing which is which is the point.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "TypeScript sometimes allows operations that are not fully sound — by design, for pragmatism. Treating the type system as a proof assistant leads to confusion when those holes show up.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "deserialization-attack-surface",
      "title": "Deserialization as an attack surface",
      "category": "The runtime boundary",
      "tab": "advanced",
      "route": "/lessons/runtime-boundary/deserialization-attack-surface",
      "icon": "ph-shield-check",
      "keywords": [
        "prototype pollution",
        "reviver",
        "structuredClone",
        "merge",
        "security",
        "runtime-boundary",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Prototype pollution, `JSON.parse` revivers, `structuredClone`, and merge helpers — the places where parsing untrusted data changes your program rather than describing it.. A deep-merge helper applied to a request body can rewrite `Object.prototype` for the whole process. Merging untrusted keys can pollute `Object.prototype`. Never deep-merge untrusted objects onto prototypes or shared config.. Never deep-merge untrusted objects onto prototypes or shared config.. Allowlist keys; reject __proto__, prototype, and constructor.. Types describe intent after a safe parse — they do not make merge safe.. Merging untrusted keys can pollute `Object.prototype`.. Allowlists + skipped proto keys; name is still string | `undefined`.. Write function isPlainObject(x: `unknown`): x is `Record<string, unknown>` using `typeof` and prototype checks.. Safest approach when accepting JSON objects?. // JS: recursive merge trusts keys like __proto__. function merge(target, source) { for (const key of Object.keys(source)) { target[key] = source[key]; } return target; } merge({}, JSON.parse('{\"__proto__\":{\"polluted\":true}}'));. type Json = string | number | boolean | null | Json[] | { [k: string]: Json }; function parseJson(text: string): Json { return JSON.parse(text) as Json; } const SAFE = new Set([\"name\", \"email\"]); function pickSafe(input: { [k: string]: Json }): { name?: string } { const out: { name?: string } = {}; for (const key of Object.keys(input)) { if (!SAFE.has(key)) continue; if (key === \"__proto__\" || ke…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Prototype pollution, `JSON.parse` revivers, `structuredClone`, and merge helpers — the places where parsing untrusted data changes your program rather than describing it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A deep-merge helper applied to a request body can rewrite `Object.prototype` for the whole process. Merging untrusted keys can pollute `Object.prototype`. Never deep-merge untrusted objects onto prototypes or shared config.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "dirname-and-import-meta",
      "title": "__dirname is gone",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/dirname-and-import-meta",
      "icon": "ph-hard-drives",
      "keywords": [
        "__dirname",
        "import.meta",
        "fileURLToPath",
        "path",
        "esm",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`import.meta`.url, `import.meta`.dirname, and fileURLToPath — replacing the CommonJS path globals without breaking either module system.. Nearly every file-reading helper in a Node codebase reaches for `__dirname`, and that binding simply does not exist in an ES module. The helper still 'looks right' until the first ESM entrypoint loads it and throws. Path math becomes a module-system problem, not a filesystem one.. In ESM, build directory paths from `import.meta`.url via fileURLToPath.. __dirname and __filename are CJS-only bindings.. Keep a small helper so every file does not re-implement the URL dance.. __dirname in a CJS file.. ESM uses a helper from `import.meta`.url. __dirname typed as `undefined` is not a string.. const configPath = path.join(__dirname, \"config.json\");. type PathApi = { join(...parts: string[]): string }; declare const path: PathApi; declare function moduleDir(): string; export const dirname = moduleDir(); export const configPath = path.join(dirname, \"config.json\"); declare const __dirname: undefined; const broken: string = __dirname;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`import.meta`.url, `import.meta`.dirname, and fileURLToPath — replacing the CommonJS path globals without breaking either module system.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Nearly every file-reading helper in a Node codebase reaches for `__dirname`, and that binding simply does not exist in an ES module. The helper still 'looks right' until the first ESM entrypoint loads it and throws. Path math becomes a module-system problem, not a filesystem one.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "discriminated-payloads-and-versioning",
      "title": "Evolving a wire format",
      "category": "The runtime boundary",
      "tab": "advanced",
      "route": "/lessons/runtime-boundary/discriminated-payloads-and-versioning",
      "icon": "ph-shield-check",
      "keywords": [
        "versioning",
        "wire format",
        "message",
        "compatibility",
        "queue",
        "runtime-boundary",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Versioned message unions, forwards- and backwards-compatible shapes, and exhaustiveness checks that fail the build when a producer moves first.. A queue holds messages written by three versions of the producer, and the consumer's type describes only the newest. Missing version branches fail open in JS. Version discriminants belong on the wire; exhaustiveness forces consumer updates.. Version discriminants belong on the wire; exhaustiveness forces consumer updates.. Keep old versions in the union until the queue is drained — do not delete early.. Parse `unknown` JSON into the versioned union before switching.. Missing version branches fail open in JS.. Passing a wider union into an older handler fails the build.. Define Event = { type: \"ping\" } | { type: \"pong\"; n: number } and handle with exhaustiveness.. Why keep MsgV1 in the union after shipping v2?. // JS: consumers switch on version — miss a case, silent drop. function handle(msg) { if (msg.v === 1) return msg.text; if (msg.v === 2) return msg.body; // v3 forgotten }. type MsgV1 = { v: 1; text: string }; type MsgV2 = { v: 2; body: string }; type Msg = MsgV1 | MsgV2; function handle(msg: Msg): string { switch (msg.v) { case 1: return msg.text; case 2: return msg.body; default: { const _exhaustive: never = msg; return _exhaustive; } } } // New version without updating handle: type MsgV3 = { v: 3; html: string }; type MsgAll = Msg | MsgV3; function handleAll(msg:…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Versioned message unions, forwards- and backwards-compatible shapes, and exhaustiveness checks that fail the build when a producer moves first.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A queue holds messages written by three versions of the producer, and the consumer's type describes only the newest. Missing version branches fail open in JS. Version discriminants belong on the wire; exhaustiveness forces consumer updates.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "discriminated-results-in-practice",
      "title": "A service that never throws",
      "category": "Async, errors & Result",
      "tab": "intermediate",
      "route": "/lessons/async/discriminated-results-in-practice",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "Result",
        "boundary",
        "service",
        "error handling",
        "design",
        "async",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Designing a module boundary around Result: where to convert exceptions, how to keep the union small, and when to give up and throw.. Result types used everywhere become noise; used nowhere they become surprises. The boundary is the whole decision. `null` collapses missing and failure. Model domain outcomes as tagged unions.. Model domain outcomes as tagged unions.. Call sites switch on status instead of `null` checks.. Map transport errors separately from not-found.. `null` collapses missing and failure.. Tagged statuses. user only exists on ok.. async function load(id) { const row = await db.get(id); if (!row) return null; return row; }. type User = { id: string }; type LoadResult = | { status: \"ok\"; user: User } | { status: \"missing\" } | { status: \"error\"; message: string }; declare function dbGet(id: string): Promise<User | undefined>; export async function load(id: string): Promise<LoadResult> { try { const row = await dbGet(id); if (!row) return { status: \"missing\" }; return { status: \"ok\", user: row }; } catch { return { sta…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Designing a module boundary around Result: where to convert exceptions, how to keep the union small, and when to give up and throw.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Result types used everywhere become noise; used nowhere they become surprises. The boundary is the whole decision. `null` collapses missing and failure. Model domain outcomes as tagged unions.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "discriminated-unions",
      "title": "Discriminated unions",
      "category": "Types & narrowing",
      "tab": "intermediate",
      "route": "/lessons/types/discriminated-unions",
      "icon": "ph-shapes",
      "keywords": [
        "discriminated union",
        "tagged union",
        "kind",
        "variant",
        "state machine",
        "types",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Give every member of a union a literal tag and the checker can tell them apart. This is the single most useful modelling pattern in TypeScript.. A result object with optional data and optional error lets you construct the impossible state where both are present. Optional fields allow both data and error at once.. Give every variant a literal tag so the checker can discriminate.. Optional data+error models allow impossible states.. Switch on the tag and exhaustiveness checking becomes possible.. Optional fields allow both data and error at once.. Tagged Result forbids mixing ok with error.. Why put a literal `kind` (or similar) on every union member?. function handle(res) { if (res.error) return res.error; return res.data.name; } handle({ data: { name: \"a\" }, error: \"oops\" });. type Ok = { kind: \"ok\"; data: { name: string } }; type Err = { kind: \"err\"; error: string }; type Result = Ok | Err; function handle(res: Result): string { if (res.kind === \"err\") return res.error; return res.data.name; } const bad: Result = { kind: \"ok\", error: \"nope\" };",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Give every member of a union a literal tag and the checker can tell them apart. This is the single most useful modelling pattern in TypeScript.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A result object with optional data and optional error lets you construct the impossible state where both are present. Optional fields allow both data and error at once.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "distributive-conditional-types",
      "title": "Distributive conditional types",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/distributive-conditional-types",
      "icon": "ph-function",
      "keywords": [
        "distributive",
        "conditional type",
        "union",
        "naked type parameter",
        "Exclude",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A naked type parameter distributes over a union, which is why `Exclude` works — and why your conditional type sometimes returns a union you did not ask for.. The same conditional type returns a different answer for a union than for each of its members, and nothing in the syntax hints at it. Runtime filtering does not change how callers type the result.. Distribution happens when the checked type is a naked type parameter (not wrapped in another type constructor).. `Exclude<T, U>` and `Extract<T, U>` rely on distribution: each union member is tested separately.. Write [T] extends [U] (or T[] extends …) when you need the union kept together.. Runtime filtering does not change how callers type the result.. Naked parameters distribute; wrapping in a tuple disables it.. Implement NonNullableish<T> that removes `null` and `undefined` from a union (like `NonNullable`). The solution must type-check with a string assignment.. How do you stop a conditional type from distributing over a union?. // JS mental model: \"filter the union\" is a loop you invent. function excludeNulls(values) { return values.filter((v) => v != null); } // Callers still think the array might contain null. const cleaned = excludeNulls([\"a\", null, \"b\"]); cleaned[0].toUpperCase(); // hope. // Naked T distributes: ToArray<A | B> = ToArray<A> | ToArray<B> type ToArray<T> = T extends unknown ? T[] : never; type Distributed = ToArray<string | number>; // string[] | number[] — not (string | number)[] // Wrap T to turn distribution off: type ToArrayNondist<T> = [T] extends [unknown] ? T[] : never; type Combined = ToArrayNondist<string | number>; // (string | number)[] const oops: Distrib…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A naked type parameter distributes over a union, which is why `Exclude` works — and why your conditional type sometimes returns a union you did not ask for.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The same conditional type returns a different answer for a union than for each of its members, and nothing in the syntax hints at it. Runtime filtering does not change how callers type the result.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "dual-package-hazard",
      "title": "The dual-package hazard",
      "category": "Node.js migration",
      "tab": "advanced",
      "route": "/lessons/node-migration/dual-package-hazard",
      "icon": "ph-hard-drives",
      "keywords": [
        "dual package",
        "conditional exports",
        "cjs",
        "esm",
        "instanceof",
        "node-migration",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Shipping CommonJS and ESM builds means two copies of your module and two copies of its types. What breaks, and the conditions that avoid it.. Two copies of the same class mean `instanceof` fails between them, and TypeScript may treat the types as unrelated even when the source looks identical. Duplicate module instances break identity checks — a classic dual-package hazard when CJS and ESM graphs both load 'the same' library.. Prefer a single module format for libraries when possible; dual publishing needs careful exports.. `instanceof` and singletons are unsafe across duplicated copies.. @arethetypeswrong and Node’s dual-package docs describe the hazard conditions.. Duplicate module instances break identity checks.. Branded copies are not interchangeable — like dual package instances.. Define a single Token class and a function accept(t: Token) used from one module graph only.. Classic dual-package runtime symptom?. // JS dual packages: two evaluations of the same class file. // instanceof across CJS/ESM copies returns false for \"the same\" class.. declare const copyA: unique symbol; declare const copyB: unique symbol; type TokenA = { value: string; readonly [copyA]: void }; type TokenB = { value: string; readonly [copyB]: void }; function acceptA(t: TokenA) { void t.value; } const fromB: TokenB = { value: \"x\", [copyB]: undefined as void }; acceptA(fromB);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Shipping CommonJS and ESM builds means two copies of your module and two copies of its types. What breaks, and the conditions that avoid it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Two copies of the same class mean `instanceof` fails between them, and TypeScript may treat the types as unrelated even when the source looks identical. Duplicate module instances break identity checks — a classic dual-package hazard when CJS and ESM graphs both load 'the same' library.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "editor-driven-development",
      "title": "The editor is the compiler",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/editor-driven-development",
      "icon": "ph-compass",
      "keywords": [
        "tsserver",
        "IDE",
        "completions",
        "refactor",
        "language service",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`tsserver` powers red squiggles, completions, and rename — treat the editor as the same checker CI runs, not a separate opinion.. Fighting the editor — ignoring squiggles, overusing suppressions — turns TypeScript into a CI-only tax. The feedback loop only works if you read diagnostics while you type.. Your editor speaks to `tsserver` using the same TypeScript version and `tsconfig` as CI when configured correctly.. Prefer workspace TypeScript over a global install so local and CI stay aligned (this repo dual-installs `typescript@7` for tooling and `typescript-strada@6.0.3` for `createProgram`).. Use rename symbol and find references — they are type-aware, unlike text search.. Without a project-wide checker, renames are search-and-hope.. The editor error is `tsc`'s error — fix it before you push.. Fix the call to use `accountId`.. Why can the editor and CI disagree?. function renameField(row) { return { user_id: row.userId }; } // Rename userId → accountId in one file; callers elsewhere still use userId. renameField({ userId: \"u_1\" });. type Row = { accountId: string }; function renameField(row: Row) { return { user_id: row.accountId }; } // Same mistake the language service highlights as you type. renameField({ userId: \"u_1\" });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`tsserver` powers red squiggles, completions, and rename — treat the editor as the same checker CI runs, not a separate opinion.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Fighting the editor — ignoring squiggles, overusing suppressions — turns TypeScript into a CI-only tax. The feedback loop only works if you read diagnostics while you type.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "enums-vs-literal-unions",
      "title": "Enums against unions of literals",
      "category": "Types & narrowing",
      "tab": "beginner",
      "route": "/lessons/types/enums-vs-literal-unions",
      "icon": "ph-shapes",
      "keywords": [
        "enum",
        "literal union",
        "const enum",
        "erasable",
        "types",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "enum emits real JavaScript, does not erase, and has surprising assignability. A union of string literals does the same job with none of that.. enum is the feature that looks most familiar to developers arriving from other languages, and it is the one that behaves least like they expect. Object maps still accept `any` string at the call site.. Numeric enums are bidirectional and surprisingly assignable from number — a frequent footgun.. `String` enums require the enum member; bare strings are rejected (as shown).. For most Node apps, a union of string literals (or `as const` objects) is simpler and erases cleanly.. Object maps still accept `any` string at the call site.. Prefer type Role = \"admin\" | \"user\" for erasable string sets.. Replace the enum with a literal union and fix the call.. Why do many TypeScript style guides avoid enum?. const Role = { Admin: \"admin\", User: \"user\" }; function canDelete(role) { return role === Role.Admin; } canDelete(\"Admin\"); // wrong string — silent false. enum Role { Admin = \"admin\", User = \"user\", } function canDelete(role: Role): boolean { return role === Role.Admin; } // String enums are not the same as their string values. canDelete(\"admin\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "enum emits real JavaScript, does not erase, and has surprising assignability. A union of string literals does the same job with none of that.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "enum is the feature that looks most familiar to developers arriving from other languages, and it is the one that behaves least like they expect. Object maps still accept `any` string at the call site.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "equality-narrowing",
      "title": "Narrowing by equality",
      "category": "Types & narrowing",
      "tab": "beginner",
      "route": "/lessons/types/equality-narrowing",
      "icon": "ph-shapes",
      "keywords": [
        "equality",
        "switch",
        "discriminant",
        "===",
        "null",
        "types",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "===, !==, switch, and the double-equals-`null` idiom all narrow. This is the machinery behind every tagged union you will write later.. Comparing against a literal proves something about the value, and JavaScript throws that proof away at the closing brace. Tagged shapes work only if every caller sets kind correctly.. Comparing a discriminant field (kind === \"circle\") narrows the whole object.. switch (shape.kind) does the same, often more readably for many variants.. x == `null` narrows out both `null` and `undefined` in one check.. Tagged shapes work only if every caller sets kind correctly.. Equality on kind discriminates the union.. Narrow on kind before reading radius or width.. What makes kind a good discriminant?. function area(shape) { if (shape.kind === \"circle\") return Math.PI * shape.radius ** 2; return shape.width * shape.height; } area({ kind: \"circle\", radius: 2 });. type Circle = { kind: \"circle\"; radius: number }; type Rect = { kind: \"rect\"; width: number; height: number }; type Shape = Circle | Rect; function area(shape: Shape): number { // Without narrowing, radius is not on Shape. return Math.PI * shape.radius ** 2; }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "===, !==, switch, and the double-equals-`null` idiom all narrow. This is the machinery behind every tagged union you will write later.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Comparing against a literal proves something about the value, and JavaScript throws that proof away at the closing brace. Tagged shapes work only if every caller sets kind correctly.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "erasable-syntax-and-type-stripping",
      "title": "Erasable syntax and native type stripping",
      "category": "Foundations",
      "tab": "intermediate",
      "route": "/lessons/foundations/erasable-syntax-and-type-stripping",
      "icon": "ph-compass",
      "keywords": [
        "type stripping",
        "erasableSyntaxOnly",
        "node",
        "enum",
        "no build",
        "amaro",
        "foundations",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Node can now run `.ts` files by deleting the types. What that rules out — enums, parameter properties, namespaces — and why `erasableSyntaxOnly` exists.. Some TypeScript syntax emits runtime code (enums, namespaces, parameter properties in older modes); other syntax is erasable. Mixing them without knowing which is which surprises Node's type-stripping workflows.. Type-only syntax can be stripped without a full emit pipeline.. Enums, namespaces, and parameter properties may need transformation.. Prefer erasable forms when targeting Node type stripping.. Enums/namespaces need transform — not erasable.. Annotations strip cleanly. length returns number.. // Node type stripping runs without emit. // Erasable syntax: types, annotations, import type. type Point = { x: number; y: number }; export function length(p: Point): number { return Math.hypot(p.x, p.y); } const n: number = length({ x: 3, y: 4 }); const bad: string = length({ x: 3, y: 4 });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Node can now run `.ts` files by deleting the types. What that rules out — enums, parameter properties, namespaces — and why `erasableSyntaxOnly` exists.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Some TypeScript syntax emits runtime code (enums, namespaces, parameter properties in older modes); other syntax is erasable. Mixing them without knowing which is which surprises Node's type-stripping workflows.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "error-cause-and-chaining",
      "title": "Error.cause",
      "category": "Async, errors & Result",
      "tab": "intermediate",
      "route": "/lessons/async/error-cause-and-chaining",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "cause",
        "error chaining",
        "wrapping",
        "context",
        "unknown",
        "async",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Wrapping a low-level failure in a domain error without losing it. How cause is typed, and why `unknown` is the honest type for it.. Catching and rethrowing with a friendlier message throws away the only stack trace that pointed at the real failure. `String`-concatenating nested errors. Use the cause option to chain errors without losing the stack.. Use the cause option to chain errors without losing the stack.. cause is `unknown` — narrow before reading.. Preserve causes across async boundaries.. `String`-concatenating nested errors.. Error.cause is `unknown` — not string.. throw new Error('failed: ' + err);. declare const root: Error; const wrapped = new Error(\"failed\", { cause: root }); const c: unknown = wrapped.cause; const bad: string = wrapped.cause;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Wrapping a low-level failure in a domain error without losing it. How cause is typed, and why `unknown` is the honest type for it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Catching and rethrowing with a friendlier message throws away the only stack trace that pointed at the real failure. `String`-concatenating nested errors. Use the cause option to chain errors without losing the stack.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "eslint-with-typescript",
      "title": "ESLint and TypeScript together",
      "category": "Tooling & the strictness ladder",
      "tab": "beginner",
      "route": "/lessons/tooling/eslint-with-typescript",
      "icon": "ph-wrench",
      "keywords": [
        "eslint",
        "typescript-eslint",
        "lint",
        "floating promises",
        "tooling",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "typescript-eslint rules catch what types allow but style forbids — like floating promises and unsafe `any`.. `tsc` is green but someone left an awaited promise floating and an eslint-disable for no-explicit-`any` on every file. Unhandled rejection risk — lint can require await/`void`.. Let `tsc` own type correctness; let typescript-eslint own footguns like floating promises.. Prefer @typescript-eslint/no-explicit-`any` with disciplined exceptions.. Do not disable entire rule sets to silence one file — fix or narrow the disable.. Unhandled rejection risk — lint can require await/`void`.. Types alone do not enforce that you await; lint complements `tsc`.. Await save or type the result as `Promise<void>`.. What does ESLint add on top of `tsc`?. async function save(row) { await dbWrite(row); } function dbWrite(row) { return Promise.resolve(row); } save({ id: 1 }); // fire-and-forget in a route handler. async function save(row: { id: number }): Promise<void> { await Promise.resolve(row); } // Typed — but still a floating promise if the caller ignores it. export const ignored: void = save({ id: 1 });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "typescript-eslint rules catch what types allow but style forbids — like floating promises and unsafe `any`.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "`tsc` is green but someone left an awaited promise floating and an eslint-disable for no-explicit-`any` on every file. Unhandled rejection risk — lint can require await/`void`.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "esm-imports-and-exports",
      "title": "Modules, imports, and type-only imports",
      "category": "Objects, classes & modules",
      "tab": "beginner",
      "route": "/lessons/structures/esm-imports-and-exports",
      "icon": "ph-cube",
      "keywords": [
        "esm",
        "import type",
        "export",
        "modules",
        "structures",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "export/import in TypeScript modules, and import type so type-only dependencies stay erasable under `isolatedModules`.. A value import of a type-only symbol breaks emit under `isolatedModules` / `verbatimModuleSyntax`. Module format mismatches fail at runtime, not design time. Prefer named ESM exports over default for tree-shaking clarity.. Prefer named ESM exports over default for tree-shaking clarity.. import type { User } makes the import type-only — erased, safe with `isolatedModules`.. Keep runtime values and types distinguished when `verbatimModuleSyntax` is on.. Module format mismatches fail at runtime, not design time.. Real projects resolve this via relative ESM paths + types.. Inline a User type so the file has no external import.. When should you write import type?. // CJS leaking into an ESM package — runtime ERR_REQUIRE_ESM. const { User } = require(\"./user\"); module.exports.make = () => new User();. // In this single-file sandbox there is no \"./user\" module. import { User } from \"./user\"; export function label(u: User): string { return u.name; }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "export/import in TypeScript modules, and import type so type-only dependencies stay erasable under `isolatedModules`.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A value import of a type-only symbol breaks emit under `isolatedModules` / `verbatimModuleSyntax`. Module format mismatches fail at runtime, not design time. Prefer named ESM exports over default for tree-shaking clarity.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "esm-interop",
      "title": "Interop between the two module systems",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/esm-interop",
      "icon": "ph-hard-drives",
      "keywords": [
        "esModuleInterop",
        "default import",
        "createRequire",
        "interop",
        "namespace",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`esModuleInterop`, default imports of CommonJS packages, createRequire, and why import x from 'cjs-pkg' sometimes gives you the namespace.. The same import statement can resolve to the module namespace or to its default export depending on flags set three configs away. Calling a CJS helper 'directly' works in one setup and yields `… is not a function` in another. Interop bugs feel random until you normalize the boundary once.. CJS packages may need `esModuleInterop` or import = require depending on export style.. Runtime shape can be module or module.default — normalize once at the boundary.. Check emitted JS when bundlers and `tsc` disagree about interop.. require a CJS helper and call it directly.. Normalize default-vs-namespace interop. pad returns string, not number.. const legacy = require(\"legacy-lib\"); legacy.pad(\"x\", 3);. type Legacy = { pad(s: string, n: number): string }; type Interop<T> = T | { default: T }; declare const imported: Interop<Legacy>; function asLegacy(mod: Interop<Legacy>): Legacy { return \"default\" in mod ? mod.default : mod; } const api = asLegacy(imported); const ok: string = api.pad(\"x\", 3); const bad: number = api.pad(\"x\", 3);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`esModuleInterop`, default imports of CommonJS packages, createRequire, and why import x from 'cjs-pkg' sometimes gives you the namespace.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The same import statement can resolve to the module namespace or to its default export depending on flags set three configs away. Calling a CJS helper 'directly' works in one setup and yields `… is not a function` in another. Interop bugs feel random until you normalize the boundary once.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "exactoptionalpropertytypes",
      "title": "exactOptionalPropertyTypes",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/exactoptionalpropertytypes",
      "icon": "ph-wrench",
      "keywords": [
        "exactOptionalPropertyTypes",
        "optional",
        "undefined",
        "absent",
        "patch",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Distinguishes an absent property from one explicitly set to `undefined`. Correct, occasionally infuriating, and worth understanding before you enable it.. A PATCH body where a field is missing means leave it alone, and where it is `null` means clear it — and one type covers both. `undefined` written into optional fields casually.. `exactOptionalPropertyTypes` distinguishes missing from `undefined`.. Useful for APIs where `undefined` means something different.. Enable carefully — it is not part of `strict`.. `undefined` written into optional fields casually.. Options.timeout?: number is not number | `undefined` for fresh calls under `exactOptionalPropertyTypes` — modeled here as a mismatch.. const opts = { timeout: undefined };. // Model exact optional: optional prop cannot be explicitly undefined type Options = { timeout?: number }; type ExactOptions = { timeout?: number } & { timeout?: number }; function start(opts: { timeout: number | undefined }): void {} const loose: Options = {}; start(loose);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Distinguishes an absent property from one explicitly set to `undefined`. Correct, occasionally infuriating, and worth understanding before you enable it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A PATCH body where a field is missing means leave it alone, and where it is `null` means clear it — and one type covers both. `undefined` written into optional fields casually.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "excess-property-checks",
      "title": "Excess property checks",
      "category": "Objects, classes & modules",
      "tab": "intermediate",
      "route": "/lessons/structures/excess-property-checks",
      "icon": "ph-cube",
      "keywords": [
        "excess property",
        "freshness",
        "object literal",
        "typo",
        "assignability",
        "structures",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A fresh object literal is checked more strictly than a variable holding the same object. That asymmetry is deliberate, and it explains a whole class of confusing errors.. Passing { timeout: 100 } errors on a typo'd key, and hoisting it to a variable makes the error disappear without fixing anything. Extra fields slide through. Excess property checks apply to fresh object literals.. Excess property checks apply to fresh object literals.. Assigning through a variable bypasses them — beware.. Use `satisfies` or exact types when extras must be rejected.. Extra fields slide through.. Fresh literals get excess property checks; variables can widen past them.. paint({ colour: 'red', gloss: true });. type Paint = { colour: string }; function paint(p: Paint): void {} paint({ colour: \"red\", gloss: true }); const opts = { colour: \"red\", gloss: true }; paint(opts);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A fresh object literal is checked more strictly than a variable holding the same object. That asymmetry is deliberate, and it explains a whole class of confusing errors.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Passing { timeout: 100 } errors on a typo'd key, and hoisting it to a variable makes the error disappear without fixing anything. Extra fields slide through. Excess property checks apply to fresh object literals.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "exhaustiveness-checking",
      "title": "Exhaustiveness checking",
      "category": "Types & narrowing",
      "tab": "intermediate",
      "route": "/lessons/types/exhaustiveness-checking",
      "icon": "ph-shapes",
      "keywords": [
        "never",
        "exhaustive",
        "switch",
        "assertNever",
        "default case",
        "types",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Assign the narrowed value to `never` in the default branch and the compiler tells you the day someone adds a variant you did not handle.. Adding a case to a union is a one-line change; finding the nine switch statements that needed updating is not. Missing triangle case returns `undefined`. AssertNever(shape) in the default branch forces new variants to be handled.. assertNever(shape) in the default branch forces new variants to be handled.. Without it, forgetting a case returns `undefined` silently in JS.. Prefer switch over if-chains for tagged unions.. Missing triangle case returns `undefined`.. default: assertNever(shape) errors until triangle is handled.. Why call `assertNever(shape)` in a switch default?. function area(shape) { if (shape.kind === \"circle\") return Math.PI * shape.r ** 2; if (shape.kind === \"square\") return shape.size ** 2; }. type Shape = | { kind: \"circle\"; r: number } | { kind: \"square\"; size: number } | { kind: \"triangle\"; base: number; height: number }; function assertNever(x: never): never { throw new Error(\"unexpected\"); } function area(shape: Shape): number { switch (shape.kind) { case \"circle\": return Math.PI * shape.r ** 2; case \"square\": return shape.size ** 2; default: return assertNever(shape); } }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Assign the narrowed value to `never` in the default branch and the compiler tells you the day someone adds a variant you did not handle.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Adding a case to a union is a one-line change; finding the nine switch statements that needed updating is not. Missing triangle case returns `undefined`. AssertNever(shape) in the default branch forces new variants to be handled.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "extending-interfaces",
      "title": "Extending an interface",
      "category": "Objects, classes & modules",
      "tab": "beginner",
      "route": "/lessons/structures/extending-interfaces",
      "icon": "ph-cube",
      "keywords": [
        "extends",
        "inheritance",
        "interface",
        "structures",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "interface Child extends Parent adds fields while keeping assignability to the parent type.. Admin users need a role field; copying the whole User interface guarantees the next User change is forgotten on Admin. role is assumed; missing means `undefined` in the log line.. Child is assignable to Parent when it only adds fields (structural).. Multiple extends is allowed: interface C extends A, B.. Prefer extends over copy-paste when modeling specialization.. role is assumed; missing means `undefined` in the log line.. Extends pulls in User fields and still requires role.. Include role: \"admin\" in the call.. Is Admin assignable to User?. function audit(actor) { return actor.id + \":\" + actor.role; } audit({ id: \"1\", name: \"Ada\" });. interface User { id: string; name: string; } interface Admin extends User { role: \"admin\"; } function audit(actor: Admin): string { return actor.id + \":\" + actor.role; } audit({ id: \"1\", name: \"Ada\" });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "interface Child extends Parent adds fields while keeping assignability to the parent type.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Admin users need a role field; copying the whole User interface guarantees the next User change is forgotten on Admin. role is assumed; missing means `undefined` in the log line.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "first-type-error",
      "title": "Your first type error",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/first-type-error",
      "icon": "ph-compass",
      "keywords": [
        "tsc",
        "cli",
        "noEmit",
        "checkJs",
        "first error",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Point `tsc` at a JavaScript file you already have, watch it disagree with you, and learn the shortest loop between writing a line and being told it is wrong.. The first red squiggle feels like the tool is broken — especially when the same JavaScript 'worked' yesterday. What actually happened is that a shape mismatch finally has a name and a location.. Read a TypeScript error bottom-up: expected type, actual type, then the expression on the flagged line.. The error code (here 2345) is stable; the prose may change between TypeScript releases.. Fix the types at the boundary (parse the query string) rather than widening the parameter to string | number.. `String` coercion turns tax math into concatenation or NaN territory.. `TS2345`: the argument type must match the parameter.. Make addTax compile: parse the string before calling.. What does `TS2345` mean in this lesson?. function addTax(amount) { return amount * 1.2; } // Price arrived as a string from a query param. addTax(\"19.99\");. function addTax(amount: number): number { return amount * 1.2; } addTax(\"19.99\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Point `tsc` at a JavaScript file you already have, watch it disagree with you, and learn the shortest loop between writing a line and being told it is wrong.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The first red squiggle feels like the tool is broken — especially when the same JavaScript 'worked' yesterday. What actually happened is that a shape mismatch finally has a name and a location.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "floating-promises-and-void",
      "title": "Floating promises",
      "category": "Async, errors & Result",
      "tab": "advanced",
      "route": "/lessons/async/floating-promises-and-void",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "floating promise",
        "no-floating-promises",
        "void operator",
        "unhandled rejection",
        "async",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "An un-awaited promise is a lost rejection and, since Node 15, a crashed process. How the checker and the linter find them, and what `void` is for.. Forgetting one await turns an error path into an unhandled rejection that takes the whole process down. Forgotten await is a process-level hazard in modern Node.. TypeScript itself does not error on floating promises; @typescript-eslint/no-floating-promises does.. `void` promise is the intentional escape hatch those rules recognize.. Prefer await in async functions; only `void` when the rejection is handled elsewhere.. Forgotten await is a process-level hazard in modern Node.. `Promise<void>` is not a string — and floating calls need discipline.. Write async function run(): `Promise<number>` that returns 1, and await it in main.. What is the `void` operator used for with promises?. // JS: fire-and-forget is easy — and rejects crash Node. async function write(path, data) { throw new Error(\"disk full\"); } write(\"/tmp/x\", \"hi\"); // floating — rejection may be unhandled. async function write(_path: string, _data: string): Promise<void> { throw new Error(\"disk full\"); } async function main() { // Floating promise: callers should await or explicitly void. write(\"/tmp/x\", \"hi\"); await write(\"/tmp/y\", \"ok\"); } // void marks intentional fire-and-forget for lint rules. void write(\"/tmp/z\", \"bg\"); const p: Promise<void> = main(); const wrong: string = p;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "An un-awaited promise is a lost rejection and, since Node 15, a crashed process. How the checker and the linter find them, and what `void` is for.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Forgetting one await turns an error path into an unhandled rejection that takes the whole process down. Forgotten await is a process-level hazard in modern Node.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "function-overloads",
      "title": "Overloads",
      "category": "Functions & generics",
      "tab": "intermediate",
      "route": "/lessons/functions/function-overloads",
      "icon": "ph-function",
      "keywords": [
        "overload",
        "signature",
        "implementation signature",
        "union",
        "api design",
        "functions",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Several signatures over one implementation, when the return type depends on the arguments — and the union or generic that is usually better.. readFile returns a Buffer or a string depending on an options field, and one signature cannot say that honestly. One function, two call shapes, no guidance. List public overload signatures, then one implementation signature.. List public overload signatures, then one implementation signature.. Keep overloads minimal — unions often suffice.. Implementation signature must accept every overload input.. One function, two call shapes, no guidance.. Overload signatures document call forms. boolean is not accepted.. function read(x) { if (typeof x === 'number') return items[x]; return items.find(i => i.id === x); }. type Item = { id: string; name: string }; declare const items: Item[]; function read(id: number): Item | undefined; function read(id: string): Item | undefined; function read(id: number | string): Item | undefined { if (typeof id === \"number\") return items[id]; return items.find((i) => i.id === id); } const a = read(0); const b = read(\"1\"); const c: number = read(true);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Several signatures over one implementation, when the return type depends on the arguments — and the union or generic that is usually better.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "readFile returns a Buffer or a string depending on an options field, and one signature cannot say that honestly. One function, two call shapes, no guidance. List public overload signatures, then one implementation signature.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "function-type-expressions",
      "title": "Typing a callback",
      "category": "Functions & generics",
      "tab": "beginner",
      "route": "/lessons/functions/function-type-expressions",
      "icon": "ph-function",
      "keywords": [
        "callback",
        "function type",
        "=>",
        "higher-order",
        "functions",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Write (x: T) => U as a type, pass it to map/filter-style helpers, and catch callbacks that return the wrong thing.. A callback that was supposed to return a boolean returns a string; Array.prototype.filter still 'works' with surprising results. Truthy strings keep everything — not what you meant.. Function type expressions describe parameters and return without naming an implementation.. Generics on the helper (keep<T>) tie the callback's value type to the array element type.. Prefer named alias types for callbacks you reuse across modules.. Truthy strings keep everything — not what you meant.. The callback must return boolean, not string.. Return a boolean from the predicate.. What does (value: T) => boolean describe?. function keep(items, predicate) { return items.filter(predicate); } keep([1, 2, 3], (n) => String(n));. type Predicate<T> = (value: T) => boolean; function keep<T>(items: T[], predicate: Predicate<T>): T[] { return items.filter(predicate); } keep([1, 2, 3], (n) => String(n));",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Write (x: T) => U as a type, pass it to map/filter-style helpers, and catch callbacks that return the wrong thing.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A callback that was supposed to return a boolean returns a string; Array.prototype.filter still 'works' with surprising results. Truthy strings keep everything — not what you meant.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "function-utility-types",
      "title": "Utility types for functions",
      "category": "Type-level programming",
      "tab": "intermediate",
      "route": "/lessons/type-level/function-utility-types",
      "icon": "ph-function",
      "keywords": [
        "ReturnType",
        "Parameters",
        "Awaited",
        "signature",
        "derive",
        "type-level",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`ReturnType`, `Parameters`, `ConstructorParameters`, `ThisParameterType`, `Awaited` — deriving types from a signature you already have.. A wrapper around someone else's function restates its argument list, and the restatement is wrong within a release. Extracting callback shapes by hand. `Parameters` and `ReturnType` extract call signatures.. `Parameters` and `ReturnType` extract call signatures.. `ConstructorParameters` / `InstanceType` do the same for classes.. Use them to stay DRY with third-party function types.. Extracting callback shapes by hand.. `Parameters<Fn>`[0] is number, not string.. function call(fn, arg) { return fn(arg); }. type Fn = (n: number) => string; type Arg = Parameters<Fn>[0]; type Ret = ReturnType<Fn>; const a: Arg = 1; const r: Ret = \"x\"; const bad: Arg = \"1\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`ReturnType`, `Parameters`, `ConstructorParameters`, `ThisParameterType`, `Awaited` — deriving types from a signature you already have.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A wrapper around someone else's function restates its argument list, and the restatement is wrong within a release. Extracting callback shapes by hand. `Parameters` and `ReturnType` extract call signatures.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "generated-types-from-contracts",
      "title": "Generating types from a contract",
      "category": "The runtime boundary",
      "tab": "advanced",
      "route": "/lessons/runtime-boundary/generated-types-from-contracts",
      "icon": "ph-shield-check",
      "keywords": [
        "openapi",
        "protobuf",
        "graphql",
        "codegen",
        "contract",
        "drift",
        "runtime-boundary",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "OpenAPI, protobuf, GraphQL, and SQL schemas can all emit TypeScript. What that guarantees, what it does not, and where the drift moves to.. Generated types describe the contract as it was when the generator last ran, which is not necessarily the contract the server is serving. No contract artifact means no mechanical sync.. Codegen removes transcription bugs; it does not prove the server still matches.. CI should regenerate and fail on drift, or validate responses at runtime.. Prefer generating both types and validators from one schema when possible.. No contract artifact means no mechanical sync.. Generated DTO types still need a parse step — casts lie.. Define OrderDto with id: string and totalCents: number. Write a function that accepts OrderDto.. What does OpenAPI→TS codegen guarantee by itself?. // JS: hand-written clients drift from the server silently. async function getUser(id) { const res = await fetch(\"/users/\" + id); return res.json(); }. // Illustrative generated types — treat as a snapshot of the contract. type UserDto = { id: string; email: string }; declare function getJson(url: string): Promise<unknown>; // Runtime still returns unknown-shaped JSON unless you validate. async function getUser(id: string): Promise<UserDto> { const data: unknown = await getJson(\"/users/\" + id); return data as UserDto; } const u = await getUser(\"…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "OpenAPI, protobuf, GraphQL, and SQL schemas can all emit TypeScript. What that guarantees, what it does not, and where the drift moves to.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Generated types describe the contract as it was when the generator last ran, which is not necessarily the contract the server is serving. No contract artifact means no mechanical sync.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "generic-async-wrappers",
      "title": "Wrappers that keep the signature",
      "category": "Async, errors & Result",
      "tab": "advanced",
      "route": "/lessons/async/generic-async-wrappers",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "retry",
        "timeout",
        "wrapper",
        "decorator",
        "variadic tuple",
        "generic",
        "async",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "withRetry, withTimeout, withSpan — decorating an async function so the wrapped signature, including its generics, survives intact.. A retry helper typed with (...args: `any`[]) => `any` turns every wrapped function into an untyped one. Untyped wrappers erase parameter and return information.. Capture parameters as a tuple type A extends `unknown`[] and the Promise result as R.. Avoid `any` in wrapper signatures — that is how types leak out of a whole call graph.. The same pattern types withTimeout / withSpan / withLogging.. Untyped wrappers erase parameter and return information.. Variadic A and R keep the wrapped async signature.. Write withLog that wraps (...args: A) => `Promise<R>` and returns the same signature, calling fn once.. Why use A extends `unknown`[] instead of `any`[] for wrapper args?. // JS: wrap and hope. function withRetry(fn) { return async (...args) => { try { return await fn(...args); } catch { return await fn(...args); } }; }. function withRetry<A extends unknown[], R>( fn: (...args: A) => Promise<R>, ): (...args: A) => Promise<R> { return async (...args: A) => { try { return await fn(...args); } catch { return await fn(...args); } }; } async function loadUser(id: string): Promise<{ id: string }> { return { id }; } const load = withRetry(loadUser); const user = await load(\"u1\"); const bad: number = user.id;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "withRetry, withTimeout, withSpan — decorating an async function so the wrapped signature, including its generics, survives intact.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A retry helper typed with (...args: `any`[]) => `any` turns every wrapped function into an untyped one. Untyped wrappers erase parameter and return information.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "generic-classes-and-interfaces",
      "title": "Generic classes and interfaces",
      "category": "Objects, classes & modules",
      "tab": "intermediate",
      "route": "/lessons/structures/generic-classes-and-interfaces",
      "icon": "ph-cube",
      "keywords": [
        "generic class",
        "container",
        "repository",
        "cache",
        "type parameter",
        "structures",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Type parameters on a container: repositories, caches, and queues that remember what they hold.. A cache typed with `any` is a cache that returns `any`, and every read site loses its type. Box erases the wrapped type. Generic classes keep element types on the instance.. Generic classes keep element types on the instance.. Interfaces can be generic the same way.. Infer T from the constructor argument when possible.. Box erases the wrapped type.. Box<number>.value is number, not string.. class Box { constructor(value) { this.value = value; } }. class Box<T> { constructor(public value: T) {} } const b = new Box(1); const n: number = b.value; const bad: string = b.value;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Type parameters on a container: repositories, caches, and queues that remember what they hold.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A cache typed with `any` is a cache that returns `any`, and every read site loses its type. Box erases the wrapped type. Generic classes keep element types on the instance.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "generic-constraints",
      "title": "Constraining a type parameter",
      "category": "Functions & generics",
      "tab": "intermediate",
      "route": "/lessons/functions/generic-constraints",
      "icon": "ph-function",
      "keywords": [
        "extends",
        "constraint",
        "bounded",
        "keyof",
        "generic",
        "functions",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "T extends { id: string } lets a generic function actually use its argument. Constraints are how a hole gets a shape without losing the caller's specifics.. An unconstrained type parameter can be anything, so the function body can do nothing with it. Reading .length on anything. Extends constrains what T can be. Keep that refusal in the editor — not in a later incident review.. extends constrains what T can be.. Constraints unlock property access inside the function.. Prefer precise constraints over `any`.. Reading .length on anything.. Constraint requires length. number does not qualify.. function len(x) { return x.length; } len(1);. function len<T extends { length: number }>(x: T): number { return x.length; } len(\"hi\"); len([1, 2]); len(1);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "T extends { id: string } lets a generic function actually use its argument. Constraints are how a hole gets a shape without losing the caller's specifics.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "An unconstrained type parameter can be anything, so the function body can do nothing with it. Reading .length on anything. Extends constrains what T can be. Keep that refusal in the editor — not in a later incident review.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "generic-inference-internals",
      "title": "How inference picks a type argument",
      "category": "Functions & generics",
      "tab": "advanced",
      "route": "/lessons/functions/generic-inference-internals",
      "icon": "ph-function",
      "keywords": [
        "inference",
        "candidate",
        "priority",
        "supertype",
        "generic",
        "functions",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Inference sites, candidate collection, priorities, and the common supertype rule. Enough of the algorithm to predict what T will be.. Reordering two parameters changes the inferred type, and nothing in the signature says it should. Without inference rules, paired values share no declared relationship.. Each argument position that mentions T is an inference site; candidates are collected then combined.. When candidates disagree, TypeScript often takes a common supertype (a union), not the first argument alone.. An explicit type argument disables inference for that parameter and checks arguments against it.. Without inference rules, paired values share no declared relationship.. An explicit T checks arguments; inferred unions must be stated or formed carefully.. Write choose<T>(a: T, b: T): T that returns a. Call it with two strings.. What is T in pair(1, \"x\") for pair<T>(a: T, b: T)?. // JS: \"identity\" helpers just return what you pass — no shared T. function pair(a, b) { return [a, b]; } const p = pair(1, \"x\"); // [any, any] vibe in untyped code. function pair<T>(a: T, b: T): [T, T] { return [a, b]; } // Explicit annotation wins — \"x\" is checked against number. const q = pair<number>(1, \"x\"); // Inferred calls collect candidates into a common supertype: const p = pair<string | number>(1, \"x\"); const ok: string | number = p[0]; void ok;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Inference sites, candidate collection, priorities, and the common supertype rule. Enough of the algorithm to predict what T will be.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Reordering two parameters changes the inferred type, and nothing in the signature says it should. Without inference rules, paired values share no declared relationship.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "generic-utility-functions",
      "title": "Writing generic helpers",
      "category": "Functions & generics",
      "tab": "intermediate",
      "route": "/lessons/functions/generic-utility-functions",
      "icon": "ph-function",
      "keywords": [
        "utility",
        "pluck",
        "groupBy",
        "keyof",
        "generic",
        "lodash",
        "functions",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "pluck, groupBy, keyBy — the small utilities every Node codebase reinvents, typed so they return something more useful than `any`.. The hand-rolled groupBy at the bottom of utils.js returns an object of arrays of anything, forever. pick returns a plain object with no key relationship. `keyof` + generics model dictionary utilities safely.. `keyof` + generics model dictionary utilities safely.. Return `Pick`/`Omit` rather than a loose record.. This is how lodash-style helpers become honest.. pick returns a plain object with no key relationship.. `Pick<T,K>` only has selected keys — age is gone.. function pick(obj, keys) { const out = {}; for (const k of keys) out[k] = obj[k]; return out; }. function pick<T extends object, K extends keyof T>( obj: T, keys: K[], ): Pick<T, K> { const out = {} as Pick<T, K>; for (const k of keys) out[k] = obj[k]; return out; } const user = { id: \"1\", name: \"Ada\", age: 36 }; const idName = pick(user, [\"id\", \"name\"]); const age: number = idName.age;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "pluck, groupBy, keyBy — the small utilities every Node codebase reinvents, typed so they return something more useful than `any`.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The hand-rolled groupBy at the bottom of utils.js returns an object of arrays of anything, forever. pick returns a plain object with no key relationship. `keyof` + generics model dictionary utilities safely.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "generics-intro",
      "title": "Generics: keeping the caller's type",
      "category": "Functions & generics",
      "tab": "intermediate",
      "route": "/lessons/functions/generics-intro",
      "icon": "ph-function",
      "keywords": [
        "generic",
        "type parameter",
        "identity",
        "reuse",
        "inference",
        "functions",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A type parameter is a hole the caller fills. The difference between a function that takes `any` and one that takes T is the difference between forgetting and remembering.. A helper typed with `any` hands back `any`, so one utility function erases types across the entire codebase that uses it. One function, many shapes — no relationship preserved.. Generics preserve relationships between inputs and outputs.. Let inference work — annotate when the relationship matters.. Start with one type parameter before adding constraints.. One function, many shapes — no relationship preserved.. Generic T ties input to output. string is not number.. What does identity<T>(x: T): T preserve that identity(x: `any`): `any` loses?. function identity(x) { return x; } const n = identity(1); const s = identity('a');. function identity<T>(x: T): T { return x; } const n: number = identity(1); const s: string = identity(\"a\"); const bad: number = identity(\"a\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A type parameter is a hole the caller fills. The difference between a function that takes `any` and one that takes T is the difference between forgetting and remembering.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A helper typed with `any` hands back `any`, so one utility function erases types across the entire codebase that uses it. One function, many shapes — no relationship preserved.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "global-augmentation-for-node",
      "title": "Augmenting globals",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/global-augmentation-for-node",
      "icon": "ph-hard-drives",
      "keywords": [
        "declare global",
        "globalThis",
        "Express.Request",
        "d.ts",
        "augmentation",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`declare` global for globalThis, adding a user to Express's Request, and doing it in a .d.ts that does not accidentally become a module.. A declaration file that gains a single import stops being ambient, and every `declare global` augmentation in it silently stops applying. Teams stash state on `global` without declaring it, then wonder why the editor and `tsc` disagree about what exists.. Augment globals from a module file (export {} if needed).. Prefer explicit imports over ambient globals for app state.. Keep augmentations minimal — they apply everywhere.. Stashing state on global without declaring it.. `declare` global adds appCache. A Map is not a number.. global.cache = new Map();. export {}; declare global { // eslint-disable-next-line no-var var appCache: Map<string, string> | undefined; } appCache = new Map(); const hit: string | undefined = appCache.get(\"k\"); const wrong: number = appCache;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`declare` global for globalThis, adding a user to Express's Request, and doing it in a .d.ts that does not accidentally become a module.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A declaration file that gains a single import stops being ambient, and every `declare global` augmentation in it silently stops applying. Teams stash state on `global` without declaring it, then wonder why the editor and `tsc` disagree about what exists.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "higher-order-generic-signatures",
      "title": "Generic functions as values",
      "category": "Functions & generics",
      "tab": "advanced",
      "route": "/lessons/functions/higher-order-generic-signatures",
      "icon": "ph-function",
      "keywords": [
        "higher order",
        "instantiation expression",
        "generic value",
        "hkt",
        "functions",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Passing a generic function without instantiating it, instantiation expressions, and where higher-kinded types would help if TypeScript had them.. Storing a generic function in a variable collapses it to one instantiation, and the abstraction you were building disappears. Higher-order helpers in JS carry no type parameters as values.. Generic function values stay generic when passed to higher-order functions that expect `<T>(…) => …`.. Instantiation expressions (`fn<Type>`) fix type arguments without invoking the function.. TypeScript lacks higher-kinded types; encode patterns with generics on functions, not type constructors as values.. Higher-order helpers in JS carry no type parameters as values.. Once collapsed to a concrete signature, other T values are rejected.. Create const asNumber = identity<number> and call it with 42.. What does `identity<string>` (without call parentheses) produce?. // JS: map is generic in spirit but values are untyped. const map = (arr, fn) => arr.map(fn); const mapped = map([1, 2], (n) => String(n));. function identity<T>(x: T): T { return x; } // Instantiation expression: fix T without calling. const idString = identity<string>; const s = idString(\"ok\"); // Assigning the generic to a non-generic variable collapses it: const collapsed: (x: string) => string = identity; const n = collapsed(1);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Passing a generic function without instantiating it, instantiation expressions, and where higher-kinded types would help if TypeScript had them.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Storing a generic function in a variable collapses it to one instantiation, and the abstraction you were building disappears. Higher-order helpers in JS carry no type parameters as values.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "implements-vs-extends",
      "title": "implements against extends",
      "category": "Objects, classes & modules",
      "tab": "intermediate",
      "route": "/lessons/structures/implements-vs-extends",
      "icon": "ph-cube",
      "keywords": [
        "implements",
        "extends",
        "inheritance",
        "structural",
        "contract",
        "structures",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "implements checks a class against a shape without inheriting anything; extends inherits. Structural typing means you often need neither.. Developers arriving from nominal languages reach for implements to make a class assignable, which it never needed. extends used for everything. Extends inherits implementation; implements only checks shape.. extends inherits implementation; implements only checks shape.. A class can implement multiple interfaces.. Failing implements is a compile error on the class body.. extends used for everything.. implements checks the shape; bark returns string.. class Dog extends Animal {}. interface CanBark { bark(): string; } class Animal { move(): string { return \"walk\"; } } class Dog extends Animal implements CanBark { bark(): string { return \"woof\"; } } const d = new Dog(); const s: string = d.bark(); const bad: number = d.bark();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "implements checks a class against a shape without inheriting anything; extends inherits. Structural typing means you often need neither.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Developers arriving from nominal languages reach for implements to make a class assignable, which it never needed. extends used for everything. Extends inherits implementation; implements only checks shape.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "in-operator-narrowing",
      "title": "Narrowing with in",
      "category": "Types & narrowing",
      "tab": "intermediate",
      "route": "/lessons/types/in-operator-narrowing",
      "icon": "ph-shapes",
      "keywords": [
        "in operator",
        "narrowing",
        "property presence",
        "duck typing",
        "types",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "'id' in value narrows a union by property presence — useful when you do not own the shapes and cannot add a tag.. Third-party union types rarely come with a discriminant, so branching on them needs a different proof. Runtime key check without types. `in` narrows unions when members have distinct keys.. `in` narrows unions when members have distinct keys.. It still follows the prototype chain — pair with untrusted-object lessons.. Prefer discriminated unions when you control the data model.. Runtime key check without types.. `in` narrows the true branch to Dog; the else is Cat, so bark() errors.. When is `\"bark\" in animal` a good narrowing tool?. function label(pet) { if (\"bark\" in pet) return pet.bark(); return pet.meow(); }. type Dog = { bark(): string }; type Cat = { meow(): string }; function label(pet: Dog | Cat): string { if (\"bark\" in pet) return pet.bark(); // Else branch is Cat — bark is gone. return pet.bark(); }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "'id' in value narrows a union by property presence — useful when you do not own the shapes and cannot add a tag.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Third-party union types rarely come with a discriminant, so branching on them needs a different proof. Runtime key check without types. `in` narrows unions when members have distinct keys.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "incremental-builds",
      "title": "Incremental builds",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/incremental-builds",
      "icon": "ph-wrench",
      "keywords": [
        "incremental",
        "tsbuildinfo",
        "composite",
        "cache",
        "build",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "incremental, tsbuildinfo, composite, and why the second build is fast — plus the cache invalidations that make it slow again.. A stale tsbuildinfo makes `tsc` report success on code it did not check, which is worse than being slow. No composite/incremental cache. Incremental and tsBuildInfoFile speed rebuilds.. incremental and tsBuildInfoFile speed rebuilds.. composite + project references scale monorepos.. CI can still warm caches carefully.. No composite/incremental cache.. incremental mode reports cacheHits as number.. // tsc runs cold every CI job. type BuildMode = \"full\" | \"incremental\"; declare function compile(mode: BuildMode): { cacheHits: number }; const r = compile(\"incremental\"); const hits: number = r.cacheHits; const bad: string = r.cacheHits;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "incremental, tsbuildinfo, composite, and why the second build is fast — plus the cache invalidations that make it slow again.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A stale tsbuildinfo makes `tsc` report success on code it did not check, which is worse than being slow. No composite/incremental cache. Incremental and tsBuildInfoFile speed rebuilds.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "index-signatures",
      "title": "Index signatures",
      "category": "Objects, classes & modules",
      "tab": "intermediate",
      "route": "/lessons/structures/index-signatures",
      "icon": "ph-cube",
      "keywords": [
        "index signature",
        "Record",
        "dictionary",
        "open shape",
        "key",
        "structures",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "[key: string]: T describes an open bag. What it buys, what it costs, and why `Record` and a mapped type are usually the better tool.. An index signature says every key exists, so a typo'd lookup type-checks and returns `undefined`. Open string key bag with mixed values. Index signatures describe open-ended key sets.. Index signatures describe open-ended key sets.. They weaken specific known keys — use carefully.. Prefer `Record<K,V>` or maps for many dynamic keys.. Open string key bag with mixed values.. Index signature number forbids string values.. const bag = {}; bag[key] = value;. type Bag = { [key: string]: number }; const bag: Bag = {}; bag[\"score\"] = 1; bag[\"label\"] = \"x\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "[key: string]: T describes an open bag. What it buys, what it costs, and why `Record` and a mapped type are usually the better tool.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "An index signature says every key exists, so a typo'd lookup type-checks and returns `undefined`. Open string key bag with mixed values. Index signatures describe open-ended key sets.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "indexed-access-types",
      "title": "Indexed access types",
      "category": "Type-level programming",
      "tab": "intermediate",
      "route": "/lessons/type-level/indexed-access-types",
      "icon": "ph-function",
      "keywords": [
        "indexed access",
        "lookup type",
        "T[number]",
        "element type",
        "keyof",
        "type-level",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "User['id'], Routes[`keyof` Routes], and array element types via T[number] — reading a type out of another type.. Duplicating a nested field's type gives you two declarations to keep in step and no error when they drift. Nested field access without a named nested type. Until that contract is checkable, “Indexed access types” stays a runtime surprise.. T['key'] extracts a property type.. Chain for nested fields.. Useful when you do not want to export every nested alias.. Nested field access without a named nested type.. Indexed access digs out number. string fails.. What does User['id'] mean as a type?. function ageOf(user) { return user.age; }. type User = { id: string; profile: { age: number } }; type Age = User[\"profile\"][\"age\"]; const a: Age = 30; const bad: Age = \"30\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "User['id'], Routes[`keyof` Routes], and array element types via T[number] — reading a type out of another type.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Duplicating a nested field's type gives you two declarations to keep in step and no error when they drift. Nested field access without a named nested type. Until that contract is checkable, “Indexed access types” stays a runtime surprise.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "infer-keyword",
      "title": "infer",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/infer-keyword",
      "icon": "ph-function",
      "keywords": [
        "infer",
        "conditional type",
        "pattern match",
        "extract",
        "ReturnType",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Capturing a type out of a match inside a conditional type. The mechanism behind `ReturnType`, `Awaited`, and every parser you will write here.. Reading a type argument back out of a generic type has no syntax at all until you reach for `infer`. JavaScript has no way to name “whatever getUser returns”. `infer` only works inside the extends clause of a conditional type — it is pattern-matching, not a free-standing operator.. `infer` only works inside the extends clause of a conditional type — it is pattern-matching, not a free-standing operator.. Multiple `infer` positions can bind several type variables in one match (e.g. parameters and return together).. `ReturnType`, `Parameters`, and `Awaited` in lib.es5 / es2022 are built from `infer`; writing your own is the same mechanism.. JavaScript has no way to name “whatever getUser returns”.. `infer R` binds the return type inside the match; wrong assignments fail.. Implement Head<T> that extracts the first element type of a tuple (or array). For [string, number] it should be string. Non-array inputs should be `never`.. Where is `infer legal`?. // JS: \"return type of fn\" is a comment, not a check. function getUser() { return { id: 1, name: \"Ada\" }; } // Somewhere else: hope the shape still matches. function printName(user) { console.log(user.name.toUpperCase()); } printName(getUser());. type MyReturnType<F> = F extends (...args: never[]) => infer R ? R : never; function getUser() { return { id: 1, name: \"Ada\" }; } type User = MyReturnType<typeof getUser>; // User is { id: number; name: string } const wrong: User = { id: 1 }; // missing name — the inferred shape is enforceable",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Capturing a type out of a match inside a conditional type. The mechanism behind `ReturnType`, `Awaited`, and every parser you will write here.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Reading a type argument back out of a generic type has no syntax at all until you reach for `infer`. JavaScript has no way to name “whatever getUser returns”. `infer` only works inside the extends clause of a conditional type — it is pattern-matching, not a free-standing operator.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "inference-and-widening",
      "title": "Inference and widening",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/inference-and-widening",
      "icon": "ph-compass",
      "keywords": [
        "widening",
        "literal types",
        "const",
        "let",
        "fresh literals",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "How let widens a string literal to string, how const keeps the literal, and when that difference breaks an API that wanted a specific status.. A variable initialized as `\"ready\"` can silently widen to `string`, so typos like `\"redy\"` compile until a comparison fails at runtime. Inference is helpful until widening erases the literal you cared about.. const status = \"ready\" infers the literal type \"ready\"; let status = \"ready\" usually widens to string.. Use a type annotation (let status: Status) or `as const` when the set of values matters.. Widening is why stringly-typed enums feel fine in JS and then fail under TypeScript.. JavaScript has no notion of 'only these strings'.. let widens the literal; Status no longer accepts the binding.. Annotate status as Status so setStatus accepts it (and fix the bad assignment).. What is the usual inferred type of `let status = \"ready\"`?. function setStatus(status) { return status; } let status = \"ready\"; status = \"redy\"; // typo — still a string setStatus(status);. type Status = \"ready\" | \"done\"; function setStatus(s: Status) { return s; } let status = \"ready\"; // inferred as string, not \"ready\" status = \"pending\"; setStatus(status);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "How let widens a string literal to string, how const keeps the literal, and when that difference breaks an API that wanted a specific status.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A variable initialized as `\"ready\"` can silently widen to `string`, so typos like `\"redy\"` compile until a comparison fails at runtime. Inference is helpful until widening erases the literal you cared about.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "inferring-type-arguments",
      "title": "Where type arguments come from",
      "category": "Functions & generics",
      "tab": "intermediate",
      "route": "/lessons/functions/inferring-type-arguments",
      "icon": "ph-function",
      "keywords": [
        "inference",
        "type argument",
        "explicit",
        "candidate",
        "widening",
        "functions",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Inference reads the arguments you passed. When it guesses well, when it guesses too wide, and when to write the type argument out by hand.. The inferred T is string when you needed \"GET\" | \"POST\", and nothing in the error mentions inference at all. Tuple contents forget their types. TypeScript infers type arguments from call-site values.. TypeScript infers type arguments from call-site values.. Multiple parameters can carry different type args.. Hover in the editor to confirm inference before annotating.. Tuple contents forget their types.. Inferred [number, string]. Second element is not number.. function pair(a, b) { return [a, b]; } const p = pair(1, 'x');. function pair<A, B>(a: A, b: B): [A, B] { return [a, b]; } const p = pair(1, \"x\"); const a: number = p[0]; const b: string = p[1]; const bad: number = p[1];",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Inference reads the arguments you passed. When it guesses well, when it guesses too wide, and when to write the type argument out by hand.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The inferred T is string when you needed \"GET\" | \"POST\", and nothing in the error mentions inference at all. Tuple contents forget their types. TypeScript infers type arguments from call-site values.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "installing-types",
      "title": "Getting types for your dependencies",
      "category": "Node.js migration",
      "tab": "beginner",
      "route": "/lessons/node-migration/installing-types",
      "icon": "ph-hard-drives",
      "keywords": [
        "@types",
        "DefinitelyTyped",
        "types field",
        "bundled types",
        "node-migration",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Bundled types versus DefinitelyTyped (@types/*) — how to tell which you need and what to do when neither exists.. `import express from 'express'` fails type-checking until `@types/express` is installed — or until the package ships its own types. Runtime works; the editor has no API surface. That gap trains people to silence `TS2307` instead of fixing the dependency boundary.. Check `package.json` \"types\" / \"exports\".types before reaching for @types.. Install @types/foo as a devDependency matching the major of foo when needed.. If nothing exists, write a minimal `declare` module \"foo\" locally and upstream later.. Runtime works; the editor has no API surface.. `TS2307` until types exist — bundled or @types.. Replace the import with a local typed function.. Where do @types packages usually belong?. const leftPad = require(\"left-pad\"); leftPad(\"x\", 3);. import leftPad from \"left-pad\"; export const padded = leftPad(\"x\", 3);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Bundled types versus DefinitelyTyped (@types/*) — how to tell which you need and what to do when neither exists.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "`import express from 'express'` fails type-checking until `@types/express` is installed — or until the package ships its own types. Runtime works; the editor has no API surface. That gap trains people to silence `TS2307` instead of fixing the dependency boundary.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "instanceof-narrowing",
      "title": "Narrowing with instanceof",
      "category": "Types & narrowing",
      "tab": "intermediate",
      "route": "/lessons/types/instanceof-narrowing",
      "icon": "ph-shapes",
      "keywords": [
        "instanceof",
        "prototype",
        "class",
        "narrowing",
        "realm",
        "types",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`instanceof` narrows to a class, and does it by prototype chain — which is why it fails across realms and after transpiled subclassing.. `instanceof` Error is the standard way to inspect a caught value, and it silently stops working across a worker or vm boundary. Assuming err has .message. `instanceof` narrows class instances across catch/`unknown` boundaries.. `instanceof` narrows class instances across catch/`unknown` boundaries.. Custom error classes carry fields the base Error does not.. Cross-realm `instanceof` can fail — prefer brand checks for libraries.. Assuming err has .message.. message returns string — assigning to number fails.. What is `instanceof` especially useful for in TypeScript catch blocks?. function message(err) { if (err instanceof Error) return err.message; return String(err); }. function message(err: unknown): string { if (err instanceof Error) return err.message; return String(err); } class HttpError extends Error { constructor(readonly status: number, msg: string) { super(msg); } } function statusOf(err: unknown): number { if (err instanceof HttpError) return err.status; return 500; } const n: number = message(new Error(\"x\"));",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`instanceof` narrows to a class, and does it by prototype chain — which is why it fails across realms and after transpiled subclassing.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "`instanceof` Error is the standard way to inspect a caught value, and it silently stops working across a worker or vm boundary. Assuming err has .message. `instanceof` narrows class instances across catch/`unknown` boundaries.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "interface-vs-type-alias",
      "title": "Interface against type alias",
      "category": "Objects, classes & modules",
      "tab": "beginner",
      "route": "/lessons/structures/interface-vs-type-alias",
      "icon": "ph-cube",
      "keywords": [
        "interface vs type",
        "declaration merging",
        "style",
        "structures",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "When to reach for interface versus type — merging, unions, and a practical rule of thumb for application code.. A codebase mixes interface and type for the same kind of shape, so newcomers cannot tell which rules apply. Without a convention, shapes drift between modules. Use type for unions, tuples, and mapped/conditional results.. Use type for unions, tuples, and mapped/conditional results.. Use interface when you want extends and are modeling an object contract.. Avoid declaration merging in app code — it surprises readers; keep ambient merges in .d.ts.. Without a convention, shapes drift between modules.. Behavior for object shapes is nearly identical; unions need type.. Fix the area call to use numbers.. You need string | number. Which do you use?. // Two docs, two styles, one runtime shape — no single source of truth. function area(box) { return box.w * box.h; }. interface BoxI { w: number; h: number; } type BoxT = { w: number; h: number; }; // Both describe the same shape — pick one style per codebase. function area(box: BoxI): number { return box.w * box.h; } area({ w: 2, h: \"3\" });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "When to reach for interface versus type — merging, unions, and a practical rule of thumb for application code.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A codebase mixes interface and type for the same kind of shape, so newcomers cannot tell which rules apply. Without a convention, shapes drift between modules. Use type for unions, tuples, and mapped/conditional results.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "interfaces-intro",
      "title": "Interfaces",
      "category": "Objects, classes & modules",
      "tab": "beginner",
      "route": "/lessons/structures/interfaces-intro",
      "icon": "ph-cube",
      "keywords": [
        "interface",
        "implements",
        "object shape",
        "structures",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "interface names an object shape you can implement and extend — the workhorse type for records in application code.. Two modules invent slightly different field names for the same record; nothing forces them to agree. id vs userId — `undefined` in the database key. Interfaces describe object shapes and can be extended later.. Interfaces describe object shapes and can be extended later.. They are open to declaration merging — useful for ambient libs, surprising in app code.. Use interfaces for object contracts you expect to grow; prefer type for unions and mapped work.. id vs userId — `undefined` in the database key.. The interface is the shared contract.. Pass a User-shaped object.. Does an interface exist at runtime?. function saveUser(user) { return user.userId + \":\" + user.email; } saveUser({ id: \"1\", email: \"a@b.co\" });. interface User { userId: string; email: string; } function saveUser(user: User): string { return user.userId + \":\" + user.email; } saveUser({ id: \"1\", email: \"a@b.co\" });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "interface names an object shape you can implement and extend — the workhorse type for records in application code.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Two modules invent slightly different field names for the same record; nothing forces them to agree. id vs userId — `undefined` in the database key. Interfaces describe object shapes and can be extended later.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "intersection-types",
      "title": "Intersection types",
      "category": "Objects, classes & modules",
      "tab": "intermediate",
      "route": "/lessons/structures/intersection-types",
      "icon": "ph-cube",
      "keywords": [
        "intersection",
        "and",
        "mixin",
        "never",
        "composition",
        "structures",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A & B is everything from both. How it composes mixins and props, and how it produces `never` when the two disagree.. Intersecting two types with a conflicting property gives a type with a `never` property, and the error appears at the use site. Assuming both id and role exist.. A & B has properties of both.. Conflicting properties can collapse to `never`.. Prefer interfaces with extends for object merges you own.. Assuming both id and role exist.. Intersection requires both sides. Missing role fails.. function save(user) { return user.id + user.role; }. type Id = { id: string }; type Role = { role: \"admin\" | \"user\" }; type Staff = Id & Role; function save(user: Staff): string { return user.id + user.role; } save({ id: \"1\" });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A & B is everything from both. How it composes mixins and props, and how it produces `never` when the two disagree.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Intersecting two types with a conflicting property gives a type with a `never` property, and the error appears at the use site. Assuming both id and role exist.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "isolatedmodules-and-verbatimmodulesyntax",
      "title": "isolatedModules and verbatimModuleSyntax",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/isolatedmodules-and-verbatimmodulesyntax",
      "icon": "ph-wrench",
      "keywords": [
        "isolatedModules",
        "verbatimModuleSyntax",
        "import type",
        "transpile",
        "esbuild",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "What single-file transpilers cannot know, why import type exists, and how these flags keep `tsc` and esbuild agreeing.. A re-exported type compiles under `tsc` and produces a runtime import of something that does not exist under esbuild. Type and value imports look the same. `isolatedModules` assumes each file is transpiled alone.. `isolatedModules` assumes each file is transpiled alone.. `verbatimModuleSyntax` forces type/value import honesty.. Prefer import type for types-only bindings.. Type and value imports look the same.. Cannot resolve ./user — illustrates why type-only imports must be marked for isolated transpile.. export { type User } from './user';. type User = { id: string }; // Under verbatimModuleSyntax, type-only re-exports need type modifier. export type { User }; import { User as ValueUser } from \"./user\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "What single-file transpilers cannot know, why import type exists, and how these flags keep `tsc` and esbuild agreeing.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A re-exported type compiles under `tsc` and produces a runtime import of something that does not exist under esbuild. Type and value imports look the same. `isolatedModules` assumes each file is transpiled alone.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "json-parse-returns-any",
      "title": "JSON.parse returns any",
      "category": "The runtime boundary",
      "tab": "beginner",
      "route": "/lessons/runtime-boundary/json-parse-returns-any",
      "icon": "ph-shield-check",
      "keywords": [
        "JSON.parse",
        "any",
        "unknown",
        "parse",
        "runtime-boundary",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`JSON.parse` is typed as `any` in lib.es5 — wrap it, annotate `unknown`, and parse into a checked shape.. const data = `JSON.parse`(text) silently becomes `any`, undoing strictness for the rest of the function. Missing fields crash later; parse never checked them. Treat `JSON.parse` as returning `unknown` even when the lib says `any`.. Treat `JSON.parse` as returning `unknown` even when the lib says `any`.. Write a helper: function parseJson(text: string): `unknown` { return `JSON.parse`(text); }. Then validate with predicates or a schema library before use.. Missing fields crash later; parse never checked them.. No TypeScript error here — that's the point: `any` accepts everything — including missing.. Type the result as `unknown` and narrow before reading n.. Safest immediate type for `JSON.parse` results?. const data = JSON.parse('{\"n\":1}'); data.n.toFixed(2); data.missing.toFixed(2);. // Default lib typing: JSON.parse → any (escape hatch). const data = JSON.parse('{\"n\":1}'); const n: number = data.n; const oops: number = data.missing;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`JSON.parse` is typed as `any` in lib.es5 — wrap it, annotate `unknown`, and parse into a checked shape.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "const data = `JSON.parse`(text) silently becomes `any`, undoing strictness for the rest of the function. Missing fields crash later; parse never checked them. Treat `JSON.parse` as returning `unknown` even when the lib says `any`.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "key-remapping-with-as",
      "title": "Key remapping",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/key-remapping-with-as",
      "icon": "ph-function",
      "keywords": [
        "key remapping",
        "as clause",
        "mapped type",
        "rename",
        "filter",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "as inside a mapped type renames or drops keys as it goes — getters from fields, prefixed events, filtered shapes.. Generating a getter interface from a model interface means renaming every key, and a mapped type could not do that at all until remapping arrived. Manual getter maps fall out of sync when fields change.. Key remapping uses `as NewKey` after `in`; `as never` filters a key out of the result.. Template literal types pair naturally with remapping for getX / setX / onX patterns.. `Capitalize` and other intrinsic string helpers are part of how library APIs mint names.. Manual getter maps fall out of sync when fields change.. as renames keys; mapping to `never` drops them.. Implement Prefixed<T, P extends string> that prefixes every key with P (e.g. Prefixed<{ id: number }, \"user_\"> has user_id).. What happens if a remapped key resolves to `never`?. // JS: rename by hand, drift forever. const user = { id: 1, name: \"Ada\" }; const getters = { getId: () => user.id, getName: () => user.name, };. type Getters<T> = { [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K]; }; type User = { id: number; name: string }; type UserGetters = Getters<User>; // { getId: () => number; getName: () => string } // Filter keys by remapping to never: type OnlyStrings<T> = { [K in keyof T as T[K] extends string ? K : never]: T[K]; }; const g: UserGetters = { getId: () => 1, // getName missing };",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "as inside a mapped type renames or drops keys as it goes — getters from fields, prefixed events, filtered shapes.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Generating a getter interface from a model interface means renaming every key, and a mapped type could not do that at all until remapping arrived. Manual getter maps fall out of sync when fields change.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "keyof-operator",
      "title": "keyof",
      "category": "Type-level programming",
      "tab": "intermediate",
      "route": "/lessons/type-level/keyof-operator",
      "icon": "ph-function",
      "keywords": [
        "keyof",
        "keys",
        "union",
        "operator",
        "type query",
        "type-level",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The union of an object type's keys. The first operator that computes a type from a type, and the one every other lesson here builds on.. A helper that takes a property name accepts `any` string, so a renamed field breaks at runtime instead of at build time. Dynamic key access with no key constraint.. `keyof` T is the union of keys of T.. Pair with T[K] for safe property access.. `keyof` `any` is string | number | symbol — avoid `any`.. Dynamic key access with no key constraint.. `keyof` User forbids `unknown` keys.. function get(obj, key) { return obj[key]; }. type User = { id: string; age: number }; function get<T, K extends keyof T>(obj: T, key: K): T[K] { return obj[key]; } const u: User = { id: \"1\", age: 2 }; const id: string = get(u, \"id\"); const bad = get(u, \"nope\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The union of an object type's keys. The first operator that computes a type from a type, and the one every other lesson here builds on.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A helper that takes a property name accepts `any` string, so a renamed field breaks at runtime instead of at build time. Dynamic key access with no key constraint.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "literal-types",
      "title": "Literal types",
      "category": "Types & narrowing",
      "tab": "beginner",
      "route": "/lessons/types/literal-types",
      "icon": "ph-shapes",
      "keywords": [
        "literal",
        "string literal",
        "magic string",
        "const",
        "types",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "\"GET\" is a type with exactly one value. Unions of literals replace magic strings with something the compiler and the editor both understand.. A typo'd method name — \"POSt\" — is a perfectly good string, so it fails at runtime against a route table rather than at the keystroke. Typos in method verbs are invisible without a closed set.. Literal types are types with a single inhabitant — \"GET\" is not string.. Unions of literals give you exhaustive-friendly APIs without enums.. const assertions and annotated bindings prevent widening back to string.. Typos in method verbs are invisible without a closed set.. Only the literals in Method are assignable.. Call handle with a valid Method.. What is the type \"GET\" | \"POST\"?. function handle(method, path) { if (method === \"GET\") return \"read \" + path; if (method === \"POST\") return \"write \" + path; throw new Error(\"unsupported\"); } handle(\"POSt\", \"/users\");. type Method = \"GET\" | \"POST\"; function handle(method: Method, path: string): string { if (method === \"GET\") return \"read \" + path; return \"write \" + path; } handle(\"POSt\", \"/users\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "\"GET\" is a type with exactly one value. Unions of literals replace magic strings with something the compiler and the editor both understand.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A typo'd method name — \"POSt\" — is a perfectly good string, so it fails at runtime against a route table rather than at the keystroke. Typos in method verbs are invisible without a closed set.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "mapped-type-modifiers",
      "title": "Modifiers on mapped types",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/mapped-type-modifiers",
      "icon": "ph-function",
      "keywords": [
        "mapped type",
        "modifier",
        "Required",
        "Mutable",
        "readonly",
        "optional",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "+? and -? , +`readonly` and -`readonly`. Adding optionality is easy; removing it is the trick behind `Required` and every Mutable helper.. Making every field required again after `Partial` has been applied is not expressible without modifier removal. Re-requiring fields is a pile of `null` checks with no shared type.. Prefix modifiers with + or -; + is the default when you write ? or `readonly` alone.. `Required<T>` is homomorphic mapped type with -?; `Partial` uses +?.. Homomorphic mapped types (K in `keyof` T) preserve property modifiers you do not touch — until you explicitly add or remove them.. Re-requiring fields is a pile of `null` checks with no shared type.. -? strips optionality; -`readonly` strips `readonly`.. Implement MutableProps<T> that removes `readonly` from every property. The assignment of a `readonly` Config to MutableProps should then allow mutation typing-wise (no error on the object literal assignment shown).. What does `-?` mean in `{ [K in keyof T]-?: T[K] }`?. // JS: \"required again\" means runtime checks scattered everywhere. function requireConfig(partial) { if (partial.host == null) throw new Error(\"host required\"); if (partial.port == null) throw new Error(\"port required\"); return partial; }. type Partialish<T> = { [K in keyof T]?: T[K] }; type Requiredish<T> = { [K in keyof T]-?: T[K] }; type Mutable<T> = { -readonly [K in keyof T]: T[K] }; type Config = { readonly host: string; port?: number }; type Ready = Requiredish<Partialish<Config>>; type Editable = Mutable<Config>; const ready: Ready = { host: \"localhost\" }; // port is required again after Requiredish",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "+? and -? , +`readonly` and -`readonly`. Adding optionality is easy; removing it is the trick behind `Required` and every Mutable helper.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Making every field required again after `Partial` has been applied is not expressible without modifier removal. Re-requiring fields is a pile of `null` checks with no shared type.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "mapped-types-intro",
      "title": "Mapped types",
      "category": "Type-level programming",
      "tab": "intermediate",
      "route": "/lessons/type-level/mapped-types-intro",
      "icon": "ph-function",
      "keywords": [
        "mapped type",
        "in keyof",
        "transform",
        "Partial",
        "homomorphic",
        "type-level",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "{ [K in `keyof` T]: … } transforms every property of a type. This is how `Partial`, `Readonly`, and `Record` are actually defined.. Hand-writing the nullable version of a twenty-field interface produces a second twenty-field interface to maintain. Shallow freeze with no type change. Name the shape so the broken call cannot compile quietly.. Mapped types transform each property via [K in `keyof` T].. `Readonly<T>` and `Partial<T>` are mapped types.. Start here before conditional types.. Shallow freeze with no type change.. Mapped `readonly` fields reject assignment.. What is the core pattern of a mapped type?. function freeze(obj) { return obj; }. type ReadonlyMap<T> = { readonly [K in keyof T]: T[K] }; type User = { id: string; name: string }; type Frozen = ReadonlyMap<User>; const u: Frozen = { id: \"1\", name: \"Ada\" }; u.name = \"Bob\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "{ [K in `keyof` T]: … } transforms every property of a type. This is how `Partial`, `Readonly`, and `Record` are actually defined.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Hand-writing the nullable version of a twenty-field interface produces a second twenty-field interface to maintain. Shallow freeze with no type change. Name the shape so the broken call cannot compile quietly.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "migrating-a-large-codebase",
      "title": "Migrating something large",
      "category": "Node.js migration",
      "tab": "advanced",
      "route": "/lessons/node-migration/migrating-a-large-codebase",
      "icon": "ph-hard-drives",
      "keywords": [
        "migration",
        "strangler",
        "ratchet",
        "codemod",
        "strategy",
        "legacy",
        "node-migration",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Strangler boundaries, per-directory strictness, codemods, and a ratchet in CI so the error count can only go down.. A migration that has to finish before it delivers value is a migration that gets cancelled at the halfway point. All-or-nothing rewrites rarely finish. Without a ratchet, the half-typed tree either blocks every PR or silently grows `any`.. Ship value continuously: `strict` islands expand; legacy shrinks.. CI ratchets (error count / `any` count) beat a binary pass/fail on a half-migrated tree.. Codemods + `allowJs` get files into the graph; types follow folder by folder.. All-or-nothing migrations rarely finish.. Ratchets track counts — the budget is a number, not a string.. Type RatchetState = { errors: number } and function better(a, b) that returns the smaller errors count.. What is a type error ratchet?. // JS big-bang rewrite: replace everything, ship nothing for months. function migrateAll() { throw new Error(\"not done\"); }. type DirStrictness = { path: string; strict: boolean }; // Strangler: new code is strict; legacy paths loosen gradually. const policy: DirStrictness[] = [ { path: \"src/new/\", strict: true }, { path: \"src/legacy/\", strict: false }, ]; type Ratchet = { maxErrors: number }; const ratchet: Ratchet = { maxErrors: 400 }; function assertImproving(prev: number, next: number, budget: Ratchet) { if (next >…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Strangler boundaries, per-directory strictness, codemods, and a ratchet in CI so the error count can only go down.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A migration that has to finish before it delivers value is a migration that gets cancelled at the halfway point. All-or-nothing rewrites rarely finish. Without a ratchet, the half-typed tree either blocks every PR or silently grows `any`.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "mixins-and-constructor-types",
      "title": "Mixins",
      "category": "Objects, classes & modules",
      "tab": "advanced",
      "route": "/lessons/structures/mixins-and-constructor-types",
      "icon": "ph-cube",
      "keywords": [
        "mixin",
        "constructor type",
        "abstract construct signature",
        "composition",
        "structures",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Functions that take a class and return a subclass, typed with constructor signatures — composition where single inheritance runs out.. Two orthogonal behaviours both want to be a base class, and JavaScript gives you one prototype chain. JS mixins work; typing the constructor parameter is the hard part.. Mixin constructors need `new (...args: any[]) => T` — `never`[] trips `TS2545`.. The returned class expression intersects the base instance type with new fields.. Prefer composition (has-a) when mixin lattices get hard to follow.. JS mixins work; typing the constructor parameter is the hard part.. Mixin constructors use `any`[] rest; instance fields compose.. Write Tagged<TBase extends Constructor>(Base: TBase) adding tag = \"x\", and construct one from class A {}.. What does `TBase extends Constructor` constrain?. // JS: mixins are functions that extend a class. function Timestamped(Base) { return class extends Base { createdAt = Date.now(); }; } class Entity {} const TEntity = Timestamped(Entity);. type Constructor<T = {}> = new (...args: any[]) => T; function Timestamped<TBase extends Constructor>(Base: TBase) { return class extends Base { createdAt = Date.now(); }; } class Entity { id = 1; } const TimedEntity = Timestamped(Entity); const e = new TimedEntity(); const created: number = e.createdAt; const wrong: string = e.createdAt; void created;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Functions that take a class and return a subclass, typed with constructor signatures — composition where single inheritance runs out.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Two orthogonal behaviours both want to be a base class, and JavaScript gives you one prototype chain. JS mixins work; typing the constructor parameter is the hard part.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "mocking-modules",
      "title": "Typed module mocks",
      "category": "Testing with types",
      "tab": "intermediate",
      "route": "/lessons/testing/mocking-modules",
      "icon": "ph-test-tube",
      "keywords": [
        "vi.mock",
        "jest.mock",
        "MockedFunction",
        "module mock",
        "typeof import",
        "testing",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "vi.mock and jest.mock erase types unless you help them. MockedFunction, mocked(), and keeping the mock's signature tied to the real one.. A module mock whose signature has drifted from the real module passes every test and fails in production. Mocked module with untyped factory. Type the mocked module surface explicitly.. Type the mocked module surface explicitly.. Factory return values must satisfy that surface.. Prefer dependency injection when mocks get heavy.. Mocked module with untyped factory.. Generic mockModule preserves connect's `Promise<void>`.. jest.mock('./db');. // Module mocks still need the public type of the module. type DbModule = { connect(): Promise<void> }; declare function mockModule<T>(factory: () => T): T; const db = mockModule<DbModule>(() => ({ connect: async () => undefined, })); const p: Promise<void> = db.connect(); const bad: number = db.connect();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "vi.mock and jest.mock erase types unless you help them. MockedFunction, mocked(), and keeping the mock's signature tied to the real one.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A module mock whose signature has drifted from the real module passes every test and fails in production. Mocked module with untyped factory. Type the mocked module surface explicitly.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "module-augmentation",
      "title": "Augmenting another package's types",
      "category": "Objects, classes & modules",
      "tab": "advanced",
      "route": "/lessons/structures/module-augmentation",
      "icon": "ph-cube",
      "keywords": [
        "module augmentation",
        "declare module",
        "declare global",
        "d.ts",
        "express",
        "structures",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`declare` module to add to a dependency's types, `declare` global for the runtime globals — done in a way that survives an upgrade.. Attaching a user object to Express's Request is a five-line change that half of all Node codebases get subtly wrong. JS monkey-patches freely; TypeScript needs an augmentation.. In real packages, augmentation uses `declare module \"pkg\"` in a .d.ts that is itself a module.. Match the dependency’s module name exactly; path mapping typos silently create a new ambient module.. Prefer documenting required app-owned fields as required only when middleware always sets them.. JS monkey-patches freely; TypeScript needs an augmentation.. Merged optional user still needs a narrowing before .id.. Merge interface Box { value: number } with label?: string, then read value.. Where should Express Request augmentation usually live?. // JS: just hang properties on req — no types to update. function auth(req, _res, next) { req.user = { id: \"u1\" }; next(); }. // Interface merging models the same idea as declare module augmentation. interface Request { url: string; } interface Request { user?: { id: string }; } function handle(req: Request) { const id: string = req.user.id; void id; } const req: Request = { url: \"/\" }; handle(req);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`declare` module to add to a dependency's types, `declare` global for the runtime globals — done in a way that survives an upgrade.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Attaching a user object to Express's Request is a five-line change that half of all Node codebases get subtly wrong. JS monkey-patches freely; TypeScript needs an augmentation.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "module-resolution-explained",
      "title": "How an import is resolved",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/module-resolution-explained",
      "icon": "ph-wrench",
      "keywords": [
        "moduleResolution",
        "nodenext",
        "bundler",
        "paths",
        "baseUrl",
        "resolution",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "node16, nodenext, and bundler; baseUrl and paths; why the resolution mode changes what an import even means.. Cannot find module for a package that is definitely installed is nearly always a resolution-mode mismatch, not a missing dependency. Extensionless requires everywhere.. `moduleResolution` bundler vs nodenext change legal specifiers.. Match resolution to your runtime (Node vs bundler).. Do not mix modes across packages carelessly.. Extensionless requires everywhere.. Illustrative API: nodenext wants ./util.js — extensionless fails the SpecFor check.. require('./util');. // Illustrative: nodenext wants a .js extension on relative specs. type Mode = \"bundler\" | \"nodenext\"; type SpecFor<M extends Mode> = M extends \"nodenext\" ? `${string}.js` : string; declare function resolve<M extends Mode>(mode: M, spec: SpecFor<M>): string; const a = resolve(\"bundler\", \"./util\"); const b = resolve(\"nodenext\", \"./util.js\"); const bad = resolve(\"nodenext\", \"./util\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "node16, nodenext, and bundler; baseUrl and paths; why the resolution mode changes what an import even means.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Cannot find module for a package that is definitely installed is nearly always a resolution-mode mismatch, not a missing dependency. Extensionless requires everywhere.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "namespaces-and-legacy-code",
      "title": "Namespaces, and the code that still uses them",
      "category": "Objects, classes & modules",
      "tab": "intermediate",
      "route": "/lessons/structures/namespaces-and-legacy-code",
      "icon": "ph-cube",
      "keywords": [
        "namespace",
        "module",
        "legacy",
        "declare",
        "DefinitelyTyped",
        "structures",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`declare` namespace is everywhere in older type definitions. How to read it, how to consume it, and why not to write new ones.. Half of DefinitelyTyped predates ES modules, so consuming it means understanding a module system you would never choose. Iife/global namespaces in old scripts. Namespaces are legacy — use ES modules for new code.. Namespaces are legacy — use ES modules for new code.. They still appear in older DefinitelyTyped patterns.. Migrate outward-in: leave namespace wrappers until the end.. Iife/global namespaces in old scripts.. Prefer ES modules; namespaces still type. greet returns string.. var App = App || {}; App.util = {};. namespace Legacy { export function greet(name: string): string { return \"hi \" + name; } } const s: string = Legacy.greet(\"Ada\"); const bad: number = Legacy.greet(\"Ada\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`declare` namespace is everywhere in older type definitions. How to read it, how to consume it, and why not to write new ones.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Half of DefinitelyTyped predates ES modules, so consuming it means understanding a module system you would never choose. Iife/global namespaces in old scripts. Namespaces are legacy — use ES modules for new code.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "narrowing-that-does-not-survive",
      "title": "Where narrowing is thrown away",
      "category": "Types & narrowing",
      "tab": "advanced",
      "route": "/lessons/types/narrowing-that-does-not-survive",
      "icon": "ph-shapes",
      "keywords": [
        "narrowing",
        "closure",
        "mutation",
        "callback",
        "invalidation",
        "types",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Callbacks, closures, mutable properties, and `any` function call can reset a narrowed type. Which ones do, and what to do instead.. The value is definitely not `null` on line 4 and possibly `null` again on line 6, and the only thing between them is a callback. Closures capture values; JS `never` re-checks assumptions.. Store the narrowed value in a const local (`const u = user`) before async boundaries.. Mutable properties are invalidated aggressively — narrow the field into a local.. Assertion functions and type guards re-establish facts the checker will not assume across calls.. Closures capture values; JS `never` re-checks assumptions.. Capture a local const after the `null` check to keep a stable narrow across the callback.. After the `null` check, assign const u = user, then use u.name inside later(...). Match the solution shape.. Best way to keep a non-`null` user across setTimeout?. // JS: nothing tracks nullability across callbacks. function load(user, cb) { if (user != null) { setTimeout(() => cb(user.name), 0); } }. type User = { name: string }; declare function later(cb: () => void): void; function schedule(cb: (name: string) => void) { let user: User | null = { name: \"Ada\" }; if (user !== null) { later(() => { // CFA invalidates the narrow: user can be reassigned before this runs. cb(user.name); }); } user = null; }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Callbacks, closures, mutable properties, and `any` function call can reset a narrowed type. Which ones do, and what to do instead.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The value is definitely not `null` on line 4 and possibly `null` again on line 6, and the only thing between them is a callback. Closures capture values; JS `never` re-checks assumptions.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "narrowing-untrusted-objects",
      "title": "Inspecting an object you did not create",
      "category": "The runtime boundary",
      "tab": "intermediate",
      "route": "/lessons/runtime-boundary/narrowing-untrusted-objects",
      "icon": "ph-shield-check",
      "keywords": [
        "prototype pollution",
        "hasOwn",
        "in operator",
        "__proto__",
        "untrusted",
        "runtime-boundary",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "in, `Object.hasOwn`, and prototype-chain surprises. Why __proto__ and constructor deserve special handling before you touch anything else.. 'toString' in payload is true for every object ever created, which makes the obvious presence check useless as a validator. `in` walks the prototype chain — inherited keys look like own data.. `key in obj` is true for inherited properties — useless as an allowlist for JSON payloads.. `Object.hasOwn(obj, key)` (or `Object.prototype`.hasOwnProperty.call) checks own keys only.. Reject `__proto__`, `constructor`, and `prototype` keys before merging untrusted objects into config.. `in` walks the prototype chain — inherited keys look like own data.. Prefer `Object.hasOwn` for untrusted keys. The deliberate assign shows a bad `in`-based guard still leaves a string, not a number.. Why is `\"toString\" in payload` a bad validator check?. function looksLikeConfig(payload) { // True for {}, and for anything that inherits Object.prototype. if (\"toString\" in payload && \"name\" in payload) { return payload.name; } return null; } looksLikeConfig({ name: \"ok\" }); looksLikeConfig(JSON.parse('{\"__proto__\":{\"admin\":true},\"name\":\"x\"}'));. type Config = { name: string }; function readName(payload: object): string | null { // Object.hasOwn ignores the prototype chain. if (Object.hasOwn(payload, \"name\")) { const name = (payload as { name: unknown }).name; return typeof name === \"string\" ? name : null; } return null; } declare const raw: object; const name = readName(raw); // `in` still sees inherited keys — do not treat it as ownersh…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "in, `Object.hasOwn`, and prototype-chain surprises. Why __proto__ and constructor deserve special handling before you touch anything else.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "'toString' in payload is true for every object ever created, which makes the obvious presence check useless as a validator. `in` walks the prototype chain — inherited keys look like own data.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "narrowing-with-typeof",
      "title": "Narrowing with typeof",
      "category": "Types & narrowing",
      "tab": "beginner",
      "route": "/lessons/types/narrowing-with-typeof",
      "icon": "ph-shapes",
      "keywords": [
        "typeof",
        "narrowing",
        "type guard",
        "null",
        "types",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A `typeof` check does not just branch at runtime; it changes the type inside the branch. Plus the one lie `typeof` still tells about `null`.. You checked the type on line 3, but on line 7 the value is still a union as far as anything reading the code can tell. Number(true) is 1 — `typeof` would have rejected the boolean path.. `typeof` value === \"string\" narrows value to string inside that block.. `typeof` `null` === \"object\" — use === `null` for `null`, not `typeof`.. After an early return in the number branch, the rest of the function sees the remaining union members.. Number(true) is 1 — `typeof` would have rejected the boolean path.. Multiply is not valid on string | number until you narrow.. Narrow with `typeof` before multiplying.. What does `typeof` `null` return in JavaScript?. function asNumber(value) { if (typeof value === \"number\") return value; return Number(value); } asNumber(true).toFixed(2);. function double(value: string | number): number { // Outside the typeof branch, value is still the full union. return value * 2; }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A `typeof` check does not just branch at runtime; it changes the type inside the branch. Plus the one lie `typeof` still tells about `null`.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "You checked the type on line 3, but on line 7 the value is still a union as far as anything reading the code can tell. Number(true) is 1 — `typeof` would have rejected the boolean path.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "nested-and-composed-object-types",
      "title": "Composing object types",
      "category": "Objects, classes & modules",
      "tab": "beginner",
      "route": "/lessons/structures/nested-and-composed-object-types",
      "icon": "ph-cube",
      "keywords": [
        "composition",
        "intersection",
        "nested",
        "extends",
        "structures",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Build larger shapes from smaller ones with nesting, intersection (&), and extends — without copy-pasting fields.. Address fields are duplicated on User, Order, and Invoice until one of them renames zip to postalCode alone. Duplicated shapes drift under different names. Nest object types for ownership (Order.shipTo: Address).. Nest object types for ownership (Order.shipTo: Address).. A & B intersects properties — useful for mixing capabilities.. interface Child extends Parent { ... } is the interface form of composition.. Duplicated shapes drift under different names.. Compose Address once; nest it where needed.. Use zip in the nested address.. Why compose Address instead of inlining fields?. function shipTo(order) { return order.street + \", \" + order.zip; } shipTo({ street: \"1 Main\", postalCode: \"94105\" });. type Address = { street: string; zip: string }; type Order = { id: string; shipTo: Address }; function shipTo(order: Order): string { return order.shipTo.street + \", \" + order.shipTo.zip; } shipTo({ id: \"o1\", shipTo: { street: \"1 Main\", postalCode: \"94105\" } });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Build larger shapes from smaller ones with nesting, intersection (&), and extends — without copy-pasting fields.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Address fields are duplicated on User, Order, and Invoice until one of them renames zip to postalCode alone. Duplicated shapes drift under different names. Nest object types for ownership (Order.shipTo: Address).",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "never-returning-functions",
      "title": "Functions that never return",
      "category": "Async, errors & Result",
      "tab": "intermediate",
      "route": "/lessons/async/never-returning-functions",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "never",
        "process.exit",
        "assertNever",
        "control flow",
        "throw helper",
        "async",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`process.exit`, a function that always throws, and assertNever — how `never` lets control-flow analysis see past a call.. Extracting throw new Error(...) into a helper makes the checker believe execution continues, and the code after it becomes reachable. exit looks like a normal call.. `never` helps control-flow analysis after abort/throw.. Mark `process.exit` wrappers as `never`.. Do not use `never` for functions that sometimes return.. exit looks like a normal call.. `never` makes abort() an exit for CFA; a `void` throw helper leaves x possibly `null`.. function abort() { process.exit(1); }. declare function exit(code: number): never; function abort(): never { exit(1); } function withNever(x: string | null): string { if (x === null) abort(); return x; } function fail(): void { throw new Error(\"boom\"); } function withVoid(x: string | null): string { if (x === null) fail(); return x; }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`process.exit`, a function that always throws, and assertNever — how `never` lets control-flow analysis see past a call.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Extracting throw new Error(...) into a helper makes the checker believe execution continues, and the code after it becomes reachable. exit looks like a normal call.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "node-builtin-modules",
      "title": "node: builtins and @types/node",
      "category": "Node.js migration",
      "tab": "beginner",
      "route": "/lessons/node-migration/node-builtin-modules",
      "icon": "ph-hard-drives",
      "keywords": [
        "node:",
        "@types/node",
        "fs",
        "builtins",
        "node-migration",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Prefer node:fs imports and install @types/node so builtins have accurate typings for your Node version.. Code imports `'fs'` without types; path methods accept anything and fail when a `Buffer` was expected. Wrong path types become runtime `TypeError`s long after the call site was written. Builtin modules deserve the same care as your own packages.. node:fs makes builtin imports explicit and avoids npm package name clashes.. @types/node versions should track your engines.node range.. Until types are installed, stub only what you need — don't invent a fake full fs.. Wrong path type is a runtime TypeError.. Install @types/node (and use node: specifiers) in real projects.. Provide a local typed readFileSync stub.. Why prefer import from \"node:fs\"?. const fs = require(\"fs\"); fs.readFileSync(42, \"utf8\");. // This sandbox has no @types/node — the import cannot resolve. import { readFileSync } from \"node:fs\"; export const text = readFileSync(42, \"utf8\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Prefer node:fs imports and install @types/node so builtins have accurate typings for your Node version.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Code imports `'fs'` without types; path methods accept anything and fail when a `Buffer` was expected. Wrong path types become runtime `TypeError`s long after the call site was written. Builtin modules deserve the same care as your own packages.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "noimplicitany",
      "title": "noImplicitAny",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/noimplicitany",
      "icon": "ph-wrench",
      "keywords": [
        "noImplicitAny",
        "implicit any",
        "parameters",
        "strict",
        "flag",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The first rung: make the checker complain when it had to guess. Usually the biggest single error count and the biggest single payoff.. Without it, every unannotated parameter is `any`, and a file can be fully typed on paper and unchecked in practice. `Parameters` silently become `any`. Untyped parameters become implicit `any` without the flag.. Untyped parameters become implicit `any` without the flag.. Annotate or use contextual typing from callers.. Turn this on early in a migration.. `Parameters` silently become `any`.. `noImplicitAny` (via `strict`) errors on untyped parameters.. Annotate a and b as number so Check matches the solution text.. function add(a, b) { return a + b; }. function add(a, b) { return a + b; }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The first rung: make the checker complain when it had to guess. Usually the biggest single error count and the biggest single payoff.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Without it, every unannotated parameter is `any`, and a file can be fully typed on paper and unchecked in practice. `Parameters` silently become `any`. Untyped parameters become implicit `any` without the flag.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "noimplicitoverride-and-classfields",
      "title": "noImplicitOverride and useDefineForClassFields",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/noimplicitoverride-and-classfields",
      "icon": "ph-wrench",
      "keywords": [
        "noImplicitOverride",
        "useDefineForClassFields",
        "override",
        "class fields",
        "emit",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Two class-shaped flags: one makes overriding explicit, the other changes what a field declaration actually emits.. Renaming a base-class method leaves the subclass with a method that overrides nothing and is `never` called. Overrides with no marker. NoImplicitOverride requires the override keyword.. noImplicitOverride requires the override keyword.. It catches renames on the base class.. Use with useDefineForClassFields awareness.. Overrides with no marker.. override documents intent. speak returns string.. class Dog extends Animal { speak() { return 'woof'; } }. class Animal { speak(): string { return \"...\"; } } class Dog extends Animal { speak(): string { return \"woof\"; } } // Illustrated requirement: mark overrides explicitly in real configs class Cat extends Animal { override speak(): string { return \"meow\"; } } const s: string = new Dog().speak(); const bad: number = new Dog().speak();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Two class-shaped flags: one makes overriding explicit, the other changes what a field declaration actually emits.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Renaming a base-class method leaves the subclass with a method that overrides nothing and is `never` called. Overrides with no marker. NoImplicitOverride requires the override keyword.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "noinfer-and-inference-control",
      "title": "Steering inference with NoInfer",
      "category": "Functions & generics",
      "tab": "advanced",
      "route": "/lessons/functions/noinfer-and-inference-control",
      "icon": "ph-function",
      "keywords": [
        "NoInfer",
        "inference",
        "candidate",
        "default",
        "generic",
        "functions",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`NoInfer<T>` tells the checker not to take a candidate from a position — the fix for a default argument that widens the type you were narrowing.. One extra argument silently widens T, so an API that validated its input yesterday accepts anything today. Defaults participate in the same untyped soup as other arguments.. Inference sites that should only *check* against T, not *define* T, should be wrapped in `NoInfer<T>`.. Classic cases: default values, secondary arguments, and context that must follow a primary source of truth.. Before `NoInfer`, libraries used crazy double-generic tricks; prefer the built-in now.. Defaults participate in the same untyped soup as other arguments.. `NoInfer` blocks candidates from fallback; mismatched literals error.. Write assertEqual<T>(actual: T, expected: `NoInfer<T>`): `void` that only compares, and call it with matching string literals.. What does `NoInfer<T>` do at an inference site?. // JS: defaults just fill in — they also \"teach\" the type if you imagine one. function createRoute(path, fallback = \"/\") { return { path: path ?? fallback }; } createRoute(\"/home\", 0); // weird fallback, no complaint. // Without NoInfer, the fallback argument also infers T and widens it. function pick<T extends string>( value: T, fallback: NoInfer<T>, ): T { return value ?? fallback; } const a = pick(\"left\", \"left\"); const b = pick(\"left\", \"right\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`NoInfer<T>` tells the checker not to take a candidate from a position — the fix for a default argument that widens the type you were narrowing.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "One extra argument silently widens T, so an API that validated its input yesterday accepts anything today. Defaults participate in the same untyped soup as other arguments.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "non-null-assertion",
      "title": "The non-null assertion",
      "category": "The runtime boundary",
      "tab": "beginner",
      "route": "/lessons/runtime-boundary/non-null-assertion",
      "icon": "ph-shield-check",
      "keywords": [
        "non-null assertion",
        "!",
        "definite assignment",
        "runtime-boundary",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "value! tells TypeScript a value is not `null` or `undefined` — another claim that can lie at runtime.. map.get(key)! crashes when the key is missing — the bang only silenced the checker. Map.get returns `undefined` when absent. X! is shorthand for 'I promise x is not nullish' — same family as as.. x! is shorthand for 'I promise x is not nullish' — same family as as.. Use if (!x) throw new Error(...) when absence is a bug you want to see.. Ban ! in lint for app code if your team keeps foot-gunning with it.. Map.get returns `undefined` when absent.. No TypeScript error here — that's the point: ! removes | `undefined` from the type, not from reality. Prefer an explicit check.. Handle a missing map entry without !.. What does expr! change at runtime?. const users = new Map(); users.set(\"1\", { name: \"Ada\" }); users.get(\"missing\").name;. type User = { name: string }; const users = new Map<string, User>(); users.set(\"1\", { name: \"Ada\" }); // ! removes | undefined from the type — not from reality. users.get(\"missing\")!.name;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "value! tells TypeScript a value is not `null` or `undefined` — another claim that can lie at runtime.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "map.get(key)! crashes when the key is missing — the bang only silenced the checker. Map.get returns `undefined` when absent. X! is shorthand for 'I promise x is not nullish' — same family as as.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "nouncheckedindexedaccess",
      "title": "noUncheckedIndexedAccess",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/nouncheckedindexedaccess",
      "icon": "ph-wrench",
      "keywords": [
        "noUncheckedIndexedAccess",
        "array",
        "index",
        "undefined",
        "record",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Indexing an array or a record yields T | `undefined`, which is the truth. Not part of `strict`, and the highest-value flag outside it.. map[key] is typed as present for every key, so a cache miss is typed identically to a cache hit. Indexing assumed defined. `noUncheckedIndexedAccess` adds | `undefined` to index reads.. `noUncheckedIndexedAccess` adds | `undefined` to index reads.. Even without the flag, treat indexes as optional in Node services.. Narrow before use.. Indexing assumed defined.. Model index access as T | `undefined`. Assignment to string fails.. What does `noUncheckedIndexedAccess` change about `arr[i]`?. const first = arr[0]; first.toUpperCase();. // Host baseline does not enable noUncheckedIndexedAccess. // Model the safer type explicitly: function first(arr: string[]): string | undefined { return arr[0]; } const s: string = first([\"a\"]);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Indexing an array or a record yields T | `undefined`, which is the truth. Not part of `strict`, and the highest-value flag outside it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "map[key] is typed as present for every key, so a cache miss is typed identically to a cache hit. Indexing assumed defined. `noUncheckedIndexedAccess` adds | `undefined` to index reads.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "null-and-undefined",
      "title": "null and undefined",
      "category": "Types & narrowing",
      "tab": "beginner",
      "route": "/lessons/types/null-and-undefined",
      "icon": "ph-shapes",
      "keywords": [
        "null",
        "undefined",
        "strictNullChecks",
        "optional",
        "types",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The two absences JavaScript ships with, what `strictNullChecks` changes about them, and why 'cannot read properties of `undefined`' stops being a surprise.. Without `strictNullChecks` every type silently includes `null` and `undefined`, so the most common runtime crash in Node is invisible to the checker. `undefined`.trim is a classic production TypeError.. `null` is often an intentional empty; `undefined` often means 'not provided' — pick a convention and stick to it.. `strictNullChecks` makes both absences visible in the type system.. Prefer T | `undefined` for optional params and T | `null` when a search can miss.. `undefined`.trim is a classic production TypeError.. `undefined` is not assignable to string under `strictNullChecks`.. Accept `undefined` and return a default header.. With `strictNullChecks`, is string assignable from `undefined`?. function header(name) { return name.trim().toUpperCase(); } header(undefined);. function header(name: string): string { return name.trim().toUpperCase(); } header(undefined);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The two absences JavaScript ships with, what `strictNullChecks` changes about them, and why 'cannot read properties of `undefined`' stops being a surprise.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Without `strictNullChecks` every type silently includes `null` and `undefined`, so the most common runtime crash in Node is invisible to the checker. `undefined`.trim is a classic production TypeError.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "object-type-literals",
      "title": "Object types",
      "category": "Types & narrowing",
      "tab": "beginner",
      "route": "/lessons/types/object-type-literals",
      "icon": "ph-shapes",
      "keywords": [
        "object type",
        "properties",
        "optional",
        "nested",
        "types",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Describing a shape inline: required and optional properties, nested objects, and how the checker compares two of them.. Every Node handler passes objects around, and without a shape written down the only documentation is the last person who read the code. Snake_case vs camelCase — `undefined` expiresAt math.. Write the shape once at the boundary; let inference carry it inward.. Optional properties use ?: — absent is not the same as present-but-`undefined` unless you configure `exactOptionalPropertyTypes`.. Nested objects are just properties whose types are other object types.. Snake_case vs camelCase — `undefined` expiresAt math.. The object type names the fields callers must provide.. Fix the call to use user_id.. What is SessionInput in this lesson?. function createSession(input) { return { userId: input.user_id, expiresAt: Date.now() + input.ttlMs, }; } createSession({ userId: \"u1\", ttlMs: 60_000 });. type SessionInput = { user_id: string; ttlMs: number; }; function createSession(input: SessionInput) { return { userId: input.user_id, expiresAt: Date.now() + input.ttlMs, }; } createSession({ userId: \"u1\", ttlMs: 60_000 });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Describing a shape inline: required and optional properties, nested objects, and how the checker compares two of them.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Every Node handler passes objects around, and without a shape written down the only documentation is the last person who read the code. Snake_case vs camelCase — `undefined` expiresAt math.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "optional-and-default-parameters",
      "title": "Optional and default parameters",
      "category": "Functions & generics",
      "tab": "beginner",
      "route": "/lessons/functions/optional-and-default-parameters",
      "icon": "ph-function",
      "keywords": [
        "optional parameter",
        "default parameter",
        "?",
        "undefined",
        "functions",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "name?: string versus name = \"guest\" — how each shows up in the type, and why optional parameters must come last.. Callers skip an argument; callees read it as a real value and crash on .toUpperCase(). Missing args become `undefined` with no warning. Optional parameters are T | `undefined` and must follow required ones.. Optional parameters are T | `undefined` and must follow required ones.. Defaults (name = \"guest\") make the parameter optional for callers and defined inside the body.. Prefer defaults when you have a sensible fallback; prefer ? when absence is meaningful.. Missing args become `undefined` with no warning.. name is required; omitting it is a type error.. Give name a default so greet() is valid.. Inside greet(name = \"guest\"), what is `typeof` name?. function greet(name, excited) { const base = \"hello \" + name.toUpperCase(); return excited ? base + \"!\" : base; } greet();. function greet(name: string, excited?: boolean): string { const base = \"hello \" + name.toUpperCase(); return excited ? base + \"!\" : base; } greet();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "name?: string versus name = \"guest\" — how each shows up in the type, and why optional parameters must come last.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Callers skip an argument; callees read it as a real value and crash on .toUpperCase(). Missing args become `undefined` with no warning. Optional parameters are T | `undefined` and must follow required ones.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "optional-and-readonly-properties",
      "title": "Optional and readonly properties",
      "category": "Objects, classes & modules",
      "tab": "beginner",
      "route": "/lessons/structures/optional-and-readonly-properties",
      "icon": "ph-cube",
      "keywords": [
        "optional",
        "readonly",
        "?",
        "mutation",
        "structures",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "?: for fields that may be absent, `readonly` for fields that must not be reassigned — and how each changes assignability.. A config object gets mutated mid-request; the next handler sees a different port than the one that was validated. Mutation changes meaning for every other reference to cfg.. optional (host?) means the property may be missing; read it as T | `undefined`.. `readonly` is a type-level constraint — runtime code can still mutate if it cheats.. Combine them: `readonly` id: string for identity fields that never change.. Mutation changes meaning for every other reference to cfg.. `readonly` blocks assignment through that property.. Return port without reassigning it.. What does `readonly` prevent?. function bind(config) { config.port = Number(config.port); return config.port; } const cfg = { port: \"3000\" }; bind(cfg);. interface Config { readonly port: number; host?: string; } function bind(config: Config): number { config.port = 4000; return config.port; }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "?: for fields that may be absent, `readonly` for fields that must not be reassigned — and how each changes assignability.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A config object gets mutated mid-request; the next handler sees a different port than the one that was validated. Mutation changes meaning for every other reference to cfg.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "optional-chaining-and-nullish",
      "title": "Optional chaining and nullish coalescing",
      "category": "Types & narrowing",
      "tab": "beginner",
      "route": "/lessons/types/optional-chaining-and-nullish",
      "icon": "ph-shapes",
      "keywords": [
        "optional chaining",
        "nullish coalescing",
        "?.",
        "??",
        "types",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "?. and ?? are runtime operators the type system understands: how each one changes the type of the expression around it.. || treats 0 and the empty string as missing, so a default quietly overrides a legitimate value. Missing profile throws; empty email is replaced by ||. ?. short-circuits to `undefined` when the receiver is nullish.. ?. short-circuits to `undefined` when the receiver is nullish.. ?? defaults only for `null` and `undefined` — keep 0 and \"\".. Combine them: user.profile?.email ?? \"nobody@example.com\".. Missing profile throws; empty email is replaced by ||.. Optional properties are `undefined` until you chain with ?.. Use optional chaining so email type-checks.. What does 0 ?? 3000 evaluate to?. function email(user) { return user.profile.email || \"nobody@example.com\"; } email({ profile: { email: \"\" } });. type User = { profile?: { email?: string } }; function email(user: User): string { // profile and email may be missing — need ?. before reading. return user.profile.email ?? \"nobody@example.com\"; }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "?. and ?? are runtime operators the type system understands: how each one changes the type of the expression around it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "|| treats 0 and the empty string as missing, so a default quietly overrides a legitimate value. Missing profile throws; empty email is replaced by ||. ?. short-circuits to `undefined` when the receiver is nullish.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "overload-resolution-order",
      "title": "How an overload is chosen",
      "category": "Functions & generics",
      "tab": "advanced",
      "route": "/lessons/functions/overload-resolution-order",
      "icon": "ph-function",
      "keywords": [
        "overload",
        "resolution",
        "order",
        "arity",
        "declaration",
        "functions",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The checker tries signatures in declaration order and takes the first that fits. Why that makes ordering load-bearing and generic overloads risky.. Adding a convenience overload at the top of the list quietly captures calls that were meant for the one below it. JS overloads are comments; `any` call shape is allowed.. Overloads are tried top-to-bottom; put more specific signatures first.. The implementation signature is not visible to callers — only the overload list is.. Generic overloads are easy to get wrong; prefer unions or separate functions when possible.. JS overloads are comments; `any` call shape is allowed.. Declaration order picks the fd overload; assigning to string fails.. Declare overloads for len(s: string): number and len(arr: `unknown`[]): number with a shared implementation.. If two overloads both match a call, which is chosen?. // JS: one function, many call shapes — documentation only. function read(source, encoding) { if (typeof source === \"number\") return Buffer.alloc(0); return String(source); } read(10); read(\"file.txt\", \"utf8\");. function read(fd: number): Uint8Array; function read(path: string, encoding: string): string; function read( source: number | string, encoding?: string, ): Uint8Array | string { if (typeof source === \"number\") return new Uint8Array(); return String(source); } const a = read(3); const b = read(\"notes.txt\", \"utf8\"); // First matching overload wins — number fits the first signature. const c: string …",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The checker tries signatures in declaration order and takes the first that fits. Why that makes ordering load-bearing and generic overloads risky.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Adding a convenience overload at the top of the list quietly captures calls that were meant for the one below it. JS overloads are comments; `any` call shape is allowed.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "package-json-exports-and-types",
      "title": "exports, types, and how consumers find them",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/package-json-exports-and-types",
      "icon": "ph-hard-drives",
      "keywords": [
        "exports",
        "types condition",
        "subpath",
        "package.json",
        "resolution",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The exports map, the types condition, subpath exports, and why the order of conditions in that object is load-bearing.. A package with an `exports` map and a stale top-level `types` field can resolve fine for the author and to `any` for everyone else. Consumers trust the published entrypoints; if types and runtime diverge, their editor lies while your CI stays green.. Put a types condition in exports so TypeScript resolves declarations reliably.. Dual packages need both import and require entry points.. Prefer exports.types over legacy typesVersions when you can.. exports with a runtime path only.. Author a types condition. Optional types is not a number.. const exportsMap = { \".\": \"./dist/index.js\" };. type ExportEntry = { types?: string; import?: string; require?: string }; const exportsMap: Record<string, ExportEntry> = { \".\": { types: \"./dist/index.d.ts\", import: \"./dist/index.js\", require: \"./dist/index.cjs\", }, }; const main = exportsMap[\".\"]; const typesPath: string | undefined = main.types; const missing: number = main.types;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The exports map, the types condition, subpath exports, and why the order of conditions in that object is load-bearing.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A package with an `exports` map and a stale top-level `types` field can resolve fine for the author and to `any` for everyone else. Consumers trust the published entrypoints; if types and runtime diverge, their editor lies while your CI stays green.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "page-about",
      "title": "About",
      "category": "Pages",
      "tab": "pages",
      "route": "/about",
      "icon": "ph-info",
      "keywords": [
        "about",
        "typescript school",
        "mission",
        "compiler",
        "why"
      ],
      "headings": [
        "About"
      ],
      "bodyText": "about. typescript school. mission. compiler. why",
      "classes": [],
      "chunks": []
    },
    {
      "id": "page-curriculum",
      "title": "Curriculum",
      "category": "Pages",
      "tab": "pages",
      "route": "/curriculum",
      "icon": "ph-map-trifold",
      "keywords": [
        "curriculum",
        "map",
        "syllabus",
        "tracks",
        "tiers",
        "overview"
      ],
      "headings": [
        "Curriculum"
      ],
      "bodyText": "curriculum. map. syllabus. tracks. tiers. overview",
      "classes": [],
      "chunks": []
    },
    {
      "id": "page-glossary",
      "title": "Glossary",
      "category": "Pages",
      "tab": "pages",
      "route": "/glossary",
      "icon": "ph-book-bookmark",
      "keywords": [
        "glossary",
        "terms",
        "definitions",
        "vocabulary",
        "jargon"
      ],
      "headings": [
        "Glossary"
      ],
      "bodyText": "glossary. terms. definitions. vocabulary. jargon",
      "classes": [],
      "chunks": []
    },
    {
      "id": "page-history",
      "title": "History",
      "category": "Pages",
      "tab": "pages",
      "route": "/history",
      "icon": "ph-clock-countdown",
      "keywords": [
        "history",
        "timeline",
        "javascript",
        "nodejs",
        "typescript",
        "adoption",
        "es5"
      ],
      "headings": [
        "History"
      ],
      "bodyText": "history. timeline. javascript. nodejs. typescript. adoption. es5",
      "classes": [],
      "chunks": []
    },
    {
      "id": "page-home",
      "title": "Home",
      "category": "Pages",
      "tab": "pages",
      "route": "/",
      "icon": "ph-house",
      "keywords": [
        "home",
        "start",
        "typescript school"
      ],
      "headings": [
        "Home"
      ],
      "bodyText": "home. start. typescript school",
      "classes": [],
      "chunks": []
    },
    {
      "id": "page-profile",
      "title": "Profile",
      "category": "Pages",
      "tab": "pages",
      "route": "/profile",
      "icon": "ph-user",
      "keywords": [
        "profile",
        "progress",
        "notes",
        "export",
        "clear",
        "local data",
        "privacy"
      ],
      "headings": [
        "Profile"
      ],
      "bodyText": "profile. progress. notes. export. clear. local data. privacy",
      "classes": [],
      "chunks": []
    },
    {
      "id": "page-terms",
      "title": "Terms",
      "category": "Pages",
      "tab": "pages",
      "route": "/terms",
      "icon": "ph-file-text",
      "keywords": [
        "terms",
        "disclaimer",
        "liability",
        "AI Act",
        "transparency",
        "MIT",
        "license"
      ],
      "headings": [
        "Terms"
      ],
      "bodyText": "terms. disclaimer. liability. AI Act. transparency. MIT. license",
      "classes": [],
      "chunks": []
    },
    {
      "id": "parameter-properties",
      "title": "Parameter properties",
      "category": "Objects, classes & modules",
      "tab": "intermediate",
      "route": "/lessons/structures/parameter-properties",
      "icon": "ph-cube",
      "keywords": [
        "parameter property",
        "constructor",
        "shorthand",
        "erasable",
        "di",
        "structures",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "constructor(private `readonly` db: Db) declares and assigns in one place — and emits real JavaScript, so it cannot be type-stripped.. The most convenient class syntax in TypeScript is one of the few that Node's native type stripping refuses to run. Manual field assignment in constructors. `readonly`/public/private on ctor params create fields.. `readonly`/public/private on ctor params create fields.. Keep them for simple data holders; prefer explicit fields when logic grows.. `erasableSyntaxOnly` may restrict some parameter property forms.. Manual field assignment in constructors.. Parameter properties `declare` and assign. name is string.. class User { constructor(name) { this.name = name; } }. class User { constructor(readonly name: string) {} } const u = new User(\"Ada\"); const n: string = u.name; const bad: number = u.name;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "constructor(private `readonly` db: Db) declares and assigns in one place — and emits real JavaScript, so it cannot be type-stripped.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The most convenient class syntax in TypeScript is one of the few that Node's native type stripping refuses to run. Manual field assignment in constructors. `readonly`/public/private on ctor params create fields.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "parse-dont-validate",
      "title": "Parse, don't validate",
      "category": "The runtime boundary",
      "tab": "intermediate",
      "route": "/lessons/runtime-boundary/parse-dont-validate",
      "icon": "ph-shield-check",
      "keywords": [
        "parse don't validate",
        "illegal states",
        "modelling",
        "narrowing",
        "design",
        "runtime-boundary",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Return the narrowed value rather than a boolean, so the proof travels with the data and cannot be forgotten one caller later.. isValid(input) checks the data and returns nothing about it, so the next line still handles a type that includes the invalid case. Boolean validation — the proof does not travel with the value.. Validation asks a question; parsing produces a value whose type encodes the answer.. Branded types (or dedicated interfaces) stop raw strings from entering privileged APIs.. Prefer `parseX(unknown): X | error` over `isX` + unchecked use at every call site.. Boolean validation — the proof does not travel with the value.. Parsing returns Email | `null`. Passing a plain string to send is a type error.. What is the advantage of `parseEmail` returning `Email | null` over `isEmail` returning boolean?. function isEmail(value) { return typeof value === \"string\" && value.includes(\"@\"); } function send(value) { if (!isEmail(value)) return; // value is still \"whatever\" to the next maintainer. return value.toLowerCase(); }. type Email = string & { readonly __brand: \"Email\" }; function parseEmail(value: unknown): Email | null { if (typeof value !== \"string\" || !value.includes(\"@\")) { return null; } return value as Email; } function send(email: Email): string { return email.toLowerCase(); } declare const raw: unknown; const parsed = parseEmail(raw); if (parsed) { send(parsed); } // A bare string is not Email — call si…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Return the narrowed value rather than a boolean, so the proof travels with the data and cannot be forgotten one caller later.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "isValid(input) checks the data and returns nothing about it, so the next line still handles a type that includes the invalid case. Boolean validation — the proof does not travel with the value.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "partial-mocks",
      "title": "Partial and deep-partial mocks",
      "category": "Testing with types",
      "tab": "intermediate",
      "route": "/lessons/testing/partial-mocks",
      "icon": "ph-test-tube",
      "keywords": [
        "Partial",
        "DeepPartial",
        "mock",
        "cast",
        "recursive",
        "testing",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`Partial<T>` for the shallow case, a recursive helper for the deep one, and the single cast at the boundary that you should make deliberately and once.. Mocking a client with thirty methods to test one of them means writing twenty-nine you will never call. `Partial` mock passed where full API required. Type partial mocks with `Pick`/`Partial` of the real interface.. Type partial mocks with `Pick`/`Partial` of the real interface.. Do not cast partials to the full type.. Keep tests honest about which methods are used.. `Partial` mock passed where full API required.. `Pick<Api,'get'>` is not Api — missing post.. const api = { get: jest.fn(), post: jest.fn() };. type Api = { get(path: string): Promise<unknown>; post(path: string, body: unknown): Promise<unknown>; }; const api: Pick<Api, \"get\"> = { get: async () => ({}), }; declare function useGet(client: Pick<Api, \"get\">): void; useGet(api); declare function useAll(client: Api): void; useAll(api);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`Partial<T>` for the shallow case, a recursive helper for the deep one, and the single cast at the boundary that you should make deliberately and once.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Mocking a client with thirty methods to test one of them means writing twenty-nine you will never call. `Partial` mock passed where full API required. Type partial mocks with `Pick`/`Partial` of the real interface.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "polymorphic-this-types",
      "title": "Polymorphic this",
      "category": "Objects, classes & modules",
      "tab": "advanced",
      "route": "/lessons/structures/polymorphic-this-types",
      "icon": "ph-cube",
      "keywords": [
        "this type",
        "fluent",
        "builder",
        "chaining",
        "subclass",
        "structures",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A method that returns this keeps the subclass's type through a chain — the mechanism behind every fluent builder that survives inheritance.. A builder method typed to return the base class truncates the chain the moment someone subclasses it. Runtime this-chaining works; bad return types break it in TS.. The polymorphic this type is the type of the implementing class, not the base declaration site.. Fluent APIs should return this (or a generic subtype) rather than the base class name.. Interfaces can use thisType and methods returning this for the same pattern.. Runtime this-chaining works; bad return types break it in TS.. Returning this preserves UserBuilder through the chain.. Add setX(x: number): this to a class Point and chain setX(1).. Why return `this` instead of `Builder` from withName?. // JS: fluent builders return this — subclasses keep working at runtime. class Builder { withName(name) { this.name = name; return this; } } class UserBuilder extends Builder { withEmail(email) { this.email = email; return this; } } new UserBuilder().withName(\"Ada\").withEmail(\"a@b.co\");. class Builder { name = \"\"; withName(name: string): this { this.name = name; return this; } } class UserBuilder extends Builder { email = \"\"; withEmail(email: string): this { this.email = email; return this; } } const u = new UserBuilder().withName(\"Ada\").withEmail(\"a@b.co\"); // If withName returned Builder, withEmail would not exist on the chain: const bad: Builder = new UserBuilder().withName(\"A…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A method that returns this keeps the subclass's type through a chain — the mechanism behind every fluent builder that survives inheritance.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A builder method typed to return the base class truncates the chain the moment someone subclasses it. Runtime this-chaining works; bad return types break it in TS.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "primitive-types",
      "title": "The primitives, and their impostors",
      "category": "Types & narrowing",
      "tab": "beginner",
      "route": "/lessons/types/primitive-types",
      "icon": "ph-shapes",
      "keywords": [
        "string",
        "number",
        "boolean",
        "bigint",
        "String object",
        "types",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "string, number, boolean, `null`, `undefined`, symbol, `bigint` — and why `String` with a capital S is a different type that will bite you.. JavaScript has one number type and seven falsy values, and a codebase that never says which it means ends up with '1' + 1 in production. Primitives mix silently; the bug is often string vs number.. Prefer lowercase primitives: string, number, boolean — not `String`, Number, Boolean wrappers.. `bigint` is a separate type; you cannot mix it with number without an explicit conversion.. At HTTP boundaries, parse strings into the primitive you mean before calling domain functions.. Primitives mix silently; the bug is often string vs number.. number is not `String`, and it is not the string \"7\".. Parse the days argument before calling addDays.. Which annotation is almost always wrong for text data?. function addDays(start, days) { return start + days * 86400000; } // Query params are strings; \"7\" + math becomes concatenation chaos. addDays(Date.now(), \"7\");. function addDays(start: number, days: number): number { return start + days * 86400000; } addDays(Date.now(), \"7\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "string, number, boolean, `null`, `undefined`, symbol, `bigint` — and why `String` with a capital S is a different type that will bite you.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "JavaScript has one number type and seven falsy values, and a codebase that never says which it means ends up with '1' + 1 in production. Primitives mix silently; the bug is often string vs number.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "project-references",
      "title": "Project references and monorepos",
      "category": "Tooling & the strictness ladder",
      "tab": "advanced",
      "route": "/lessons/tooling/project-references",
      "icon": "ph-wrench",
      "keywords": [
        "project references",
        "monorepo",
        "composite",
        "tsc --build",
        "workspace",
        "tooling",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Splitting a repository into buildable units, `tsc` --build, and the difference between referencing a project and importing its source.. A monorepo where every package sees every other package's source has one enormous compilation unit and no boundaries at all. No project graph means no incremental typecheck boundaries.. composite: true + references build a DAG that `tsc -b` can check incrementally.. Prefer depending on emitted declarations of referenced projects, not their raw src across the graph.. Solution-style roots list references without compiling app code themselves.. No project graph means no incremental typecheck boundaries.. composite marks buildable units in the reference graph.. Type Ref = { path: string } and a root config with references: Ref[].. What does composite enable?. // JS monorepos often import source across packages with no build graph. // Everything is one pile of files to the bundler.. type TsConfig = { composite?: boolean; references?: { path: string }[]; }; const root: TsConfig = { references: [{ path: \"./packages/core\" }, { path: \"./packages/app\" }], }; const core: TsConfig = { composite: true }; // Prefer boolean checks over literal true/false assignments from optional fields. const isComposite = core.composite === true; const bad: string = isComposite; void root;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Splitting a repository into buildable units, `tsc` --build, and the difference between referencing a project and importing its source.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A monorepo where every package sees every other package's source has one enormous compilation unit and no boundaries at all. No project graph means no incremental typecheck boundaries.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "promise-combinators",
      "title": "all, allSettled, race, any",
      "category": "Async, errors & Result",
      "tab": "intermediate",
      "route": "/lessons/async/promise-combinators",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "Promise.all",
        "allSettled",
        "race",
        "tuple",
        "AggregateError",
        "async",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`Promise.all` preserves a tuple's element types, allSettled gives you a discriminated union per element, and race and `any` differ in how they fail.. `Promise.all` over a heterogeneous array collapses to a union unless the argument is a tuple, and array literals are not tuples by default. Assuming `Promise.all` shape.. all fails fast; allSettled preserves per-task status.. race/`any` pick first settlement — type the winner carefully.. Type the array you pass in — inference follows.. Assuming `Promise.all` shape.. `Promise.all` returns User[], not number.. Promise.all(tasks).then(xs => xs[0].id);. type User = { id: string }; declare const tasks: Promise<User>[]; export async function firstId(): Promise<string> { const xs = await Promise.all(tasks); return xs[0]!.id; } const n: number = await Promise.all(tasks);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`Promise.all` preserves a tuple's element types, allSettled gives you a discriminated union per element, and race and `any` differ in how they fail.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "`Promise.all` over a heterogeneous array collapses to a union unless the argument is a tuple, and array literals are not tuples by default. Assuming `Promise.all` shape.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "promise-types",
      "title": "Promise<T>",
      "category": "Async, errors & Result",
      "tab": "beginner",
      "route": "/lessons/async/promise-types",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "Promise",
        "async",
        "generics",
        "then",
        "async",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A Promise carries a value type — `Promise<User>` is not `Promise<any>`, and mixing them loses safety at await.. A function returns a Promise that sometimes resolves to a user and sometimes to `null`, but callers always await .email. `null`.email awaits you in production. Annotate `Promise<T>` on functions that return promises so callers see T.. Annotate `Promise<T>` on functions that return promises so callers see T.. `Promise<User | null>` is honest; `Promise<User>` with silent `null` is not.. Avoid `Promise<any>` — it undoes the generic.. `null`.email awaits you in production.. The Promise type includes `null` — narrow after await/then.. Narrow u before reading email.. What is the type of await findUser(...) here?. function findUser(id) { return Promise.resolve(id === \"x\" ? null : { id, email: \"a@b.co\" }); } findUser(\"x\").then((u) => u.email);. type User = { id: string; email: string }; function findUser(id: string): Promise<User | null> { return Promise.resolve(id === \"x\" ? null : { id, email: \"a@b.co\" }); } findUser(\"x\").then((u) => u.email);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A Promise carries a value type — `Promise<User>` is not `Promise<any>`, and mixing them loses safety at await.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A function returns a Promise that sometimes resolves to a user and sometimes to `null`, but callers always await .email. `null`.email awaits you in production. Annotate `Promise<T>` on functions that return promises so callers see T.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "publishing-types",
      "title": "Publishing a typed package",
      "category": "Node.js migration",
      "tab": "advanced",
      "route": "/lessons/node-migration/publishing-types",
      "icon": "ph-hard-drives",
      "keywords": [
        "publishing",
        "declaration",
        "@arethetypeswrong",
        "d.ts",
        "public api",
        "node-migration",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Declaration emit, declaration maps, the types condition per export, and checking the result with a tool rather than hope.. A package's types are only correct in the configuration its author used, and consumers find out at install time. Without verified `.d.ts`, downstream teams guess the API — or abandon your types entirely.. Emit declaration files and verify them with @arethetypeswrong / publint.. Prefer explicit return types on exports so .d.ts does not reference private names.. Align `package.json` exports types conditions with the JS entrypoints you ship.. Without verified .d.ts, consumers guess the API.. Public returns should be explicit; optional Map values are | `undefined`.. Export function add(a: number, b: number): number with an explicit return type.. Why annotate exported return types explicitly?. // JS packages ship .js — types are optional afterthoughts. // \"types\" in package.json may point at missing or wrong files.. // Public API should use portable, named types — not inferred internals. type Internal = { secret: string }; export type PublicUser = { id: string; name: string }; export function toPublic(u: Internal): PublicUser { return { id: \"1\", name: u.secret }; } // Shipping a function whose return type cannot be named breaks declaration emit. const cache = new Map<string, PublicUser>(); export function ca…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Declaration emit, declaration maps, the types condition per export, and checking the result with a tool rather than hope.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A package's types are only correct in the configuration its author used, and consumers find out at install time. Without verified `.d.ts`, downstream teams guess the API — or abandon your types entirely.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "reading-type-errors",
      "title": "Reading a TypeScript error",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/reading-type-errors",
      "icon": "ph-compass",
      "keywords": [
        "diagnostics",
        "error codes",
        "Did you mean",
        "assignability",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "How to decode TS2xxx messages: expected vs actual, property paths, and the 'Did you mean…?' hints that save a refactor.. Long error messages get skimmed, then silenced. The useful part is usually near the top: the property that failed assignability, or the overload that did not match. Learning to read them is faster than learning to disable them.. Start at the last line of the message: it names the expression TypeScript rejected.. Work upward: required property, expected type, actual type.. Error codes are searchable; 'Did you mean' hints often expose typos like nam vs name.. A casing typo becomes '`undefined` Lovelace' at runtime.. `TS2345` spells out the missing property — firstName vs firstname.. Fix the object so fullName type-checks.. Best first step when reading a long assignability error?. function fullName(user) { return user.firstName + \" \" + user.lastName; } const user = { firstname: \"Ada\", lastName: \"Lovelace\" }; fullName(user);. type User = { firstName: string; lastName: string }; function fullName(user: User) { return user.firstName + \" \" + user.lastName; } const user = { firstname: \"Ada\", lastName: \"Lovelace\" }; fullName(user);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "How to decode TS2xxx messages: expected vs actual, property paths, and the 'Did you mean…?' hints that save a refactor.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Long error messages get skimmed, then silenced. The useful part is usually near the top: the property that failed assignability, or the overload that did not match. Learning to read them is faster than learning to disable them.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "readonly-and-immutability",
      "title": "readonly, and how far it goes",
      "category": "Types & narrowing",
      "tab": "intermediate",
      "route": "/lessons/types/readonly-and-immutability",
      "icon": "ph-shapes",
      "keywords": [
        "readonly",
        "ReadonlyArray",
        "immutability",
        "shallow",
        "Object.freeze",
        "types",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`readonly` properties, ReadonlyArray, and the honest limits: it is a compile-time promise about one level, not a frozen object.. Passing an array to a helper and getting it back sorted in place is a bug the type system will happily allow. Mutating the caller's array. `readonly` arrays forbid mutating methods like push.. `readonly` arrays forbid mutating methods like push.. Return a new array instead of mutating shared state.. `Readonly<T>` / `readonly` props document intent at API boundaries.. Mutating the caller's array.. push does not exist on `readonly` string[].. What does marking an array as `readonly` prevent at the type level?. function add(list, item) { list.push(item); return list; }. function add(list: readonly string[], item: string): string[] { return [...list, item]; } const xs: readonly string[] = [\"a\"]; xs.push(\"b\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`readonly` properties, ReadonlyArray, and the honest limits: it is a compile-time promise about one level, not a frozen object.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Passing an array to a helper and getting it back sorted in place is a bug the type system will happily allow. Mutating the caller's array. `readonly` arrays forbid mutating methods like push.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "recursive-conditional-types",
      "title": "Recursive conditional types",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/recursive-conditional-types",
      "icon": "ph-function",
      "keywords": [
        "recursion",
        "DeepPartial",
        "DeepReadonly",
        "depth limit",
        "tail recursion",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Conditional types that call themselves: DeepReadonly, DeepPartial, flattening, and the instantiation-depth limit that ends the fun.. Type instantiation is excessively deep and possibly infinite is the error where type-level programming stops being free. Runtime deep-freeze cannot make nested fields `readonly` in types.. Recursive conditionals are powerful and expensive — each instantiation costs checker work.. TypeScript caps instantiation depth; pathological recursion yields `TS2589`.. Prefer depth limits, leaf special-cases (Date, Map), and simpler `Partial` when “deep” is not worth it.. Runtime deep-freeze cannot make nested fields `readonly` in types.. DeepReadonly recurses; assignment to a nested field fails.. Implement DeepPartial<T> so nested objects become optional at every level. Assign a partial nested object without error.. What typically causes “Type instantiation is excessively deep”?. // JS: deep-freeze by walking at runtime — no compile-time depth story. function deepFreeze(obj) { Object.freeze(obj); for (const v of Object.values(obj)) { if (v && typeof v === \"object\") deepFreeze(v); } return obj; }. type DeepReadonly<T> = { readonly [K in keyof T]: T[K] extends object ? DeepReadonly<T[K]> : T[K]; }; type Config = { db: { host: string } }; type Frozen = DeepReadonly<Config>; const cfg: Frozen = { db: { host: \"x\" } }; cfg.db.host = \"y\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Conditional types that call themselves: DeepReadonly, DeepPartial, flattening, and the instantiation-depth limit that ends the fun.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Type instantiation is excessively deep and possibly infinite is the error where type-level programming stops being free. Runtime deep-freeze cannot make nested fields `readonly` in types.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "recursive-types",
      "title": "Recursive types",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/recursive-types",
      "icon": "ph-function",
      "keywords": [
        "recursive type",
        "json",
        "tree",
        "self reference",
        "lazy",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Self-referential types for trees, linked structures, and JSON. How lazy resolution makes them possible and where it gives up.. JSON has no fixed shape, so the honest type for it is recursive and most codebases substitute `any` instead. Without a recursive type, nested JSON is unchecked hope.. Type aliases may refer to themselves; interfaces may too via property types.. The checker expands recursion lazily — it does not unfold infinitely at definition time.. Prefer Json over `any` at boundaries; pair with a runtime parse that enforces the same grammar.. Without a recursive type, nested JSON is unchecked hope.. A recursive alias names the real JSON grammar.. Define a Tree<T> type: a node with value: T and children: Tree<T>[]. Then create a valid number tree.. Why does `type Json = … | Json[] | …` compile instead of looping forever?. // JS: JSON is \"whatever\" — any nested object or array. function readConfig(raw) { return JSON.parse(raw); // any shape, no recursion named } const cfg = readConfig('{\"db\":{\"host\":\"x\"}}'); cfg.db.host.toUpperCase(); // hope the nesting is right. type Json = | string | number | boolean | null | Json[] | { [key: string]: Json }; const ok: Json = { db: { host: \"localhost\", port: 5432 } }; const bad: Json = { db: { host: () => \"nope\" } }; // functions are not JSON",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Self-referential types for trees, linked structures, and JSON. How lazy resolution makes them possible and where it gives up.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "JSON has no fixed shape, so the honest type for it is recursive and most codebases substitute `any` instead. Without a recursive type, nested JSON is unchecked hope.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "renaming-your-first-file",
      "title": "Renaming your first file",
      "category": "Node.js migration",
      "tab": "beginner",
      "route": "/lessons/node-migration/renaming-your-first-file",
      "icon": "ph-hard-drives",
      "keywords": [
        "rename",
        ".ts",
        "incremental",
        "imports",
        "node-migration",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Move one `.js` module to `.ts`, fix the new errors, update imports/extensions, and leave the rest of the tree alone.. Renaming a leaf file reveals that callers passed the wrong shapes all along — and that is the point, not a reason to stop. A typo like `yrs` vs `years` stays hidden until the file is checked. The first rename teaches more than a week of planning meetings.. Rename leaves first (utils), then move inward toward HTTP entrypoints.. Fix import paths/extensions according to your module setting (nodenext cares).. One file green is progress — do not batch-rename hundreds at once.. yrs vs years — hidden until the file is checked.. The first rename teaches more than a week of planning.. Read user.years.. Why rename one file at a time?. // users.js export function age(user) { return user.yrs; }. type User = { years: number }; export function age(user: User): number { return user.yrs; }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Move one `.js` module to `.ts`, fix the new errors, update imports/extensions, and leave the rest of the tree alone.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Renaming a leaf file reveals that callers passed the wrong shapes all along — and that is the point, not a reason to stop. A typo like `yrs` vs `years` stays hidden until the file is checked. The first rename teaches more than a week of planning meetings.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "rest-parameters",
      "title": "Rest parameters and spread",
      "category": "Functions & generics",
      "tab": "beginner",
      "route": "/lessons/functions/rest-parameters",
      "icon": "ph-function",
      "keywords": [
        "rest",
        "spread",
        "...",
        "variadic",
        "functions",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Typing ...args as a tuple or an array — and why spreading the wrong shape fails at the call site instead of inside the loop.. A logger that takes 'any number of values' eventually receives an options object in the middle of the list. arguments is untyped; string sneaks into numeric reduce.. ...nums: number[] means zero or more numbers.. For a fixed head plus rest, use a tuple rest: ...args: [string, ...number[]].. Prefer rest over arguments — rest is an array with a real type.. arguments is untyped; string sneaks into numeric reduce.. Rest element types apply to every variadic argument.. Call sum with only numbers.. What is the type of nums inside sum(...nums: number[])?. function sum() { return [...arguments].reduce((a, b) => a + b, 0); } sum(1, 2, \"3\");. function sum(...nums: number[]): number { return nums.reduce((a, b) => a + b, 0); } sum(1, 2, \"3\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Typing ...args as a tuple or an array — and why spreading the wrong shape fails at the call site instead of inside the loop.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A logger that takes 'any number of values' eventually receives an options object in the middle of the list. arguments is untyped; string sneaks into numeric reduce.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "result-types",
      "title": "Returning failures instead of throwing",
      "category": "Async, errors & Result",
      "tab": "intermediate",
      "route": "/lessons/async/result-types",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "Result",
        "Either",
        "ok",
        "err",
        "typed errors",
        "neverthrow",
        "async",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A Result union puts the failure in the return type, where the checker can insist you handle it. The costs are real too.. Nothing in a signature says which of the forty functions below it can throw, so error handling is guesswork by inspection. `null` means both empty and failure. Result types make failure explicit in the type.. Result types make failure explicit in the type.. Prefer them when exceptions are control flow.. Keep error payloads structured for logging.. `null` means both empty and failure.. Discriminated Result — .value only on ok. Direct access fails.. Only read .value after checking r.ok. Match the solution text.. function parse(x) { try { return JSON.parse(x); } catch { return null; } }. type Result<T, E = string> = | { ok: true; value: T } | { ok: false; error: E }; function parseJson(text: string): Result<unknown> { try { return { ok: true, value: JSON.parse(text) as unknown }; } catch { return { ok: false, error: \"invalid json\" }; } } const r = parseJson(\"{}\"); if (r.ok) { const v: unknown = r.value; } const bad: string = parseJson(\"{}\").value;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A Result union puts the failure in the return type, where the checker can insist you handle it. The costs are real too.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Nothing in a signature says which of the forty functions below it can throw, so error handling is guesswork by inspection. `null` means both empty and failure. Result types make failure explicit in the type.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "running-typescript-in-node",
      "title": "Actually running the thing",
      "category": "Node.js migration",
      "tab": "beginner",
      "route": "/lessons/node-migration/running-typescript-in-node",
      "icon": "ph-hard-drives",
      "keywords": [
        "tsx",
        "ts-node",
        "emit",
        "production",
        "node",
        "node-migration",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Emit with `tsc`, or run with a loader/tsx in development — know what belongs in production versus local loops.. Production runs `ts-node` (or similar) on every request; startup cost and trust in an on-the-fly compiler become the outage. Runtime should execute trusted emit, not an ad-hoc transform under load.. Production: compile with `tsc` (or a bundler) and run the JS output.. Local: tsx / node --import tsx are fine for DX; keep them out of prod images.. Match module settings (nodenext/bundler) to how you actually launch Node.. Runtime should execute trusted emit, not an ad-hoc transform.. Catch entry mistakes before any runner ships them.. Assign main() to a string-typed binding.. Preferred production approach?. // Prod still needs plain JS (or a compiled artifact). function main() { return \"ok\"; } main();. export function main(): string { return \"ok\"; } // Demonstrates a typed entry — emit or a trusted runner executes this. const status: number = main();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Emit with `tsc`, or run with a loader/tsx in development — know what belongs in production versus local loops.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Production runs `ts-node` (or similar) on every request; startup cost and trust in an on-the-fly compiler become the outage. Runtime should execute trusted emit, not an ad-hoc transform under load.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "satisfies-operator",
      "title": "satisfies",
      "category": "Type-level programming",
      "tab": "intermediate",
      "route": "/lessons/type-level/satisfies-operator",
      "icon": "ph-function",
      "keywords": [
        "satisfies",
        "validation",
        "inference",
        "literals",
        "config",
        "type-level",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Check a value against a type without widening to that type — keep the literals you care about.. Annotating a config as a wide `Record` loses the literal keys you wanted for autocomplete. Palette validated only by convention. `satisfies` checks against a type without widening to it.. `satisfies` checks against a type without widening to it.. Preserves literal inference for keys/values.. Prefer it over as when you want both check and inference.. Palette validated only by convention.. `satisfies` checks the value while preserving literals. danger is not number.. Compared to `const x: Palette = {...}`, what does `satisfies Palette` keep?. const palette = { primary: \"#0af\", danger: \"red\" };. type Hex = `#${string}`; type Palette = Record<string, Hex | \"red\" | \"blue\">; const palette = { primary: \"#0af\", danger: \"red\", } satisfies Palette; const p: Hex | \"red\" | \"blue\" = palette.primary; const bad: number = palette.danger;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Check a value against a type without widening to that type — keep the literals you care about.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Annotating a config as a wide `Record` loses the literal keys you wanted for autocomplete. Palette validated only by convention. `satisfies` checks against a type without widening to it.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "schema-validation-libraries",
      "title": "Schemas as the source of truth",
      "category": "The runtime boundary",
      "tab": "intermediate",
      "route": "/lessons/runtime-boundary/schema-validation-libraries",
      "icon": "ph-shield-check",
      "keywords": [
        "zod",
        "valibot",
        "schema",
        "infer",
        "validation",
        "standard schema",
        "runtime-boundary",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Zod, Valibot, ArkType, TypeBox: `declare` the schema, `infer the` type from it. The direction of that arrow is what stops the two from drifting.. A hand-written interface and a hand-written validator describe the same shape twice, and only one of them gets updated. Two sources of truth — the typedef and the runtime checks — drift apart.. One schema → inferred static type + runtime parse. Invert that and drift returns.. Libraries differ in bundle size and error UX; the architecture (schema as source of truth) is the lesson.. At trust boundaries, call `.parse` / safeParse — never `as User` on JSON.. Two sources of truth — the typedef and the runtime checks — drift apart.. User is inferred from the schema object. Assigning age (number) to string fails.. Why `infer the` type from the schema instead of writing both by hand?. // types.js — hope someone updates this when the validator changes /** @typedef {{ id: string, age: number }} User */ function parseUser(input) { if (typeof input?.id !== \"string\") throw new Error(\"bad id\"); // forgot to check age return input; }. // Minimal schema-shaped API (stands in for Zod/Valibot in the sandbox). type Infer<S> = S extends { __type: infer T } ? T : never; type StrSchema = { kind: \"string\"; __type: string }; type NumSchema = { kind: \"number\"; __type: number }; type ObjSchema<S extends Record<string, { __type: unknown }>> = { kind: \"object\"; shape: S; __type: { [K in keyof S]: Infer<S[K]> }; }; const str = (): StrSchema…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Zod, Valibot, ArkType, TypeBox: `declare` the schema, `infer the` type from it. The direction of that arrow is what stops the two from drifting.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A hand-written interface and a hand-written validator describe the same shape twice, and only one of them gets updated. Two sources of truth — the typedef and the runtime checks — drift apart.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "shimming-untyped-dependencies",
      "title": "Dependencies with no types",
      "category": "Node.js migration",
      "tab": "advanced",
      "route": "/lessons/node-migration/shimming-untyped-dependencies",
      "icon": "ph-hard-drives",
      "keywords": [
        "declare module",
        "shim",
        "ambient",
        "DefinitelyTyped",
        "untyped",
        "node-migration",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`declare` module shims, writing just enough of a definition to be useful, and when to send it to DefinitelyTyped instead of keeping it.. One untyped dependency turns a whole call graph into `any`, and `noImplicitAny` cannot see through it. Teams either abandon the library or sprinkle casts until the checker is decorative.. Start with a narrow shim of the functions you call — not a full fictional API.. In real projects that shim is `declare module \"pkg\" { … }` in a .d.ts.. Prefer @types from DefinitelyTyped when available; contribute improvements upstream.. Untyped deps accept anything and return anything.. A minimal shim restores checking at the boundary.. Type Add = (a: number, b: number) => number and call a `declare` const add: Add.. Best first shim for a single function dependency?. // JS: require an untyped helper and pass anything. const slugify = require(\"legacy-slugify\"); slugify(null);. // Local shim standing in for declare module \"legacy-slugify\". type Slugify = (input: string) => string; declare const slugify: Slugify; const s = slugify(\"Hello World\"); const bad = slugify(null); void s;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`declare` module shims, writing just enough of a definition to be useful, and when to send it to DefinitelyTyped instead of keeping it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "One untyped dependency turns a whole call graph into `any`, and `noImplicitAny` cannot see through it. Teams either abandon the library or sprinkle casts until the checker is decorative.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "static-members-and-static-blocks",
      "title": "Static members",
      "category": "Objects, classes & modules",
      "tab": "intermediate",
      "route": "/lessons/structures/static-members-and-static-blocks",
      "icon": "ph-cube",
      "keywords": [
        "static",
        "static block",
        "class type",
        "typeof class",
        "factory",
        "structures",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Statics, static blocks, and the type of the class itself — which is what you need when a function takes a class rather than an instance.. `typeof` MyClass and MyClass are different types, and the error message uses both words without distinguishing them. Static mutable state with no types. Static blocks initialize per class evaluation.. Static blocks initialize per class evaluation.. Private static fields keep counters encapsulated.. Prefer modules for singletons when inheritance is not required.. Static mutable state with no types.. Static bump returns number.. class Counter { static n = 0; static bump() { Counter.n++; } }. class Counter { static #n = 0; static { Counter.#n = 0; } static bump(): number { Counter.#n += 1; return Counter.#n; } } const n: number = Counter.bump(); const bad: string = Counter.bump();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Statics, static blocks, and the type of the class itself — which is what you need when a function takes a class rather than an instance.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "`typeof` MyClass and MyClass are different types, and the error message uses both words without distinguishing them. Static mutable state with no types. Static blocks initialize per class evaluation.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "strict-mode",
      "title": "What strict actually turns on",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/strict-mode",
      "icon": "ph-compass",
      "keywords": [
        "strict",
        "strictNullChecks",
        "noImplicitAny",
        "flags",
        "config",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`strict` is eight flags in a trench coat. What each one rejects, and why turning them on individually is the practical route for an existing codebase.. A project on non-`strict` settings trains the team that `null` is fine until it is not. Turning on `strict` later feels like a thousand new bugs — they were already there, just invisible.. `strict` enables a bundle: `strictNullChecks`, `noImplicitAny`, `strictFunctionTypes`, and more.. For a brownfield Node app, turn flags on one at a time — start with `noImplicitAny` and `strictNullChecks`.. A codebase that compiles only with `strict: false` is barely typed; prefer fixing errors over disabling the flag.. Optional values and missing arguments are everyday Node bugs.. `strictNullChecks` turns 'maybe missing' into a compile error.. Accept an optional user and return a fallback when missing.. Which `strict` flag rejects `undefined` where a User is required?. function label(user) { // user might be missing; .name still \"works\" until it doesn't. return user.name.toUpperCase(); } label(undefined);. type User = { name: string }; function label(user: User) { return user.name.toUpperCase(); } // Under strictNullChecks (part of strict), undefined is not a User. label(undefined);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`strict` is eight flags in a trench coat. What each one rejects, and why turning them on individually is the practical route for an existing codebase.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A project on non-`strict` settings trains the team that `null` is fine until it is not. Turning on `strict` later feels like a thousand new bugs — they were already there, just invisible.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "strictfunctiontypes",
      "title": "strictFunctionTypes",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/strictfunctiontypes",
      "icon": "ph-wrench",
      "keywords": [
        "strictFunctionTypes",
        "variance",
        "contravariance",
        "callback",
        "method",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Contravariant parameter checking for function-typed properties, the method exemption it deliberately keeps, and what that means for your callbacks.. The flag is on, the check still does not apply to methods, and nothing explains why one of your two handlers is checked. Assuming a Dog handler can accept `any` Animal.. Function parameters are checked more strictly under this flag.. A Dog=>`void` is not an Animal=>`void`.. This prevents barking on plain Animals.. Assuming a Dog handler can accept `any` Animal.. Under `strictFunctionTypes`, parameter positions are checked contravariantly.. const handler = (dog) => dog.bark(); acceptAnimal(handler);. type Animal = { eat(): void }; type Dog = Animal & { bark(): void }; type AnimalHandler = (a: Animal) => void; const handler = (dog: Dog) => { dog.bark(); }; const ok: AnimalHandler = handler;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Contravariant parameter checking for function-typed properties, the method exemption it deliberately keeps, and what that means for your callbacks.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The flag is on, the check still does not apply to methods, and nothing explains why one of your two handlers is checked. Assuming a Dog handler can accept `any` Animal.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "strictnullchecks",
      "title": "strictNullChecks",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/strictnullchecks",
      "icon": "ph-wrench",
      "keywords": [
        "strictNullChecks",
        "null",
        "undefined",
        "flag",
        "migration",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The rung that pays for the whole migration. What it flags, why the error count is front-loaded, and the patterns that clear it fastest.. Cannot read properties of `undefined` is the most common runtime error in Node, and this is the flag that finds it. `null` slides into string APIs. `strictNullChecks` makes `null`/`undefined` illicit for ordinary types.. `strictNullChecks` makes `null`/`undefined` illicit for ordinary types.. It is included in `strict`: true — keep it on.. Use unions explicitly when absence is allowed.. `null` slides into string APIs.. With `strictNullChecks`, `null` is not string.. With `strictNullChecks`, is `null` assignable to string?. function len(s) { return s.length; } len(null);. function len(s: string): number { return s.length; } len(null);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The rung that pays for the whole migration. What it flags, why the error count is front-loaded, and the patterns that clear it fastest.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Cannot read properties of `undefined` is the most common runtime error in Node, and this is the flag that finds it. `null` slides into string APIs. `strictNullChecks` makes `null`/`undefined` illicit for ordinary types.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "strictpropertyinitialization",
      "title": "strictPropertyInitialization",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/strictpropertyinitialization",
      "icon": "ph-wrench",
      "keywords": [
        "strictPropertyInitialization",
        "class",
        "field",
        "definite assignment",
        "constructor",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Class fields must be assigned by the end of the constructor. What that catches, and the three legitimate escape hatches.. A field initialised in an async init method is `undefined` for every call that arrives before it, and the type says otherwise. Fields used before assigned. Definite assignment analysis catches uninitialised fields.. Definite assignment analysis catches uninitialised fields.. Use definite assignment assertions sparingly (!).. Constructor parameter properties satisfy the check.. Fields used before assigned.. Strict property initialization requires assigning name.. class User { name; constructor() {} }. class User { name: string; constructor() {} }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Class fields must be assigned by the end of the constructor. What that catches, and the three legitimate escape hatches.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A field initialised in an async init method is `undefined` for every call that arrives before it, and the type says otherwise. Fields used before assigned. Definite assignment analysis catches uninitialised fields.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "structural-test-doubles",
      "title": "Why fakes are cheap here",
      "category": "Testing with types",
      "tab": "intermediate",
      "route": "/lessons/testing/structural-test-doubles",
      "icon": "ph-test-tube",
      "keywords": [
        "test double",
        "fake",
        "structural",
        "dependency injection",
        "seam",
        "testing",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Structural typing means no interface declaration, no implements, and no framework — a plain object is a valid double if its shape matches.. Mocking frameworks exist to solve a nominal-typing problem that TypeScript does not have. Duck-typed logger with no contract. Structural typing makes lightweight stubs easy.. Structural typing makes lightweight stubs easy.. Export small interfaces for dependencies.. Avoid relying on excess fields in doubles.. Duck-typed logger with no contract.. Structural typing accepts matching shapes; debug-only object fails.. function greet(logger) { logger.info('hi'); }. type Logger = { info(msg: string): void }; function greet(logger: Logger): void { logger.info(\"hi\"); } greet({ info: (m) => undefined }); greet({ info: (m) => m.length }); greet({ debug: (m) => undefined });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Structural typing means no interface declaration, no implements, and no framework — a plain object is a valid double if its shape matches.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Mocking frameworks exist to solve a nominal-typing problem that TypeScript does not have. Duck-typed logger with no contract. Structural typing makes lightweight stubs easy.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "structural-typing",
      "title": "Structural typing: shape, not name",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/structural-typing",
      "icon": "ph-compass",
      "keywords": [
        "structural",
        "duck typing",
        "excess property",
        "compatibility",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "TypeScript compares shapes, not class or interface names — why an object with the right fields is accepted, and when excess properties still get rejected.. Two types with the same fields are assignable even if they came from different 'nominal' ideas in your head. Extra properties sometimes fail (fresh object literals) and sometimes do not — which surprises people coming from nominal languages.. Compatibility is structural: if the required fields exist with compatible types, the name of the type rarely matters.. Object literals passed directly are checked for `unknown` properties — a footgun-prevention feature, not a second type system.. Assign the literal to a variable first if you intentionally need extra fields (or use a wider type).. Shape compatibility is informal in JS; secrets hitch a ride easily.. Fresh object literals get excess-property checks; named variables do not.. Make the failing call compile without removing role — use a variable.. Why does printUser(admin) succeed while the inline object fails?. function printUser(user) { return user.id + \":\" + user.name; } // Any object with id/name works — including ones with surprises. printUser({ id: \"1\", name: \"Ada\", passwordHash: \"…\" });. type User = { id: string; name: string }; function printUser(user: User) { return user.id + \":\" + user.name; } const admin = { id: \"1\", name: \"Ada\", role: \"admin\" }; printUser(admin); // OK: structural — extra fields on a variable are fine printUser({ id: \"2\", name: \"Bob\", role: \"admin\" }); // excess-property check",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "TypeScript compares shapes, not class or interface names — why an object with the right fields is accepted, and when excess properties still get rejected.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Two types with the same fields are assignable even if they came from different 'nominal' ideas in your head. Extra properties sometimes fail (fresh object literals) and sometimes do not — which surprises people coming from nominal languages.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "suppressions",
      "title": "Suppressing an error honestly",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/suppressions",
      "icon": "ph-wrench",
      "keywords": [
        "ts-expect-error",
        "ts-ignore",
        "suppression",
        "debt",
        "review",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "@ts-expect-error against @ts-ignore, why the first is nearly always right, and keeping suppressions from becoming permanent.. @ts-ignore stays silent forever, including after the underlying problem is fixed and the suppression is hiding a new one. @ts-ignore hiding a landmine. Prefer @ts-expect-error over @ts-ignore — it fails when the error disappears.. Prefer @ts-expect-error over @ts-ignore — it fails when the error disappears.. Leave a comment explaining why.. Fix the type instead when you can.. @ts-ignore hiding a landmine.. Suppressions are last resort. Final line shows a real error still caught.. // @ts-ignore const n = null.length;. const maybe: string | null = null; // Prefer @ts-expect-error with a reason — still a last resort: // @ts-expect-error demo — null has no length const n: number = maybe.length; const ok: string = \"x\"; const bad: number = ok;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "@ts-expect-error against @ts-ignore, why the first is nearly always right, and keeping suppressions from becoming permanent.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "@ts-ignore stays silent forever, including after the underlying problem is fixed and the suppression is hiding a new one. @ts-ignore hiding a landmine. Prefer @ts-expect-error over @ts-ignore — it fails when the error disappears.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "template-literal-inference",
      "title": "Parsing strings in the type system",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/template-literal-inference",
      "icon": "ph-function",
      "keywords": [
        "template literal",
        "infer",
        "parse",
        "route params",
        "string",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`infer inside` a template literal type turns a string into structure: route parameters, query keys, and typed string builders.. \"/users/:id/posts/:postId\" contains two parameter names that no framework can check unless the type system reads the string. Parameter names are untyped strings in plain JS routers.. Template literal types + `infer are` a parser: match a prefix, bind a piece, recurse on the rest.. Keep grammars small — string parsers explode compile time quickly.. Frameworks use this for typed routes; prefer codegen when the string language gets rich.. Parameter names are untyped strings in plain JS routers.. `infer walks` the template; wrong param names are not assignable.. Implement StartsWithHello<S> that is true if S starts with \"hello\", else false (as type booleans).. What does `S extends \\`${string}:${`infer` P}\\`` bind in P for \"/users/:id\"?. // JS: route params are strings looked up by name — typos are runtime 404s. function param(path, name) { const parts = path.split(\"/\"); const i = parts.indexOf(\":\" + name); return i >= 0 ? parts[i] : undefined; } param(\"/users/:id\", \"Id\"); // typo, silent miss. type ExtractParams<S extends string> = S extends `${string}:${infer P}/${infer Rest}` ? P | ExtractParams<Rest> : S extends `${string}:${infer P}` ? P : never; type Params = ExtractParams<\"/users/:id/posts/:postId\">; // \"id\" | \"postId\" const need: Params = \"userId\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`infer inside` a template literal type turns a string into structure: route parameters, query keys, and typed string builders.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "\"/users/:id/posts/:postId\" contains two parameter names that no framework can check unless the type system reads the string. Parameter names are untyped strings in plain JS routers.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "template-literal-types-intro",
      "title": "Template literal types",
      "category": "Type-level programming",
      "tab": "intermediate",
      "route": "/lessons/type-level/template-literal-types-intro",
      "icon": "ph-function",
      "keywords": [
        "template literal type",
        "Capitalize",
        "string type",
        "event",
        "route",
        "type-level",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`on${Capitalize<E>}` builds string types from other string types — event names, CSS properties, route paths, all checked.. Event names built by string concatenation at runtime are opaque to the checker, so a listener for a name nobody emits is silent. `String` concatenation for event names.. Template literal types compose string unions.. Great for event names, CSS, and routes.. Keep the unions small enough to read.. `String` concatenation for event names.. Template literal unions forbid `unknown` actions.. function eventName(entity, action) { return entity + \":\" + action; }. type Entity = \"user\" | \"order\"; type Action = \"created\" | \"updated\"; type EventName = `${Entity}:${Action}`; const ok: EventName = \"user:created\"; const bad: EventName = \"user:deleted\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`on${Capitalize<E>}` builds string types from other string types — event names, CSS properties, route paths, all checked.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Event names built by string concatenation at runtime are opaque to the checker, so a listener for a name nobody emits is silent. `String` concatenation for event names.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "testing-declaration-files",
      "title": "Testing a package's public types",
      "category": "Testing with types",
      "tab": "advanced",
      "route": "/lessons/testing/testing-declaration-files",
      "icon": "ph-test-tube",
      "keywords": [
        "d.ts",
        "public api",
        "@arethetypeswrong",
        "resolution",
        "package",
        "testing",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Checking the emitted .d.ts rather than the source: resolution under each module mode, and what consumers actually see.. The types you tested are the source's; the types you shipped are the emitted ones, and they are not always the same. Without .d.ts, consumers only have runtime discovery.. Run type tests against packed output or emitted .d.ts, not only src/. @arethetypeswrong checks export maps across module resolutions.. Breakages in declaration emit (cannot be named) show up here first.. Without .d.ts, consumers only have runtime discovery.. Tests against the public `declare` surface catch return-type drift.. export `declare` function version(): string and assign the result to a string.. What should consumer type tests import?. // JS: there is no declaration file to test — the .js is the API. export function api() { return 1; }. // Consumer view of an emitted declaration surface: export declare function api(): number; const n: string = api();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Checking the emitted .d.ts rather than the source: resolution under each module mode, and what consumers actually see.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The types you tested are the source's; the types you shipped are the emitted ones, and they are not always the same. Without .d.ts, consumers only have runtime discovery.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "testing-error-paths",
      "title": "Testing the failure cases",
      "category": "Testing with types",
      "tab": "intermediate",
      "route": "/lessons/testing/testing-error-paths",
      "icon": "ph-test-tube",
      "keywords": [
        "error",
        "Result",
        "rejects",
        "throws",
        "unknown",
        "catch",
        "testing",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Asserting on a typed error or a Result variant, and why catching in a test needs the same `unknown` handling as production code.. expect(fn).toThrow() passes for the wrong error, and a caught value in a test is `unknown` just as it is everywhere else. Reading .code on `unknown` catch values.. Error path tests should narrow `unknown` catches.. Assert on discriminant fields of custom errors.. Do not use `any` to reach .code.. Reading .code on `unknown` catch values.. Narrow in tests too. test404 returns number.. try { await fn(); } catch (e) { expect(e.code).toBe(404); }. class HttpError extends Error { constructor(readonly code: number) { super(\"http\"); } } declare function fn(): Promise<never>; export async function test404(): Promise<number> { try { await fn(); } catch (e) { if (e instanceof HttpError) return e.code; throw e; } } const bad: string = await test404();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Asserting on a typed error or a Result variant, and why catching in a test needs the same `unknown` handling as production code.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "expect(fn).toThrow() passes for the wrong error, and a caught value in a test is `unknown` just as it is everywhere else. Reading .code on `unknown` catch values.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "testing-generic-functions",
      "title": "Testing generic code",
      "category": "Testing with types",
      "tab": "intermediate",
      "route": "/lessons/testing/testing-generic-functions",
      "icon": "ph-test-tube",
      "keywords": [
        "generic",
        "instantiation",
        "constraint",
        "coverage",
        "test",
        "testing",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A generic function is not one function. Choosing the instantiations worth testing, and testing the constraint as well as the behaviour.. Testing a generic helper with one type argument tests one instantiation and tells you nothing about the rest. Only runtime equality checked. Test generics at the type level as well as runtime.. Test generics at the type level as well as runtime.. Helpers like Expect/Equal catch inference regressions.. Keep type tests in `.ts` files checked by `tsc`.. Only runtime equality checked.. Type-level Equal `asserts` inference. false is not true.. expect(identity(1)).toBe(1);. function identity<T>(x: T): T { return x; } const n: number = identity(1); const s: string = identity(\"a\"); // Compile-time assertion pattern: type Equal<A, B> = (<T>() => T extends A ? 1 : 2) extends <T>() => T extends B ? 1 : 2 ? true : false; type Ok = Equal<ReturnType<typeof identity<number>>, number>; const ok: Ok = true; const bad: Ok = false;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A generic function is not one function. Choosing the instantiations worth testing, and testing the constraint as well as the behaviour.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Testing a generic helper with one type argument tests one instantiation and tells you nothing about the rest. Only runtime equality checked. Test generics at the type level as well as runtime.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "the-strictness-ladder",
      "title": "The strictness ladder",
      "category": "Tooling & the strictness ladder",
      "tab": "beginner",
      "route": "/lessons/tooling/the-strictness-ladder",
      "icon": "ph-wrench",
      "keywords": [
        "strict",
        "noImplicitAny",
        "strictNullChecks",
        "migration",
        "tooling",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Turn on `strict` flags in a sensible order for a brownfield Node app — measure errors, fix, then climb.. Enabling \"`strict`\": true on a million-line repo produces 40k errors and a revert by Friday. Each `strict` flag targets a class of this bug. Suggested order: `noImplicitAny` → `strictNullChecks` → `strictFunctionTypes` → full `strict`.. Suggested order: `noImplicitAny` → `strictNullChecks` → `strictFunctionTypes` → full `strict`.. Track error counts per flag in CI so progress is visible.. Never \"fix\" a flag by sprinkling `any` — that climbs down the ladder.. Each `strict` flag targets a class of this bug.. `strictNullChecks` is usually the highest-value early climb.. Accept `undefined` safely.. Why climb one flag at a time?. function len(s) { return s.length; } len(undefined);. function len(s: string): number { return s.length; } len(undefined);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Turn on `strict` flags in a sensible order for a brownfield Node app — measure errors, fix, then climb.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Enabling \"`strict`\": true on a million-line repo produces 40k errors and a revert by Friday. Each `strict` flag targets a class of this bug. Suggested order: `noImplicitAny` → `strictNullChecks` → `strictFunctionTypes` → full `strict`.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "this-parameter-typing",
      "title": "Typing this",
      "category": "Functions & generics",
      "tab": "intermediate",
      "route": "/lessons/functions/this-parameter-typing",
      "icon": "ph-function",
      "keywords": [
        "this",
        "noImplicitThis",
        "receiver",
        "bind",
        "arrow function",
        "functions",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The fake first parameter that types the receiver, why arrow functions do not have one, and what `noImplicitThis` is protecting you from.. A method pulled off an object and passed as a callback loses its receiver, and JavaScript reports it as `undefined` is not a function. Relying on dynamic this without binding.. The this parameter is erased and only types the receiver.. Methods on objects usually get this from the containing type.. Prefer arrows or explicit args when this is confusing.. Relying on dynamic this without binding.. this: Entity requires a proper receiver. Bare call errors.. function label() { return this.id; } label(); // boom. type Entity = { id: string }; function label(this: Entity): string { return this.id; } const e: Entity = { id: \"1\" }; label.call(e); label();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The fake first parameter that types the receiver, why arrow functions do not have one, and what `noImplicitThis` is protecting you from.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A method pulled off an object and passed as a callback loses its receiver, and JavaScript reports it as `undefined` is not a function. Relying on dynamic this without binding.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "trusting-your-own-database",
      "title": "Rows are untrusted too",
      "category": "The runtime boundary",
      "tab": "advanced",
      "route": "/lessons/runtime-boundary/trusting-your-own-database",
      "icon": "ph-shield-check",
      "keywords": [
        "database",
        "sql",
        "rows",
        "prisma",
        "kysely",
        "nullable",
        "runtime-boundary",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Query results are `any` in most drivers. Schema-derived row types, nullable columns, and why the database is a boundary like any other.. A migration made a column nullable and every read site still believes it cannot be `null`. Nullable columns are runtime `null` with no warning in JS. Treat query results like untrusted input: types should match the live schema, including nulls.. Treat query results like untrusted input: types should match the live schema, including nulls.. Regenerate row types in CI when migrations land.. ORM client types help only if they track schema; raw SQL needs your own row types.. Nullable columns are runtime `null` with no warning in JS.. string | `null` must be narrowed before string methods.. Type Row = { name: string | `null` } and write label(row) returning name or \"anonymous\".. After a column becomes nullable, what should happen to TypeScript?. // JS: drivers return plain objects — nulls surprise you in production. function getUser(row) { return row.email.toLowerCase(); }. // Schema-derived row — email became nullable in a migration. type UserRow = { id: string; email: string | null }; function emailDomain(row: UserRow): string { return row.email.split(\"@\")[1] ?? \"\"; } function emailDomainSafe(row: UserRow): string { if (row.email === null) return \"\"; return row.email.split(\"@\")[1] ?? \"\"; } void emailDomainSafe;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Query results are `any` in most drivers. Schema-derived row types, nullable columns, and why the database is a boundary like any other.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A migration made a column nullable and every read site still believes it cannot be `null`. Nullable columns are runtime `null` with no warning in JS. Treat query results like untrusted input: types should match the live schema, including nulls.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "truthiness-narrowing",
      "title": "Truthiness narrowing",
      "category": "Types & narrowing",
      "tab": "beginner",
      "route": "/lessons/types/truthiness-narrowing",
      "icon": "ph-shapes",
      "keywords": [
        "truthiness",
        "falsy",
        "narrowing",
        "if",
        "types",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "if (value) removes `null`, `undefined`, 0, NaN, and the empty string from a type. That is usually more than you meant.. if (count) skips the branch when count is 0, which is the one case the code was written to handle. No TypeScript error here — that's the point: truthiness treats 0 as empty. Prefer != `null` when 0 is meaningful.. Truthiness narrowing removes all falsy values, not just nullish ones.. For numbers and strings that may be 0 or \"\", check == `null` or === `undefined` explicitly.. if (value) is fine for objects and nullable references when empty is not a value you care about.. 0 is falsy — a valid count becomes \"empty\".. No TypeScript error here — that's the point: truthiness treats 0 as empty. Prefer != `null` when 0 is meaningful.. Change the guard so 0 returns \"count=0\".. Which check keeps 0 but rejects `undefined`?. function label(count) { if (count) return \"count=\" + count; return \"empty\"; } label(0);. function label(count: number | undefined): string { // Truthiness narrows away 0 as well as undefined. if (count) { return \"count=\" + count.toFixed(0); } return \"empty\"; } // This call is fine to the checker — and wrong for product behavior. export const shown = label(0);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "if (value) removes `null`, `undefined`, 0, NaN, and the empty string from a type. That is usually more than you meant.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "if (count) skips the branch when count is 0, which is the one case the code was written to handle. No TypeScript error here — that's the point: truthiness treats 0 as empty. Prefer != `null` when 0 is meaningful.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "ts-expect-error-as-an-assertion",
      "title": "Asserting that code does not compile",
      "category": "Testing with types",
      "tab": "advanced",
      "route": "/lessons/testing/ts-expect-error-as-an-assertion",
      "icon": "ph-test-tube",
      "keywords": [
        "ts-expect-error",
        "negative test",
        "compile error",
        "assertion",
        "api",
        "testing",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "@ts-expect-error fails when the error disappears, which makes it the only built-in way to test that an invalid call is still rejected.. Nothing stops a type from getting looser, so the invalid usage your API deliberately rejects starts compiling and no test notices. Negative type tests have no JS equivalent.. @ts-expect-error is better than @ts-ignore for tests — unused expect-errors fail.. Put negative tests next to the API so loosenings break CI.. Do not use expect-error to silence real bugs in production code paths.. Negative type tests have no JS equivalent.. @ts-expect-error documents intentional failures; returns stay number.. Write onlyNumber(n: number) and a @ts-expect-error call with a string, plus a valid call.. What happens if @ts-expect-error sits on a line with no error?. // JS cannot assert “this call is illegal” — everything is legal. function takeNumber(n) { return n + 1; } takeNumber(\"nope\");. function takeNumber(n: number): number { return n + 1; } // Negative test: this line must stay an error. // @ts-expect-error string is not a number takeNumber(\"nope\"); // If the API loosens to accept string, @ts-expect-error becomes unused (TS2578). const ok = takeNumber(1); const bad: string = ok;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "@ts-expect-error fails when the error disappears, which makes it the only built-in way to test that an invalid call is still rejected.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Nothing stops a type from getting looser, so the invalid usage your API deliberately rejects starts compiling and no test notices. Negative type tests have no JS equivalent.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "tsc-cli",
      "title": "The tsc command line",
      "category": "Tooling & the strictness ladder",
      "tab": "beginner",
      "route": "/lessons/tooling/tsc-cli",
      "icon": "ph-wrench",
      "keywords": [
        "tsc",
        "CLI",
        "noEmit",
        "CI",
        "tooling",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`tsc` --`noEmit` for CI typechecking, `tsc` for emit, and why the flags you pass must match the `tsconfig` the editor uses.. CI runs `tsc` with different flags than developers, so the same PR is green locally and red in pipeline. Without `tsc` in CI, string concatenation ships. Use `tsc` --`noEmit` (or -b --pretty false) as the CI type gate.. Use `tsc` --`noEmit` (or -b --pretty false) as the CI type gate.. Prefer project flags via `tsconfig` over long ad-hoc CLI lists.. Pin TypeScript so local, editor, and CI agree — this site uses typescript@7 for the CLI and typescript-strada@6.0.3 where createProgram is required.. Without `tsc` in CI, string concatenation ships.. What `tsc -p . --noEmit` should fail on in CI.. Call add with two numbers.. What does --`noEmit` do?. // \"It works when I run node\" is not a typecheck. function add(a, b) { return a + b; } add(1, \"2\");. function add(a: number, b: number): number { return a + b; } add(1, \"2\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`tsc` --`noEmit` for CI typechecking, `tsc` for emit, and why the flags you pass must match the `tsconfig` the editor uses.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "CI runs `tsc` with different flags than developers, so the same PR is green locally and red in pipeline. Without `tsc` in CI, string concatenation ships. Use `tsc` --`noEmit` (or -b --pretty false) as the CI type gate.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "tsc-compiler-pipeline",
      "title": "What tsc actually does",
      "category": "Foundations",
      "tab": "advanced",
      "route": "/lessons/foundations/tsc-compiler-pipeline",
      "icon": "ph-compass",
      "keywords": [
        "compiler",
        "binder",
        "checker",
        "program",
        "AST",
        "compiler api",
        "foundations",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Scanner, parser, binder, checker, emitter — and where a Program, a SourceFile, and a TypeChecker sit. This site runs Strada createProgram in the diagnostic generator and CI, not in the browser.. People confuse 'it bundled' with 'it type-checked.' Transpile-only pipelines can emit JS from code that would fail `tsc`. The failure mode is a green deploy of red types.. `Program` owns `SourceFile`s; `TypeChecker` answers type questions after bind.. Errors can originate in parse, bind, or check — the code number hints which family.. Emit is optional: `noEmit` / transpile-only tools skip or replace the checker.. Transpile-only pipelines skip the checker TypeScript spends time in.. Checker diagnostics exist without emit — generator/CI mode uses `noEmit`.. Define type Phase = \"parse\" | \"check\" and a function that accepts only \"check\".. Which stage assigns symbols and builds the scope graph?. // JS tooling often \"compiles\" by parsing+emitting only (esbuild/swc). // That pipeline has no checker stage — types are deleted, not proven. function add(a, b) { return a + b; }. // Mental model of stages (illustrative — not the real API surface): type Stage = | \"scan\" | \"parse\" | \"bind\" | \"check\" | \"emit\"; const pipeline: Stage[] = [\"scan\", \"parse\", \"bind\", \"check\", \"emit\"]; // Build-time Strada createProgram stops before emit: noEmit + diagnostics only. function typecheckOnly(stages: Stage[]): Stage[] { return stages.filter((s) => s !== \"emit\"); } const generatorStages …",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Scanner, parser, binder, checker, emitter — and where a Program, a SourceFile, and a TypeChecker sit. This site runs Strada createProgram in the diagnostic generator and CI, not in the browser.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "People confuse 'it bundled' with 'it type-checked.' Transpile-only pipelines can emit JS from code that would fail `tsc`. The failure mode is a green deploy of red types.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "tsconfig-essentials",
      "title": "tsconfig.json: the options that matter first",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/tsconfig-essentials",
      "icon": "ph-compass",
      "keywords": [
        "tsconfig",
        "target",
        "module",
        "lib",
        "outDir",
        "configuration",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A `tsconfig` for a Node service, option by option: `target`, `module`, `moduleResolution`, `lib`, `outDir`, `rootDir`, `strict`, and `skipLibCheck`.. Copying a `tsconfig` from a blog post produces errors that make no sense, because half its options were written for a different runtime. Module format is a runtime/config concern; types inherit the mess.. For Node today: `module`/`moduleResolution` bundler or nodenext, `target` es2022+, `strict` true.. `lib` should match your runtime — do not pull `dom` into a pure Node service.. `skipLibCheck` speeds builds by skipping `.d.ts` checking; it does not fix your code.. Module format is a runtime/config concern; types inherit the mess.. Wrong module assumptions surface as 'Cannot find name require' — fix config, don't cast.. Replace require with a local typed stub so the file type-checks under ESM.. Why avoid `\"lib\": [\"dom\"]` in a Node API service?. // package.json \"type\": \"module\" but code still uses require — // config and runtime disagree long before types enter the picture. const fs = require(\"fs\"); module.exports = { read: (p) => fs.readFileSync(p, \"utf8\") };. // This site's checker uses ESNext modules. CommonJS require is not defined. const fs = require(\"fs\"); export function read(path: string): string { return fs.readFileSync(path, \"utf8\"); }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A `tsconfig` for a Node service, option by option: `target`, `module`, `moduleResolution`, `lib`, `outDir`, `rootDir`, `strict`, and `skipLibCheck`.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Copying a `tsconfig` from a blog post produces errors that make no sense, because half its options were written for a different runtime. Module format is a runtime/config concern; types inherit the mess.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "tsserver-and-your-editor",
      "title": "tsserver and your editor",
      "category": "Tooling & the strictness ladder",
      "tab": "beginner",
      "route": "/lessons/tooling/tsserver-and-your-editor",
      "icon": "ph-wrench",
      "keywords": [
        "tsserver",
        "language service",
        "workspace TypeScript",
        "tooling",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The language service is a long-lived `tsc` — select the workspace TypeScript version so squiggles match CI.. The editor uses a global TypeScript while CI uses the workspace pin (here typescript@7, with Strada 6 for tools that still need createProgram) — errors appear in only one place. Without a project version, every machine invents its own truth.. In VS Code/Cursor: \"TypeScript: Select Workspace Version\".. Restart `tsserver` after `tsconfig` changes if diagnostics look stale.. Huge monorepos may need project references so `tsserver` stays responsive.. Without a project version, every machine invents its own truth.. Same checker as CI once the workspace TS version is selected.. Assign pick's result to a number.. Why select the workspace TypeScript version?. function pick(arr) { return arr[0]; }. function pick(arr: number[]): number { return arr[0]; } const n: string = pick([1, 2, 3]);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The language service is a long-lived `tsc` — select the workspace TypeScript version so squiggles match CI.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The editor uses a global TypeScript while CI uses the workspace pin (here typescript@7, with Strada 6 for tools that still need createProgram) — errors appear in only one place. Without a project version, every machine invents its own truth.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "type-aliases-intro",
      "title": "Type aliases",
      "category": "Objects, classes & modules",
      "tab": "beginner",
      "route": "/lessons/structures/type-aliases-intro",
      "icon": "ph-cube",
      "keywords": [
        "type alias",
        "type",
        "union alias",
        "structures",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "type Name = ... gives `any` type a reusable name — objects, unions, tuples, and function types alike.. The same User | `null` union is copy-pasted through ten files until one of them forgets the `null`. Without a shared alias, shape drift is invisible. Type can name unions, intersections, primitives, and tuples — interface cannot.. type can name unions, intersections, primitives, and tuples — interface cannot.. Aliases are transparent: UserOrAnon is exactly User | `null`.. Prefer one exported alias over repeating the same union inline.. Without a shared alias, shape drift is invisible.. Aliases keep the union and the object shape in one place.. Pass a string name.. Which can a type alias express that interface cannot?. function nameOf(user) { return user ? user.name : \"anonymous\"; } nameOf({ name: 42 });. type User = { name: string }; type UserOrAnon = User | null; function nameOf(user: UserOrAnon): string { return user ? user.name : \"anonymous\"; } nameOf({ name: 42 });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "type Name = ... gives `any` type a reusable name — objects, unions, tuples, and function types alike.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The same User | `null` union is copy-pasted through ten files until one of them forgets the `null`. Without a shared alias, shape drift is invisible. Type can name unions, intersections, primitives, and tuples — interface cannot.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "type-assertions-are-claims",
      "title": "as is a claim, not a check",
      "category": "The runtime boundary",
      "tab": "beginner",
      "route": "/lessons/runtime-boundary/type-assertions-are-claims",
      "icon": "ph-shield-check",
      "keywords": [
        "assertion",
        "as",
        "angle-bracket",
        "cast",
        "runtime-boundary",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Type assertions tell the checker to trust you. They do not convert values and they do not validate shapes.. Developers 'fix' a type error with as Type and ship a value that never matched Type. A mental cast does nothing at runtime. As Type is a compile-time claim; prefer narrowing that the checker can verify.. as Type is a compile-time claim; prefer narrowing that the checker can verify.. Double assertions (as `unknown` as T) are a smell — usually a wrong model.. Use assertions for DOM/legacy interop after you have a real reason, not to silence errors.. A mental cast does nothing at runtime.. No TypeScript error here — that's the point: the assertion skipped the check. Compiles. Crashes.. Return a real User or throw instead of asserting `null`.. Does `value as User` validate fields at runtime?. function asUser(value) { return value; // \"trust me\" } asUser(null).email;. type User = { email: string }; function load(): User { const raw: unknown = null; return raw as User; } load().email.toLowerCase();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Type assertions tell the checker to trust you. They do not convert values and they do not validate shapes.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Developers 'fix' a type error with as Type and ship a value that never matched Type. A mental cast does nothing at runtime. As Type is a compile-time claim; prefer narrowing that the checker can verify.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "type-checking-performance",
      "title": "When the checker gets slow",
      "category": "Tooling & the strictness ladder",
      "tab": "advanced",
      "route": "/lessons/tooling/type-checking-performance",
      "icon": "ph-wrench",
      "keywords": [
        "performance",
        "generateTrace",
        "diagnostics",
        "slow",
        "profiling",
        "tooling",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "--diagnostics, --generateTrace, and the analyser: finding the type, the file, or the dependency that is costing you thirty seconds.. Compile times grow gradually until the editor is unusable, and by then nobody knows which change caused it. Checker cost is invisible until the editor lags. Start with `tsc` --diagnostics / --extendedDiagnostics for counts and timings.. Start with `tsc` --diagnostics / --extendedDiagnostics for counts and timings.. generateTrace + @typescript/analyze-trace finds hot files and types.. Fix shared package types and giant unions before buying bigger CI machines.. Checker cost is invisible until the editor lags.. Heavy slowdowns call for generateTrace, not vibes.. Type Metric = { checkTimeMs: number } and a function slower(a, b) comparing checkTimeMs.. Which flag produces a trace for the analyser?. // JS build slowness is usually transform/bundle — profile with different tools. // TypeScript slowness is often checker instantiation, not emit.. type TraceHint = \"diagnostics\" | \"generateTrace\" | \"extendedDiagnostics\"; function recommend(slow: boolean): TraceHint { return slow ? \"generateTrace\" : \"diagnostics\"; } const hint = recommend(true); const wrong: \"diagnostics\" = recommend(true); void hint;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "--diagnostics, --generateTrace, and the analyser: finding the type, the file, or the dependency that is costing you thirty seconds.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Compile times grow gradually until the editor is unusable, and by then nobody knows which change caused it. Checker cost is invisible until the editor lags. Start with `tsc` --diagnostics / --extendedDiagnostics for counts and timings.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "type-level-assertions-and-equality",
      "title": "Comparing two types",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/type-level-assertions-and-equality",
      "icon": "ph-function",
      "keywords": [
        "Equal",
        "type assertion",
        "identity",
        "Expect",
        "type test",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Why Equal<A, B> is surprisingly hard, what the conditional-identity trick actually tests, and how to build assertions you can trust.. Mutual assignability is not equality, so the obvious type-equality check quietly passes for `any` and `never`. Runtime deep-equal cannot assert compile-time type identity.. A extends B and B extends A is assignability, not equality — `any` and `never` break it.. The `<T>() => T extends X ? 1 : 2` trick compares how X behaves under inference.. Use Expect<Equal<A, B>> in type-level tests; treat failures as red builds.. Runtime deep-equal cannot assert compile-time type identity.. The identity trick rejects `any`/`never` false friends.. Using Equal and Expect from the lesson pattern, assert that string | number equals number | string (order should not matter for this Equal).. Why is `any extends number ? (number extends any ? true : false) : false` true?. // JS: \"same shape?\" is JSON.stringify or a hand-rolled deepEqual. function sameShape(a, b) { return JSON.stringify(a) === JSON.stringify(b); } // Types do not exist at runtime — equality of types is a compile-time problem.. type Equal<A, B> = (<T>() => T extends A ? 1 : 2) extends <T>() => T extends B ? 1 : 2 ? true : false; type Expect<T extends true> = T; type _ok = Expect<Equal<{ a: 1 }, { a: 1 }>>; // Equal rejects any === number; Expect fails when the answer is false. type _fail = Expect<Equal<any, number>>;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Why Equal<A, B> is surprisingly hard, what the conditional-identity trick actually tests, and how to build assertions you can trust.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Mutual assignability is not equality, so the obvious type-equality check quietly passes for `any` and `never`. Runtime deep-equal cannot assert compile-time type identity.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "type-level-performance",
      "title": "The cost of clever types",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/type-level-performance",
      "icon": "ph-function",
      "keywords": [
        "performance",
        "instantiation",
        "trace",
        "compile time",
        "recursion",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Instantiation counts, caching, and why one recursive helper can add seconds to every build in the repository that imports it.. A clever type in a shared package makes the editor lag in every consumer, and the cost is invisible where it was written. Unchecked recursion is expensive in values and in types.. Every conditional/mapped expansion is work; shared packages multiply that work by consumer count.. Prefer interfaces for object shapes when possible — they cache better than complex aliases.. Profile with --generateTrace / --extendedDiagnostics before “optimizing” by guesswork.. Unchecked recursion is expensive in values and in types.. Deep key unions grow fast; bad keys still cost expansion.. Replace a deep recursive Keys helper usage with a simple `keyof` for a flat config type. Export type FlatKeys = `keyof` Config.. Why is a heavy type in a shared package worse than the same type in an app-local file?. // JS analogy: a recursive utility with no memoization. function deepKeys(obj, path = \"\") { return Object.keys(obj).flatMap((k) => { const p = path ? path + \".\" + k : k; const v = obj[k]; return v && typeof v === \"object\" ? deepKeys(v, p) : [p]; }); } // Fine for small objects; catastrophic for huge trees — types have the same shape.. // Illustrative: a deep key walker — prefer simpler exported APIs. type DeepKeys<T> = T extends object ? { [K in keyof T & string]: | K | `${K}.${DeepKeys<T[K]>}`; }[keyof T & string] : never; type Huge = { a: { b: { c: { d: { e: string } } } } }; type Keys = DeepKeys<Huge>; // Accidental blow-up: assigning a wrong key still type-checks the whole tree. const k: Keys = \"a.b.x\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Instantiation counts, caching, and why one recursive helper can add seconds to every build in the repository that imports it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A clever type in a shared package makes the editor lag in every consumer, and the cost is invisible where it was written. Unchecked recursion is expensive in values and in types.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "type-level-tests",
      "title": "Testing types",
      "category": "Testing with types",
      "tab": "advanced",
      "route": "/lessons/testing/type-level-tests",
      "icon": "ph-test-tube",
      "keywords": [
        "expectTypeOf",
        "tsd",
        "Equal",
        "type test",
        "assertType",
        "testing",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "expectTypeOf, tsd, and hand-rolled Expect<Equal<A, B>> — asserting that a signature is what you think it is, as a first-class test.. A refactor that changes an exported type breaks nothing in the test suite, because no test ever looked at a type. Runtime tests cannot see TypeScript signatures.. Colocate type tests with the API they protect — they are regression tests.. Libraries like expectTypeOf integrate with Vitest; Equal/Expect work without deps.. Test the public exported types, not incidental inference inside implementations.. Runtime tests cannot see TypeScript signatures.. Type-level Expect<Equal<…>> fails the build when signatures drift.. Assert `ReturnType` of identity<T>(x: T): T is number when called as identity(1) via `typeof` on a const.. What does Expect<Equal<A, B>> do when A and B differ?. // JS tests assert runtime values — signatures are undocumented. function add(a, b) { return a + b; }. type Equal<A, B> = (<T>() => T extends A ? 1 : 2) extends <T>() => T extends B ? 1 : 2 ? true : false; type Expect<T extends true> = T; function add(a: number, b: number): number { return a + b; } type _ret = Expect<Equal<ReturnType<typeof add>, number>>; type _fail = Expect<Equal<ReturnType<typeof add>, string>>;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "expectTypeOf, tsd, and hand-rolled Expect<Equal<A, B>> — asserting that a signature is what you think it is, as a first-class test.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A refactor that changes an exported type breaks nothing in the test suite, because no test ever looked at a type. Runtime tests cannot see TypeScript signatures.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "type-only-vs-value-imports",
      "title": "Type imports and what ends up in the bundle",
      "category": "Tooling & the strictness ladder",
      "tab": "intermediate",
      "route": "/lessons/tooling/type-only-vs-value-imports",
      "icon": "ph-wrench",
      "keywords": [
        "import type",
        "side effect",
        "elision",
        "bundle",
        "tree shaking",
        "tooling",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "import type, inline type specifiers, side-effect imports, and importsNotUsedAsValues' successor — controlling exactly what survives compilation.. An import used only as a type is usually erased, and the one time it is not, a server module ends up in a client bundle. Importing a type as if it were a runtime value.. import type / export type erase at emit.. Mixing type and value from one module is fine with inline type modifiers.. `verbatimModuleSyntax` makes mistakes loud.. Importing a type as if it were a runtime value.. import type is erased. User.id is string.. Why prefer `import type` for type-only symbols?. import { User } from './user';. type User = { id: string }; declare function load(): User; import type { User as UserType } from \"./types\"; const u: User = load(); const bad: number = u.id;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "import type, inline type specifiers, side-effect imports, and importsNotUsedAsValues' successor — controlling exactly what survives compilation.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "An import used only as a type is usually erased, and the one time it is not, a server module ends up in a client bundle. Importing a type as if it were a runtime value.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "type-safe-serialization",
      "title": "The round trip",
      "category": "The runtime boundary",
      "tab": "advanced",
      "route": "/lessons/runtime-boundary/type-safe-serialization",
      "icon": "ph-shield-check",
      "keywords": [
        "serialization",
        "JSON",
        "Date",
        "round trip",
        "Jsonify",
        "runtime-boundary",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Date, Map, Set, BigInt, and `undefined` do not survive JSON. Typing what comes back so it matches what is actually there.. A field typed Date is a string by the time it reaches the client, and the type says otherwise on both sides. The wire shape is not the in-memory shape. Model wire types separately from domain types when JSON is involved.. Model wire types separately from domain types when JSON is involved.. `undefined` keys disappear; Date becomes string; Map/Set become objects/arrays or fail.. Revivers and custom serializers must stay in sync with Jsonify-like types.. The wire shape is not the in-memory shape.. Jsonify maps Date to string — assigning a Date fails.. Define WireUser = { id: string; createdAt: string } matching a Jsonify of { id: string; createdAt: Date }.. After `JSON.stringify`/parse, what is a Date field?. // JS: JSON.stringify drops undefined and turns Date into a string. const payload = { at: new Date(), note: undefined }; JSON.parse(JSON.stringify(payload)); // { at: \"2020-…\" } — note gone. type Jsonify<T> = T extends Date ? string : T extends Array<infer U> ? Jsonify<U>[] : T extends object ? { [K in keyof T]: Jsonify<T[K]> } : T; type Event = { at: Date; name: string }; type WireEvent = Jsonify<Event>; // { at: string; name: string } const wire: WireEvent = { at: new Date().toISOString(), name: \"x\" }; const bad: WireEvent = { at: new Date(), name: \"x\" };",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Date, Map, Set, BigInt, and `undefined` do not survive JSON. Typing what comes back so it matches what is actually there.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A field typed Date is a string by the time it reaches the client, and the type says otherwise on both sides. The wire shape is not the in-memory shape. Model wire types separately from domain types when JSON is involved.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "type-space-vs-value-space",
      "title": "Type space against value space",
      "category": "Foundations",
      "tab": "intermediate",
      "route": "/lessons/foundations/type-space-vs-value-space",
      "icon": "ph-compass",
      "keywords": [
        "type space",
        "value space",
        "typeof",
        "declaration merging",
        "namespace",
        "foundations",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The same identifier can name a value, a type, or both. Which declarations create which, and how `typeof` and `keyof` cross between the two worlds.. The same name can mean a type and a value (`Null` vs `null`, class shapes, `typeof` confusion). Mixing the spaces produces errors that look mystical until you ask which world a name lives in.. type/interface live in type space; const/function in value space.. `typeof` bridges value to type.. Confusion here causes 'used as a value' errors.. One name, no type/value distinction.. Types and values inhabit different spaces; same name can coexist. id is string.. const User = { id: 1 };. type User = { id: string }; const User = { id: \"1\" }; const u: User = User; const bad: number = u.id;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The same identifier can name a value, a type, or both. Which declarations create which, and how `typeof` and `keyof` cross between the two worlds.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The same name can mean a type and a value (`Null` vs `null`, class shapes, `typeof` confusion). Mixing the spaces produces errors that look mystical until you ask which world a name lives in.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "type-widening-and-freshness",
      "title": "Widening and freshness",
      "category": "Types & narrowing",
      "tab": "intermediate",
      "route": "/lessons/types/type-widening-and-freshness",
      "icon": "ph-shapes",
      "keywords": [
        "widening",
        "freshness",
        "literal type",
        "excess property",
        "inference",
        "types",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Why the same literal has one type in a const, another in a let, and a third when passed directly to a parameter — and how to control which you get.. Extracting an object literal into a variable turns a compile error into silence, or silence into a compile error, with no other change. mode silently becomes a general string.. let often widens literals to string; const preserves them.. Fresh object literals get excess property checks; variables do not.. Annotate let when you need a literal union.. mode silently becomes a general string.. string is not assignable to 'read' | 'write'.. Why does a `let` string often refuse a literal-parameter API that a `const` literal accepts?. let mode = \"read\"; setMode(mode);. declare function setMode(m: \"read\" | \"write\"): void; let mode = \"read\"; setMode(mode); const modeConst = \"read\"; setMode(modeConst); let widened: string = \"read\"; setMode(widened);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Why the same literal has one type in a const, another in a let, and a third when passed directly to a parameter — and how to control which you get.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Extracting an object literal into a variable turns a compile error into silence, or silence into a compile error, with no other change. mode silently becomes a general string.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typed-error-channels",
      "title": "Typed error channels",
      "category": "Async, errors & Result",
      "tab": "advanced",
      "route": "/lessons/async/typed-error-channels",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "Effect",
        "neverthrow",
        "typed errors",
        "error channel",
        "interop",
        "async",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Effect, neverthrow, and friends put the error type in the signature. What that buys, what it costs in interop, and how to decide.. A typed-error library is all-or-nothing at a boundary, and half-adopted it produces two error models in one codebase. Thrown errors have no channel in the type system.. Typed error channels make failure part of the signature — callers must handle or propagate E.. Interop cost: thrown exceptions and Result styles do not mix cleanly at boundaries.. Adopt at module borders first; avoid dual styles inside one feature.. Thrown errors have no channel in the type system.. Result puts errors in the return type — success fields stay narrow.. Define Result<T, E> and a function ok(value: T): Result<T, never>.. Main benefit of Result<T, E> over throw?. // JS: errors are thrown values — catch whatever. async function load() { throw new Error(\"network\"); } try { await load(); } catch (e) { console.log(e.message); }. type Result<T, E> = | { ok: true; value: T } | { ok: false; error: E }; type NetErr = { kind: \"network\"; cause: string }; async function load(): Promise<Result<{ id: string }, NetErr>> { return { ok: false, error: { kind: \"network\", cause: \"timeout\" } }; } const r = await load(); if (!r.ok) { const k: \"network\" = r.error.kind; void k; } else { const n: number = r.value.id; void n; }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Effect, neverthrow, and friends put the error type in the signature. What that buys, what it costs in interop, and how to decide.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A typed-error library is all-or-nothing at a boundary, and half-adopted it produces two error models in one codebase. Thrown errors have no channel in the type system.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typed-event-emitters",
      "title": "Typed event emitters",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/typed-event-emitters",
      "icon": "ph-hard-drives",
      "keywords": [
        "EventEmitter",
        "events",
        "typed events",
        "on",
        "emit",
        "generic",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "EventEmitter's untyped on and emit, generic emitter maps, and the typed-events option that arrived in newer Node types.. Event emitters accept any string event name and any payload, so typos and wrong shapes compile until a listener mishandles a message at runtime. Untyped `on` / `emit` pairs are a protocol with no checker.. Map event names to payload types so emit/on stay in sync.. Wrap EventEmitter or use a typed emitter helper.. Remove listeners with `AbortSignal` to avoid leaks.. Emitting a partial payload while listeners assume email exists.. Typed emit requires a full User — missing email is an error.. emitter.on(\"user\", (u) => { send(u.email); }); emitter.emit(\"user\", { id: 1 });. type User = { id: string; email: string }; interface TypedEmitter { on(event: \"user\", listener: (user: User) => void): void; emit(event: \"user\", user: User): boolean; } declare const emitter: TypedEmitter; emitter.on(\"user\", (u) => { const email: string = u.email; }); emitter.emit(\"user\", { id: \"1\" });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "EventEmitter's untyped on and emit, generic emitter maps, and the typed-events option that arrived in newer Node types.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Event emitters accept any string event name and any payload, so typos and wrong shapes compile until a listener mishandles a message at runtime. Untyped `on` / `emit` pairs are a protocol with no checker.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typed-fixtures-and-factories",
      "title": "Fixtures that cannot drift",
      "category": "Testing with types",
      "tab": "beginner",
      "route": "/lessons/testing/typed-fixtures-and-factories",
      "icon": "ph-test-tube",
      "keywords": [
        "factory",
        "fixture",
        "satisfies",
        "test data",
        "testing",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Factory functions returning Satisfies/typed objects keep fixtures aligned with production types as fields evolve.. A shared fixture object is missing a new required field; half the suite still passes with partial data. Overrides are unchecked; email becomes a number. Type factories as returning the production type, not a looser blob.. Type factories as returning the production type, not a looser blob.. `Partial<T>` is ideal for overrides — values remain checked.. `satisfies` User on literal fixtures also catches missing fields without widening.. Overrides are unchecked; email becomes a number.. `Partial<User>` still requires override values to match field types.. Override email with a string.. What does `Partial<User>` mean for overrides?. function userFixture(overrides) { return { id: \"1\", email: \"a@b.co\", ...overrides }; } userFixture({ email: 1 });. type User = { id: string; email: string }; function userFixture(overrides: Partial<User> = {}): User { return { id: \"1\", email: \"a@b.co\", ...overrides }; } userFixture({ email: 1 });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Factory functions returning Satisfies/typed objects keep fixtures aligned with production types as fields evolve.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A shared fixture object is missing a new required field; half the suite still passes with partial data. Overrides are unchecked; email becomes a number. Type factories as returning the production type, not a looser blob.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typeof-type-queries",
      "title": "typeof in type position",
      "category": "Type-level programming",
      "tab": "intermediate",
      "route": "/lessons/type-level/typeof-type-queries",
      "icon": "ph-function",
      "keywords": [
        "typeof",
        "type query",
        "value to type",
        "inference",
        "config",
        "type-level",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`typeof` config in a type annotation asks for the type of a value. The bridge from the values you already have to the types you need.. The config object and the Config interface are maintained separately, and they diverge on the first hurried commit. Config object with no derived type. `typeof` value queries the type of a value.. `typeof` value queries the type of a value.. Use it to keep config objects as the source of truth.. Combine with `as const` for literal unions.. Config object with no derived type.. `typeof` + `as const` derives literal port type.. const defaults = { host: 'localhost', port: 3000 };. const defaults = { host: \"localhost\", port: 3000 } as const; type Defaults = typeof defaults; type Port = Defaults[\"port\"]; const p: Port = 3000; const bad: Port = \"3000\";",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`typeof` config in a type annotation asks for the type of a value. The bridge from the values you already have to the types you need.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The config object and the Config interface are maintained separately, and they diverge on the first hurried commit. Config object with no derived type. `typeof` value queries the type of a value.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "types-are-erased",
      "title": "Types are erased",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/types-are-erased",
      "icon": "ph-compass",
      "keywords": [
        "erasure",
        "compile time",
        "runtime",
        "emit",
        "no runtime cost",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Every annotation you write disappears before Node sees the file. What that buys you, and the one assumption it stops you from making.. People ship `if (value is string)` style thinking into runtime, or expect interfaces to exist in the bundle. Types are erased: they do not survive compilation, and they cannot protect you after the JS is running.. TypeScript erases types: the emitted JavaScript has no `User`, no annotations, no assertions.. A type annotation documents intent for the checker; it never validates bytes from the network.. At trust boundaries use `unknown` + narrowing (or a schema library) — covered in the runtime-boundary track.. `JSON.parse` returns whatever the wire sent — no shape check.. No TypeScript error here — that's the point: `as User` is a compile-time claim; Node still crashes.. Remove the unsafe assertion. Accept `unknown` and narrow before calling `sendWelcome`.. After `tsc` emits JavaScript, what remains of type `User`?. // \"Validated\" only by hope and a comment. function sendWelcome(user) { return \"welcome \" + user.email.toLowerCase(); } const raw = JSON.parse('{\"id\":\"1\"}'); sendWelcome(raw);. type User = { id: string; email: string }; function sendWelcome(user: User) { return \"welcome \" + user.email.toLowerCase(); } // Compiles. Types are gone at runtime — this still blows up if email is missing. const raw: unknown = JSON.parse('{\"id\":\"1\"}'); sendWelcome(raw as User);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Every annotation you write disappears before Node sees the file. What that buys you, and the one assumption it stops you from making.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "People ship `if (value is string)` style thinking into runtime, or expect interfaces to exist in the bundle. Types are erased: they do not survive compilation, and they cannot protect you after the JS is running.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typescript-versions-and-the-go-port",
      "title": "TypeScript 6, 7, and the Go port",
      "category": "Tooling & the strictness ladder",
      "tab": "advanced",
      "route": "/lessons/tooling/typescript-versions-and-the-go-port",
      "icon": "ph-wrench",
      "keywords": [
        "typescript 7",
        "go",
        "native",
        "compiler api",
        "tsgo",
        "strada",
        "migration",
        "tooling",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "What the native port changes, what it removes — including the in-browser programmatic API — and how this site dual-installs Strada for build-time diagnostics alongside typescript@7.. TypeScript 7 is a native binary with no JavaScript createProgram API yet, so tools that embed the compiler need a dual-install strategy. No JS API means no in-browser `tsc` from the native port.. TypeScript 7 is the primary package here (native CLI); Strada 6 stays as typescript-strada for createProgram.. Microsoft publishes the Strada API line as `@typescript/typescript6`; this site aliases it `typescript-strada` for build-time diagnostics.. Lesson diagnostics are generated at build time — the browser never ships a compiler.. Plan editor/CI native speedups separately from tools that still need the JS Compiler API.. No JS API means no in-browser `tsc` from the native port.. This site uses typescript@7 for tooling CLI and typescript-strada@6 for build-time diagnostics.. Type Cap = { programmaticApi: boolean } and const ts6: Cap = { programmaticApi: true }.. How does ts-school use TypeScript 7 today?. // Native compilers are CLIs — not importable JS libraries. // Embedding tsc in a browser worker needs a JS API that TS 7 does not ship.. type CompilerKind = \"strada-js\" | \"native-go\"; type Capability = { kind: CompilerKind; programmaticApi: boolean; browserWorker: boolean; }; const ts6: Capability = { kind: \"strada-js\", programmaticApi: true, browserWorker: true, }; const ts7: Capability = { kind: \"native-go\", programmaticApi: false, browserWorker: false, }; const canWorker: true = ts7.browserWorker; void ts6;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "What the native port changes, what it removes — including the in-browser programmatic API — and how this site dual-installs Strada for build-time diagnostics alongside typescript@7.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "TypeScript 7 is a native binary with no JavaScript createProgram API yet, so tools that embed the compiler need a dual-install strategy. No JS API means no in-browser `tsc` from the native port.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-callbacks-and-promisify",
      "title": "From callbacks to promises",
      "category": "Async, errors & Result",
      "tab": "beginner",
      "route": "/lessons/async/typing-callbacks-and-promisify",
      "icon": "ph-arrows-clockwise",
      "keywords": [
        "callback",
        "promisify",
        "errback",
        "Node",
        "async",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Node-style (err, value) callbacks become `Promise<T>` with explicit error rejection — type both sides of the bridge.. A callback forgets to check err and reads value; TypeScript can make that mistake harder when you model the pair. Ignoring err is a classic Node footgun. Model err-first callbacks as (err: Error | `null`, value?: T) => `void`.. Model err-first callbacks as (err: Error | `null`, value?: T) => `void`.. Promisify by rejecting on err and resolving only when value is present.. Prefer native promise APIs (fs/promises) over hand-rolled wrappers when available.. Ignoring err is a classic Node footgun.. Optional value on the success path must be narrowed.. Narrow value before reading .port.. In a Node callback, what should you do first?. function readConfig(cb) { cb(null, { port: 3000 }); } readConfig((err, config) => { console.log(config.port); });. type Config = { port: number }; type NodeCb<T> = (err: Error | null, value?: T) => void; function readConfig(cb: NodeCb<Config>): void { cb(null, { port: 3000 }); } function readConfigAsync(): Promise<Config> { return new Promise((resolve, reject) => { readConfig((err, value) => { if (err || !value) reject(err ?? new Error(\"missing\")); else resolve(value); }); }); } // Forgetting that value may b…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Node-style (err, value) callbacks become `Promise<T>` with explicit error rejection — type both sides of the bridge.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A callback forgets to check err and reads value; TypeScript can make that mistake harder when you model the pair. Ignoring err is a classic Node footgun. Model err-first callbacks as (err: Error | `null`, value?: T) => `void`.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-child-process-and-buffers",
      "title": "Child processes, Buffers, and binary data",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/typing-child-process-and-buffers",
      "icon": "ph-hard-drives",
      "keywords": [
        "child_process",
        "spawn",
        "Buffer",
        "Uint8Array",
        "stdio",
        "encoding",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "spawn against exec, stdio typing, Buffer against Uint8Array, and encodings that the types describe more precisely than most code uses.. Child-process output is a `Buffer` or a string depending on options, and code that always calls `.toString()` on 'the result' breaks when encoding changes. Encoding and chunk types are easy to leave implicit.. Use execFile/spawn with an args array to avoid shell injection.. encoding utf8 selects the string overload.. Bound timeout and maxBuffer for untrusted workloads.. Shelling out with concatenated user input.. Prefer execFile with an args array. Without encoding, the result is not a string.. const { stdout } = execSync(\"ls \" + userInput); parse(stdout);. type ExecResult = { stdout: Uint8Array; stderr: Uint8Array }; declare function execFileSync( file: string, args: string[], opts: { encoding: \"utf8\" }, ): string; declare function execFileSync(file: string, args: string[]): ExecResult; export function listDir(dir: string): string { return execFileSync(\"ls\", [\"-la\", dir], { encoding: \"utf8\" }); } const binary = execFileSync(\"ls\", [\"-la\"]); const as…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "spawn against exec, stdio typing, Buffer against Uint8Array, and encodings that the types describe more precisely than most code uses.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Child-process output is a `Buffer` or a string depending on options, and code that always calls `.toString()` on 'the result' breaks when encoding changes. Encoding and chunk types are easy to leave implicit.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-cli-arguments",
      "title": "Command-line arguments",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/typing-cli-arguments",
      "icon": "ph-hard-drives",
      "keywords": [
        "argv",
        "parseArgs",
        "cli",
        "commander",
        "options",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "process.argv, node:util parseArgs, and how a typed options object beats an argument parser that returns a record of `any`.. `process.argv` is `string[]` with no structure — flags, positionals, and values are all the same. Parsers that return loose objects recreate the same ambiguity one layer up.. Parse argv into a dedicated options type early.. Prefer parseArgs helpers, then validate business rules.. Fail with a usage message when operands are missing.. Trusting argv[2] without checking it exists.. Parse into CliOptions. A raw argv string is not a number.. const file = process.argv[2]; doWork(file);. declare const process: { argv: string[] }; type CliOptions = { file: string; verbose: boolean }; export function parseArgs(argv: string[]): CliOptions { const file = argv[2]; if (typeof file !== \"string\" || file.length === 0) { throw new Error(\"usage: tool <file> [--verbose]\"); } return { file, verbose: argv.includes(\"--verbose\") }; } declare function doWork(opts: CliOptions): void; doWork(parseA…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "process.argv, node:util parseArgs, and how a typed options object beats an argument parser that returns a record of `any`.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "`process.argv` is `string[]` with no structure — flags, positionals, and values are all the same. Parsers that return loose objects recreate the same ambiguity one layer up.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-fs-and-path",
      "title": "fs and path",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/typing-fs-and-path",
      "icon": "ph-hard-drives",
      "keywords": [
        "fs",
        "fs/promises",
        "path",
        "Buffer",
        "encoding",
        "overload",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The three fs APIs, the overloads that switch between Buffer and string, and typed path handling that survives Windows.. Filesystem helpers take 'a path' as `any` stringly data and blow up when callers pass a `URL`, a buffer, or a relative path from the wrong cwd. Path and fs mistakes are runtime `TypeError`s with no edit-time story.. Pass utf8 when you want string; otherwise you get binary data.. Normalize user paths before joining with trusted roots.. fs/promises follows the same string-vs-buffer split.. readFileSync without encoding — binary treated like text later.. Overloads distinguish string vs binary. Bytes are not a string.. Pass \"utf8\" to readFileSync so asString is a string. Match the solution text.. const data = fs.readFileSync(userPath); handle(data.toString());. type Fs = { readFileSync(path: string, encoding: \"utf8\"): string; readFileSync(path: string): Uint8Array; }; type PathApi = { resolve(...parts: string[]): string; normalize(p: string): string }; declare const fs: Fs; declare const path: PathApi; export function readUtf8(rel: string): string { return fs.readFileSync(path.resolve(path.normalize(rel)), \"utf8\"); } const bytes = fs.readFileSync(\"/tmp/…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The three fs APIs, the overloads that switch between Buffer and string, and typed path handling that survives Windows.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Filesystem helpers take 'a path' as `any` stringly data and blow up when callers pass a `URL`, a buffer, or a relative path from the wrong cwd. Path and fs mistakes are runtime `TypeError`s with no edit-time story.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-http-servers",
      "title": "HTTP servers",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/typing-http-servers",
      "icon": "ph-hard-drives",
      "keywords": [
        "http",
        "express",
        "fastify",
        "IncomingMessage",
        "handler",
        "generics",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "node:http, Express, and Fastify: what each framework's types actually promise about a request, and where the generics are.. HTTP handlers read `req.url` and body fields as if they were trusted structured data. Query params may be `string | string[] | undefined`; bodies are bytes until you parse them. Untyped handlers are where Node services leak.. HTTP request fields are often optional — narrow before parsing.. Framework generics only help if you parse params/body.. Keep handlers thin: parse, domain logic, encode.. Indexing db with a URL segment and assuming the row exists.. url is optional. Assigning string | `undefined` to string fails.. http.createServer((req, res) => { const id = req.url.split(\"/\")[2]; res.end(db[id].name); });. type IncomingMessage = { url?: string }; type ServerResponse = { end: (body?: string) => void; statusCode: number }; type User = { name: string }; declare const db: Record<string, User | undefined>; export function handler(req: IncomingMessage, res: ServerResponse): void { const url = req.url ?? \"\"; const id = url.split(\"/\")[2]; if (!id) { res.statusCode = 400; res.end(\"missing id\"); return; } co…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "node:http, Express, and Fastify: what each framework's types actually promise about a request, and where the generics are.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "HTTP handlers read `req.url` and body fields as if they were trusted structured data. Query params may be `string | string[] | undefined`; bodies are bytes until you parse them. Untyped handlers are where Node services leak.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-javascript-with-jsdoc",
      "title": "Typing JavaScript with JSDoc",
      "category": "Foundations",
      "tab": "intermediate",
      "route": "/lessons/foundations/typing-javascript-with-jsdoc",
      "icon": "ph-compass",
      "keywords": [
        "jsdoc",
        "checkJs",
        "allowJs",
        "@type",
        "migration",
        "no build step",
        "foundations",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`checkJs` plus JSDoc annotations gives an existing `.js` codebase real checking with no build step and no file renames — the cheapest first move in a migration.. A large Node codebase cannot be renamed to `.ts` in one commit, and until it is, nothing is checked at all. JSDoc types in a `.js` file can close that gap — if you treat them as real contracts, not comments you hope someone reads.. JSDoc + `checkJs` types an existing JS codebase.. Migration path: JSDoc first, then rename to `.ts`.. Keep JSDoc honest — it is checked like annotations.. JSDoc types in a `.js` file.. Same contract in TS syntax. greet returns string.. /** @param {string} name */ function greet(name) { return name.toUpperCase(); }. // JSDoc in .js under checkJs mirrors these annotations: function greet(name: string): string { return name.toUpperCase(); } const s: string = greet(\"ada\"); const bad: number = greet(\"ada\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`checkJs` plus JSDoc annotations gives an existing `.js` codebase real checking with no build step and no file renames — the cheapest first move in a migration.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A large Node codebase cannot be renamed to `.ts` in one commit, and until it is, nothing is checked at all. JSDoc types in a `.js` file can close that gap — if you treat them as real contracts, not comments you hope someone reads.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-mocks-and-stubs",
      "title": "Mocks that satisfy the interface",
      "category": "Testing with types",
      "tab": "intermediate",
      "route": "/lessons/testing/typing-mocks-and-stubs",
      "icon": "ph-test-tube",
      "keywords": [
        "mock",
        "stub",
        "fake",
        "interface",
        "structural",
        "test double",
        "testing",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Structural typing means a fake only needs the parts you use — as long as the type says so rather than a cast.. A mock cast to the full interface compiles today and silently misses the method added tomorrow. Mock returns a wrong-shaped user. Type mocks as the real dependency interface.. Type mocks as the real dependency interface.. async mocks should return Promises.. Avoid `any` in test doubles.. Mock returns a wrong-shaped user.. Mock must satisfy Db. u is User | `undefined`, not number.. Type db as Db and make get return `Promise<User | undefined>`. Match the solution text.. const db = { get: () => ({ id: 1 }) };. type User = { id: string }; type Db = { get(id: string): Promise<User | undefined> }; const db: Db = { get: async (id) => ({ id }), }; const u = await db.get(\"1\"); const bad: number = u;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Structural typing means a fake only needs the parts you use — as long as the type says so rather than a cast.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A mock cast to the full interface compiles today and silently misses the method added tomorrow. Mock returns a wrong-shaped user. Type mocks as the real dependency interface.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-parameters-and-returns",
      "title": "Parameters and return types",
      "category": "Functions & generics",
      "tab": "beginner",
      "route": "/lessons/functions/typing-parameters-and-returns",
      "icon": "ph-function",
      "keywords": [
        "parameters",
        "return type",
        "function",
        "void",
        "functions",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Annotate what goes in and what comes out — and when inference already knows the return so you can skip writing it.. A helper that sometimes returns a string and sometimes returns nothing becomes a landmine for every caller. A declared string return forbids the bare return. Parameter types are the contract callers must satisfy.. Parameter types are the contract callers must satisfy.. Annotate returns on public functions; let inference handle simple internals.. `void` means 'ignore the return'; `undefined` as a value is a different idea.. Implicit `undefined` concatenates into \"Total: $undefined\".. A declared string return forbids the bare return.. Return a string on the negative path too.. What does `: string` on a function mean?. function formatCents(cents) { if (cents < 0) return; return (cents / 100).toFixed(2); } const label = \"Total: $\" + formatCents(499);. function formatCents(cents: number): string { if (cents < 0) return; return (cents / 100).toFixed(2); }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Annotate what goes in and what comes out — and when inference already knows the return so you can skip writing it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A helper that sometimes returns a string and sometimes returns nothing becomes a landmine for every caller. A declared string return forbids the bare return. Parameter types are the contract callers must satisfy.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-process-env",
      "title": "process.env is not a config object",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/typing-process-env",
      "icon": "ph-hard-drives",
      "keywords": [
        "process.env",
        "config",
        "environment",
        "validation",
        "12 factor",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Every variable is string | `undefined`. Parsing environment into a validated config object once, at startup, instead of reading it forty times.. `process.env.FOO` is `string | undefined`, but code treats it as a definite string — or worse, calls `Number` on it and ships `NaN`. Env typing is the first boundary most services get wrong.. Env values are string | `undefined` until you parse them.. Build a typed config object at startup.. Treat empty string as missing.. Passing `process.env`.PORT straight into listen.. Require and parse. Env values are string | `undefined`, not number.. Write parseBoolEnv(raw: string): boolean for true/false/1/0 (case-insensitive).. const port = process.env.PORT; listen(port);. declare const process: { env: Record<string, string | undefined> }; function requireEnv(name: string): string { const value = process.env[name]; if (value === undefined || value === \"\") { throw new Error(\"Missing env \" + name); } return value; } function parsePort(raw: string): number { const n = Number(raw); if (!Number.isInteger(n) || n < 1 || n > 65535) { throw new Error(\"invalid PORT\"); } ret…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Every variable is string | `undefined`. Parsing environment into a validated config object once, at startup, instead of reading it forty times.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "`process.env.FOO` is `string | undefined`, but code treats it as a definite string — or worse, calls `Number` on it and ships `NaN`. Env typing is the first boundary most services get wrong.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-request-handlers",
      "title": "Request handlers lie",
      "category": "The runtime boundary",
      "tab": "intermediate",
      "route": "/lessons/runtime-boundary/typing-request-handlers",
      "icon": "ph-shield-check",
      "keywords": [
        "express",
        "fastify",
        "req.body",
        "params",
        "handler",
        "validation",
        "runtime-boundary",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "req.body, req.params, and req.query are typed as whatever the framework felt like — usually `any` or string. Making a handler honest without fighting the framework.. Express types req.body as `any`, so the most attacker-controlled value in the process is the least checked one. Destructuring req.body with no checks — role can be anything.. Type the framework request body as `unknown` (or leave it untyped) and parse in the handler or a middleware.. Allowlisting role literals at parse time stops privilege escalation via \"admin\" in JSON.. Generics on Express handlers are only as honest as the middleware that populated them.. Destructuring req.body with no checks — role can be anything.. Parse body into CreateUserBody before insert. A cast still yields a string role — assigning to number fails.. Write parseIdParam(input: `unknown`): ParseResult<string> that accepts non-empty strings.. function createUser(req, res) { const { email, role } = req.body; db.insert({ email, role }); res.status(201).end(); }. type CreateUserBody = { email: string; role: \"user\" | \"admin\" }; type ParseResult<T> = | { ok: true; value: T } | { ok: false; error: string }; function parseCreateUserBody(input: unknown): ParseResult<CreateUserBody> { if (typeof input !== \"object\" || input === null) { return { ok: false, error: \"body required\" }; } const rec = input as Record<string, unknown>; if (typeof rec.email !== \"string\" …",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "req.body, req.params, and req.query are typed as whatever the framework felt like — usually `any` or string. Making a handler honest without fighting the framework.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Express types req.body as `any`, so the most attacker-controlled value in the process is the least checked one. Destructuring req.body with no checks — role can be anything.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-streams",
      "title": "Streams",
      "category": "Node.js migration",
      "tab": "intermediate",
      "route": "/lessons/node-migration/typing-streams",
      "icon": "ph-hard-drives",
      "keywords": [
        "stream",
        "Readable",
        "Transform",
        "pipeline",
        "object mode",
        "backpressure",
        "node-migration",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Readable, Writable, Transform and their generics, object mode, pipeline, and the async-iterator interface that makes them tolerable.. Object-mode streams yield `any` chunks unless you say otherwise, so pipeline code looks typed while every chunk is unchecked. Backpressure bugs aside, the type hole is enough to undo `strict`.. Object-mode vs byte streams change chunk types — model that in wrappers.. Always handle stream error events.. Prefer pipeline() for cleanup over hand-rolled listeners.. Calling string methods on a Buffer chunk.. Decode explicitly. BufferLike has no toUpperCase.. Decode chunk with toString(\"utf8\") before toUpperCase. Match the solution text.. readable.on(\"data\", (chunk) => { sink.write(chunk.toUpperCase()); });. type BufferLike = { toString(enc?: string): string }; interface Readable { on(event: \"data\", listener: (chunk: BufferLike) => void): void; } interface Writable { write(chunk: string | BufferLike): boolean; } declare const readable: Readable; declare const sink: Writable; readable.on(\"data\", (chunk) => { sink.write(chunk.toString(\"utf8\").toUpperCase()); }); readable.on(\"data\", (chunk) => { const u…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Readable, Writable, Transform and their generics, object mode, pipeline, and the async-iterator interface that makes them tolerable.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Object-mode streams yield `any` chunks unless you say otherwise, so pipeline code looks typed while every chunk is unchecked. Backpressure bugs aside, the type hole is enough to undo `strict`.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "typing-your-test-files",
      "title": "Typing the tests themselves",
      "category": "Testing with types",
      "tab": "beginner",
      "route": "/lessons/testing/typing-your-test-files",
      "icon": "ph-test-tube",
      "keywords": [
        "tests",
        "vitest",
        "expect",
        "types in tests",
        "testing",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Tests are TypeScript too — typed `asserts` and fixtures catch API drift before the suite becomes a museum of `any`.. Test helpers take `any` and return `any`, so a renamed production field only fails in production. The helper 'passed' a bad fixture into a string API. Include tests in the same `tsconfig` (or a project reference) so they typecheck in CI.. Include tests in the same `tsconfig` (or a project reference) so they typecheck in CI.. Prefer `unknown` + narrowing helpers over `any` in test utils.. When production types change, failing tests should be type errors first.. The helper 'passed' a bad fixture into a string API.. Typed fixtures refuse the bad id at compile time.. Pass a string id in the fixture.. Why type test fixtures?. function expectUser(value) { if (!value || typeof value.id !== \"string\") throw new Error(\"bad\"); return value; } expectUser({ id: 1 }).id.toUpperCase();. type User = { id: string }; function expectUser(value: unknown): User { if ( !value || typeof value !== \"object\" || !(\"id\" in value) || typeof (value as User).id !== \"string\" ) { throw new Error(\"bad\"); } return value as User; } const fixture: User = { id: 1 }; expectUser(fixture).id.toUpperCase();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Tests are TypeScript too — typed `asserts` and fixtures catch API drift before the suite becomes a museum of `any`.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Test helpers take `any` and return `any`, so a renamed production field only fails in production. The helper 'passed' a bad fixture into a string API. Include tests in the same `tsconfig` (or a project reference) so they typecheck in CI.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "union-types",
      "title": "Union types",
      "category": "Types & narrowing",
      "tab": "beginner",
      "route": "/lessons/types/union-types",
      "icon": "ph-shapes",
      "keywords": [
        "union",
        "|",
        "narrowing",
        "nullable",
        "types",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A | B means one of these, not both. What you may do with a union before you have narrowed it, and why that restriction is the point.. A function that returns a user or `null` is documented in a comment, and every caller decides for itself whether to check. `null` is a valid return; calling .name is not.. A union is a value that could be any of the members — only common operations are allowed until you narrow.. User | `null` is the honest return type for 'maybe found'.. Narrow with equality checks, `typeof`, or predicates — do not assert the danger away.. `null` is a valid return; calling .name is not.. You must narrow User | `null` before reading name.. Narrow before reading name.. Before narrowing, what can you safely do with User | `null`?. function findUser(id) { if (id === \"missing\") return null; return { id, name: \"Ada\" }; } const user = findUser(\"missing\"); user.name.toUpperCase();. type User = { id: string; name: string }; function findUser(id: string): User | null { if (id === \"missing\") return null; return { id, name: \"Ada\" }; } const user = findUser(\"missing\"); user.name.toUpperCase();",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A | B means one of these, not both. What you may do with a union before you have narrowed it, and why that restriction is the point.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A function that returns a user or `null` is documented in a comment, and every caller decides for itself whether to check. `null` is a valid return; calling .name is not.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "unique-symbol",
      "title": "unique symbol",
      "category": "Types & narrowing",
      "tab": "advanced",
      "route": "/lessons/types/unique-symbol",
      "icon": "ph-shapes",
      "keywords": [
        "unique symbol",
        "symbol",
        "nominal",
        "identity",
        "declare const",
        "types",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The one place TypeScript gives a value its own identity in the type system, and the foundation of every branding trick that follows.. Structural typing has no notion of identity, so two types meant to be incompatible are interchangeable unless something breaks the tie. Runtime symbols are unique; TypeScript can reflect that in types.. Only `unique symbol` (or `readonly` unique symbol properties) creates a truly nominal singleton type for a symbol.. `declare` const x: unique symbol is the usual pattern for brand keys.. Ordinary symbol types are just symbol — interchangeable and useless for branding.. Runtime symbols are unique; TypeScript can reflect that in types.. unique symbol brands are nominally distinct.. Declare const Brand: unique symbol and type Branded = { `readonly` [Brand]: true }. Create a value.. Why prefer unique symbol over a string brand key like \"__brand\"?. // JS: Symbol() creates unique runtime keys — but types do not exist. const a = Symbol(\"a\"); const b = Symbol(\"a\"); const bag = { [a]: 1 }; console.log(bag[b]); // undefined — different symbols. declare const A: unique symbol; declare const B: unique symbol; type TaggedA = { readonly [A]: void }; type TaggedB = { readonly [B]: void }; const x: TaggedA = { [A]: undefined as void }; const y: TaggedB = x;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The one place TypeScript gives a value its own identity in the type system, and the foundation of every branding trick that follows.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Structural typing has no notion of identity, so two types meant to be incompatible are interchangeable unless something breaks the tie. Runtime symbols are unique; TypeScript can reflect that in types.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "unknown-vs-any",
      "title": "unknown against any",
      "category": "The runtime boundary",
      "tab": "beginner",
      "route": "/lessons/runtime-boundary/unknown-vs-any",
      "icon": "ph-shield-check",
      "keywords": [
        "unknown",
        "any",
        "top type",
        "narrowing",
        "runtime-boundary",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`unknown` is the type-safe top type: you must narrow before use. `any` disables checking. Prefer `unknown` at boundaries.. Typing inbound JSON as `any` lets every property access compile — including ones that will throw. No check that value is a string. `any` is contagious; `unknown` forces a decision at each use site.. `any` is contagious; `unknown` forces a decision at each use site.. Narrow `unknown` with `typeof`, `Array.isArray`, or custom predicates.. Library boundaries should accept `unknown` (or generics), not any.. No check that value is a string.. `unknown` forbids property access until you narrow.. Narrow with `typeof` before toUpperCase.. What must you do before calling methods on `unknown`?. function upper(value) { return value.toUpperCase(); } upper(JSON.parse(\"42\"));. function upper(value: unknown): string { return value.toUpperCase(); }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`unknown` is the type-safe top type: you must narrow before use. `any` disables checking. Prefer `unknown` at boundaries.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Typing inbound JSON as `any` lets every property access compile — including ones that will throw. No check that value is a string. `any` is contagious; `unknown` forces a decision at each use site.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "user-defined-type-guards",
      "title": "User-defined type guards",
      "category": "The runtime boundary",
      "tab": "intermediate",
      "route": "/lessons/runtime-boundary/user-defined-type-guards",
      "icon": "ph-shield-check",
      "keywords": [
        "type guard",
        "is",
        "predicate",
        "narrowing",
        "unknown",
        "runtime-boundary",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A predicate that returns `value is T` teaches the checker what a runtime check proved — without casting.. After `typeof x === 'string'` you know it is a string; after a custom `isUser(x)` check JavaScript still treats x as `unknown` unless you tell TypeScript. A runtime check with no type predicate — the body still trusts shape by faith.. `value is T` is a contract: if the function returns true, the checker treats the argument as T in that branch.. The guard body must actually check what T requires — a lying predicate is a security bug dressed as a type.. Prefer guards over `as T` at trust boundaries; casts skip the proof.. A runtime check with no type predicate — the body still trusts shape by faith.. `value is User` narrows inside the `if`. Accessing a non-existent field on a claimed User still fails.. Write `isPositiveNumber(value: unknown): value is number` that accepts finite numbers > 0. Then call `Math.sqrt` only inside the guard.. What does `function isFoo(x: unknown): x is Foo` change for the checker?. function isUser(value) { return ( value !== null && typeof value === \"object\" && typeof value.id === \"string\" && typeof value.email === \"string\" ); } function handle(payload) { if (isUser(payload)) { // Still a guess — nothing stopped a caller from lying. return payload.email.toLowerCase(); } return null; }. type User = { id: string; email: string }; function isUser(value: unknown): value is User { return ( typeof value === \"object\" && value !== null && \"id\" in value && \"email\" in value && typeof (value as { id: unknown }).id === \"string\" && typeof (value as { email: unknown }).email === \"string\" ); } function handle(payload: unknown): string | null { if (isUser(payload)) { return payload.email.toLow…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A predicate that returns `value is T` teaches the checker what a runtime check proved — without casting.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "After `typeof x === 'string'` you know it is a string; after a custom `isUser(x)` check JavaScript still treats x as `unknown` unless you tell TypeScript. A runtime check with no type predicate — the body still trusts shape by faith.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "utility-types-tour",
      "title": "The built-in utility types",
      "category": "Type-level programming",
      "tab": "intermediate",
      "route": "/lessons/type-level/utility-types-tour",
      "icon": "ph-function",
      "keywords": [
        "Partial",
        "Pick",
        "Omit",
        "Record",
        "Exclude",
        "utility types",
        "type-level",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`Partial`, `Required`, `Readonly`, `Pick`, `Omit`, `Record`, `Exclude`, `Extract`, `NonNullable` — what each one does and, more usefully, which one you actually meant.. `Omit` does not check that the key exists, so removing a field that was already renamed silently does nothing. Patch can overwrite id. `Partial`, `Pick`, `Omit`, `Required` cover most object transforms.. `Partial`, `Pick`, `Omit`, `Required` cover most object transforms.. Compose utilities instead of hand-rolling mapped types first.. `Readonly` and `Record` round out the everyday set.. Patch can overwrite id.. `Partial<Pick<...>>` forbids patching id.. Which utility makes every property optional?. function update(user, patch) { return Object.assign({}, user, patch); }. type User = { id: string; name: string; age: number }; type UserPatch = Partial<Pick<User, \"name\" | \"age\">>; function update(user: User, patch: UserPatch): User { return { ...user, ...patch }; } const u = update({ id: \"1\", name: \"Ada\", age: 1 }, { name: \"Bob\" }); const bad = update(u, { id: \"2\" });",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`Partial`, `Required`, `Readonly`, `Pick`, `Omit`, `Record`, `Exclude`, `Extract`, `NonNullable` — what each one does and, more usefully, which one you actually meant.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "`Omit` does not check that the key exists, so removing a field that was already renamed silently does nothing. Patch can overwrite id. `Partial`, `Pick`, `Omit`, `Required` cover most object transforms.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "validating-http-responses",
      "title": "Validating what came back",
      "category": "The runtime boundary",
      "tab": "intermediate",
      "route": "/lessons/runtime-boundary/validating-http-responses",
      "icon": "ph-shield-check",
      "keywords": [
        "fetch",
        "http",
        "response",
        "api client",
        "validation",
        "boundary",
        "runtime-boundary",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "fetch gives you `any`, your API client gives you confidence, and neither gives you a guarantee. Where the parse belongs in a request pipeline.. An upstream service renames a field and your typed client keeps compiling, all the way to the `undefined` that reaches the user. json() is unchecked — a renamed field becomes a runtime TypeError.. Wire types (DTO) are untrusted even when 'our' service produced them — deploys drift.. Put parseUserDto (or schema.parse) immediately after `JSON.parse` / res.json().. `as UserDto` on a response compiles forever and fails at the worst time.. json() is unchecked — a renamed field becomes a runtime TypeError.. Parse the body before reading fields. Casting unknown to UserDto still leaves name as string — number assign fails.. async function getUser(id) { const res = await fetch(\"/api/users/\" + id); const data = await res.json(); return data.name.toUpperCase(); }. type UserDto = { id: string; name: string }; type ParseResult<T> = | { ok: true; value: T } | { ok: false; error: string }; function parseUserDto(input: unknown): ParseResult<UserDto> { if (typeof input !== \"object\" || input === null) { return { ok: false, error: \"not an object\" }; } const rec = input as Record<string, unknown>; if (typeof rec.id !== \"string\" || typeof rec.name !== \"string\") { re…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "fetch gives you `any`, your API client gives you confidence, and neither gives you a guarantee. Where the parse belongs in a request pipeline.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "An upstream service renames a field and your typed client keeps compiling, all the way to the `undefined` that reaches the user. json() is unchecked — a renamed field becomes a runtime TypeError.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "variadic-tuple-types",
      "title": "Variadic tuple types",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/variadic-tuple-types",
      "icon": "ph-function",
      "keywords": [
        "variadic tuple",
        "spread",
        "tuple",
        "compose",
        "Promise.all",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "[...T, U] and spreads in tuple positions — how typed compose, curry, and `Promise.all` keep every element's type in order.. A function that appends an argument to another function's parameter list cannot be typed without tuple manipulation. Wrappers without tuple types erase the wrapped signature.. Variadic tuple types let you spread one tuple type into another: [...A, ...B].. They power typed `Promise.all`, middleware, and curry without any[].. `infer P` on a rest parameter yields a tuple type you can manipulate.. Wrappers without tuple types erase the wrapped signature.. Variadic [...P, A] extends the parameter tuple.. Implement Cons<H, T extends unknown[]> = [H, ...T]. Show a value of Cons<string, [number]>.. What is the type of `[...[string, number], boolean]`?. // JS: wrap a function and lose parameter types. function withLogger(fn) { return (...args) => { console.log(\"call\", args); return fn(...args); }; } const add = (a, b) => a + b; const logged = withLogger(add); // (...args: any[]) => any vibe. type AppendArg<F extends (...args: never[]) => unknown, A> = F extends (...args: infer P) => infer R ? (...args: [...P, A]) => R : never; function greet(name: string) { return `hi ${name}`; } type GreetWithExclaim = AppendArg<typeof greet, boolean>; const g: GreetWithExclaim = (name, loud) => loud ? `${greet(name)}!` : greet(name); // Wrong second arg type — boolean was appended to the tuple cons…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "[...T, U] and spreads in tuple positions — how typed compose, curry, and `Promise.all` keep every element's type in order.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A function that appends an argument to another function's parameter list cannot be typed without tuple manipulation. Wrappers without tuple types erase the wrapped signature.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "variance-and-strict-function-types",
      "title": "Variance and strictFunctionTypes",
      "category": "Functions & generics",
      "tab": "advanced",
      "route": "/lessons/functions/variance-and-strict-function-types",
      "icon": "ph-function",
      "keywords": [
        "variance",
        "covariant",
        "contravariant",
        "bivariance",
        "strictFunctionTypes",
        "functions",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`Parameters` are contravariant, returns are covariant — except for methods, which stayed bivariant on purpose. What that trade actually costs you.. A handler that accepts a narrower event than it was registered for is accepted by the checker and crashes at runtime. JS will call whatever you register; wrong assumptions crash later.. Return types are covariant: a function returning Dog may stand in for one returning Animal.. Parameter types are contravariant for function types when `strictFunctionTypes` is on.. Methods stay bivariant for DOM/framework ergonomics — prefer function-typed properties for safety.. JS will call whatever you register; wrong assumptions crash later.. Assigning a narrower-parameter handler is rejected under `strictFunctionTypes`.. Fix onClick so it accepts the Button event shape (or only uses fields that exist on it). Match the solution text.. With `strictFunctionTypes`, may Handler<Dog> be assigned to Handler<Animal>?. // JS: any callback is fine — until the event shape differs. function on(handler) { handler({ type: \"click\", x: 1, y: 2 }); } on((e) => { // Assumes a narrower shape than the emitter provides. console.log(e.key.toUpperCase()); });. type Animal = { tag: \"animal\" }; type Dog = Animal & { bark(): void }; // Under strictFunctionTypes, parameter positions are checked contravariantly // for function types (not methods). type Handler<E> = (event: E) => void; let animalHandler: Handler<Animal> = (a) => { void a.tag; }; const dogHandler: Handler<Dog> = (d) => { d.bark(); }; // Unsafe: dogHandler expects bark(); animalHandler may pas…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`Parameters` are contravariant, returns are covariant — except for methods, which stayed bivariant on purpose. What that trade actually costs you.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A handler that accepts a narrower event than it was registered for is accepted by the checker and crashes at runtime. JS will call whatever you register; wrong assumptions crash later.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "void-and-never",
      "title": "void and never",
      "category": "Types & narrowing",
      "tab": "intermediate",
      "route": "/lessons/types/void-and-never",
      "icon": "ph-shapes",
      "keywords": [
        "void",
        "never",
        "bottom type",
        "return type",
        "throw",
        "types",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "`void` means the return value is not to be used; `never` means there is no return value at all. Two very different kinds of nothing.. A function typed `void` can still return something, and a function that always throws is not typed `never` unless you say so. Throw vs log look the same without return types.. `never` means the function does not return normally.. `void` means it returns no useful value — callers should not read it.. Use `never` for exhaustive checks and fail-fast helpers.. Throw vs log look the same without return types.. log returns `void` — not a string.. How do `void` and `never` differ for function return types?. function fail(msg) { throw new Error(msg); } function log(msg) { console.log(msg); }. function fail(msg: string): never { throw new Error(msg); } function log(msg: string): void { // side effect only } declare function handle(x: string | number): string; function demo(x: string | number): string { if (typeof x === \"string\") return x; if (typeof x === \"number\") return String(x); return fail(\"unreachable\"); } const v: string = log(\"x\");",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "`void` means the return value is not to be used; `never` means there is no return value at all. Two very different kinds of nothing.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A function typed `void` can still return something, and a function that always throws is not typed `never` unless you say so. Throw vs log look the same without return types.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "void-returning-callbacks",
      "title": "Callbacks that return void",
      "category": "Functions & generics",
      "tab": "intermediate",
      "route": "/lessons/functions/void-returning-callbacks",
      "icon": "ph-function",
      "keywords": [
        "void",
        "callback",
        "return value",
        "forEach",
        "assignability",
        "functions",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "A callback typed to return `void` accepts a function that returns something. That rule is deliberate, useful, and occasionally a bug factory.. arr.forEach(async item => ...) type-checks and the promises are dropped on the floor. forEach callback return values are ignored. `void` in callback positions is special — returned values are ignored.. `void` in callback positions is special — returned values are ignored.. Do not use forEach when you need a mapped array.. Match callback return types to the combinator's contract.. forEach callback return values are ignored.. `void` callbacks may return values, but forEachNumber itself returns `void`.. [1,2].forEach(n => { return n * 2; });. declare function mapNumbers( values: number[], fn: (n: number) => number, ): number[]; declare function forEachNumber( values: number[], fn: (n: number) => void, ): void; const doubled = mapNumbers([1, 2], (n) => n * 2); forEachNumber([1, 2], (n) => n * 2); const bad: number[] = forEachNumber([1], (n) => n);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "A callback typed to return `void` accepts a function that returns something. That rule is deliberate, useful, and occasionally a bug factory.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "arr.forEach(async item => ...) type-checks and the promises are dropped on the floor. forEach callback return values are ignored. `void` in callback positions is special — returned values are ignored.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "when-not-to-type-level-program",
      "title": "When not to do any of this",
      "category": "Type-level programming",
      "tab": "advanced",
      "route": "/lessons/type-level/when-not-to-type-level-program",
      "icon": "ph-function",
      "keywords": [
        "maintenance",
        "readability",
        "trade-off",
        "error messages",
        "judgement",
        "type-level",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The maintenance argument: who reads this code next, what its error messages look like when it fails, and the runtime check that would have been enough.. The most impressive type in a codebase is often the one nobody else can change, which makes it a liability rather than an asset. Simple runtime helpers stay readable; types should not obscure them.. Type-level code is code — it needs readers, tests, and an exit strategy.. If a runtime parse already exists, duplicating the grammar in types may be redundant.. Ship the simplest type that prevents the bug class you care about; deepen only when bugs demand it.. Simple runtime helpers stay readable; types should not obscure them.. Wrong keys produce opaque errors; fixed interfaces stay clear.. Prefer a plain interface Address over a complex mapped type. Define street and city as strings and construct one.. Which is the best reason to avoid a deep type-level abstraction?. // JS: a one-liner everyone understands. function pick(obj, keys) { return Object.fromEntries(keys.map((k) => [k, obj[k]])); }. // Sometimes the “clever” Pick is worse than a plain interface. type MegaPick<T, K extends keyof T> = { [P in K]: T[P]; }; // Prefer documenting the public shape when the set is fixed: interface PublicUser { id: string; name: string; } // MegaPick explodes error messages when K is wrong: type Oops = MegaPick<{ id: string }, \"named\">;",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The maintenance argument: who reads this code next, what its error messages look like when it fails, and the runtime check that would have been enough.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "The most impressive type in a codebase is often the one nobody else can change, which makes it a liability rather than an asset. Simple runtime helpers stay readable; types should not obscure them.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "where-types-end",
      "title": "Where your types stop",
      "category": "The runtime boundary",
      "tab": "beginner",
      "route": "/lessons/runtime-boundary/where-types-end",
      "icon": "ph-shield-check",
      "keywords": [
        "boundary",
        "untrusted input",
        "IO",
        "validation",
        "runtime-boundary",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Types describe your program, not the network — the boundary between trusted and untrusted values is where TypeScript's guarantees end.. A request handler types req.body as a rich interface and then trusts every field without checking. No TypeScript error here — that's the point: the type ends at the assertion; runtime still lies.. Everything that crosses IO (HTTP, disk, env, queues) starts as untrusted.. Types inside your process are only as true as the validations at the edge.. Treat as Model and `any` as 'I stopped checking' — prefer `unknown` + parse.. `String` \"true\" is not boolean true — authz bug.. No TypeScript error here — that's the point: the type ends at the assertion; runtime still lies.. Accept `unknown` and narrow before createUser.. Where do static types stop protecting you?. function createUser(body) { return { id: body.id, admin: body.admin === true }; } createUser(JSON.parse('{\"id\":\"1\",\"admin\":\"true\"}'));. type CreateUser = { id: string; admin: boolean }; function createUser(body: CreateUser): CreateUser { return { id: body.id, admin: body.admin }; } // Looks typed. The bytes still came from outside the program. const body = JSON.parse('{\"id\":\"1\",\"admin\":\"true\"}') as CreateUser; createUser(body);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Types describe your program, not the network — the boundary between trusted and untrusted values is where TypeScript's guarantees end.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "A request handler types req.body as a rich interface and then trusts every field without checking. No TypeScript error here — that's the point: the type ends at the assertion; runtime still lies.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "why-migrate-a-node-service",
      "title": "Migrating a Node service: what changes",
      "category": "Node.js migration",
      "tab": "beginner",
      "route": "/lessons/node-migration/why-migrate-a-node-service",
      "icon": "ph-hard-drives",
      "keywords": [
        "migration",
        "Node",
        "incremental",
        "adopt",
        "node-migration",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "What adopting TypeScript changes in a running Node service — and what it deliberately does not change at runtime.. Teams rewrite everything in one PR, break production, and blame TypeScript instead of the big-bang process. In real Node, `id` may already be `string | string[] | undefined` — edit-time tools that never ran simply never told you.. Migrate incrementally: `allowJs`, then `checkJs`, then rename files.. Runtime behavior stays JavaScript — types do not deploy a new Node.. Start with boundary modules (HTTP, env, DB rows) where bugs cluster.. id may be string | string[] | `undefined` in real Node.. Migration surfaces the union you already had to handle.. Narrow id to a single string before toUpperCase.. Best first migration step for a large Node service?. // The service already works. The risk is untyped boundaries. function handler(req, res) { const id = req.query.id; res.end(String(id.toUpperCase())); }. type Req = { query: { id?: string | string[] } }; type Res = { end: (body: string) => void }; function handler(req: Req, res: Res): void { const id = req.query.id; res.end(id.toUpperCase()); }",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "What adopting TypeScript changes in a running Node service — and what it deliberately does not change at runtime.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Teams rewrite everything in one PR, break production, and blame TypeScript instead of the big-bang process. In real Node, `id` may already be `string | string[] | undefined` — edit-time tools that never ran simply never told you.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "why-types",
      "title": "Why types at all",
      "category": "Foundations",
      "tab": "beginner",
      "route": "/lessons/foundations/why-types",
      "icon": "ph-compass",
      "keywords": [
        "why typescript",
        "motivation",
        "static types",
        "bugs",
        "refactoring",
        "foundations",
        "beginner"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "The class of bug a Node service ships every week — a renamed field, a string where a number was meant — and why a type checker is cheaper than the test that would have caught it.. JavaScript will happily call a function with the wrong number of arguments, read missing properties as `undefined`, and keep going until something far away throws. The bug is not 'types exist' — it is that nothing refused the bad call when you wrote it.. The bug is not 'division by `undefined`' — it is a shape mismatch at the boundary between two modules.. A type checker moves that failure from production (or a distant unit test) to the edit you just made.. You adopt TypeScript for the class of bug you already ship, not for academic purity.. A renamed field becomes NaN with no complaint at the call site.. TypeScript refuses the payload that still uses the old field name.. Fix the call site so charge accepts a valid Order (use totalCents: 4999).. In the JavaScript version, what does charge return when totalCents is missing?. // Downstream renamed total → totalCents; callers still send total. function charge(order) { // undefined / 100 → NaN, then a bogus charge amount in production. return order.totalCents / 100; } const payload = { id: \"ord_1\", total: 4999 }; charge(payload);. type Order = { id: string; totalCents: number }; function charge(order: Order) { return order.totalCents / 100; } const payload = { id: \"ord_1\", total: 4999 }; charge(payload);",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "The class of bug a Node service ships every week — a renamed field, a string where a number was meant — and why a type checker is cheaper than the test that would have caught it.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "JavaScript will happily call a function with the wrong number of arguments, read missing properties as `undefined`, and keep going until something far away throws. The bug is not 'types exist' — it is that nothing refused the bad call when you wrote it.",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "worker-threads-and-structured-clone",
      "title": "Worker threads",
      "category": "Node.js migration",
      "tab": "advanced",
      "route": "/lessons/node-migration/worker-threads-and-structured-clone",
      "icon": "ph-hard-drives",
      "keywords": [
        "worker_threads",
        "postMessage",
        "structured clone",
        "protocol",
        "MessagePort",
        "node-migration",
        "advanced"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Typing the message protocol between threads, what structured clone can carry, and why the boundary deserves the same scepticism as the network.. `postMessage` accepts `any`, so the protocol between two threads is a handshake of hope. Structured clone silently drops functions and throws on some exotic objects — failures that look like 'the worker hung.'. Define request/response unions shared by both threads — same as a network protocol.. Structured clone supports many built-ins but not functions or DOM nodes in Node workers the same way.. Validate `unknown` messages at the edge; do not trust postMessage peers blindly.. Structured clone cannot carry functions; JS will not warn.. Protocol types make illegal messages a compile error.. Define Msg = { type: \"ping\" } | { type: \"pong\" } and a function that handles both.. Why type worker messages as discriminated unions?. // JS workers: postMessage anything — functions silently drop. parentPort.postMessage({ type: \"result\", fn: () => 1 });. type WorkerReq = { type: \"hash\"; payload: string }; type WorkerRes = { type: \"result\"; digest: string }; declare function postMessage(msg: WorkerRes): void; function onMessage(msg: WorkerReq) { if (msg.type === \"hash\") { postMessage({ type: \"result\", digest: msg.payload }); } } // Reject non-protocol messages at the type boundary: const bad: WorkerRes = { type: \"result\", digest: 1 as unknown as s…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Typing the message protocol between threads, what structured clone can carry, and why the boundary deserves the same scepticism as the network.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "`postMessage` accepts `any`, so the protocol between two threads is a handshake of hope. Structured clone silently drops functions and throws on some exotic objects — failures that look like 'the worker hung.'",
          "heading": "Problem"
        }
      ]
    },
    {
      "id": "writing-a-validator-by-hand",
      "title": "A validator you can read",
      "category": "The runtime boundary",
      "tab": "intermediate",
      "route": "/lessons/runtime-boundary/writing-a-validator-by-hand",
      "icon": "ph-shield-check",
      "keywords": [
        "validator",
        "parsing",
        "unknown",
        "runtime check",
        "error message",
        "runtime-boundary",
        "intermediate"
      ],
      "headings": [
        "Problem",
        "TypeScript",
        "Insight"
      ],
      "bodyText": "Building an honest object validator from `unknown` up: property presence, primitive checks, arrays, and the error messages that make it usable.. Every codebase writes this eventually, and the version written in a hurry checks the happy path only. Happy-path mapping with no checks — missing roles blows up later.. Validate every field you will read; skip nothing because 'the client is ours'.. Return `{ ok, value } | { ok, error }` (or throw) so failure cannot be ignored as silently as a boolean.. Build the output object from checked primitives — do not return the original `unknown` reference.. Happy-path mapping with no checks — missing roles blows up later.. Return a result, not a cast. The deliberate lines show that forcing User still leaves id as string — assigning to number fails.. Complete `parsePort` so it accepts `unknown` and returns a ParseResult<number> for integers 1–65535.. function parseUser(input) { return { id: input.id, roles: input.roles, }; } const user = parseUser(JSON.parse('{\"id\":1}')); user.roles.map((r) => r.toUpperCase());. type User = { id: string; roles: string[] }; type ParseResult<T> = | { ok: true; value: T } | { ok: false; error: string }; export function parseUser(input: unknown): ParseResult<User> { if (typeof input !== \"object\" || input === null) { return { ok: false, error: \"expected object\" }; } const rec = input as Record<string, unknown>; if (typeof rec.id !== \"string\" || rec.id.length === 0) { return {…",
      "classes": [],
      "chunks": [
        {
          "type": "paragraph",
          "text": "Building an honest object validator from `unknown` up: property presence, primitive checks, arrays, and the error messages that make it usable.",
          "heading": "Summary"
        },
        {
          "type": "paragraph",
          "text": "Every codebase writes this eventually, and the version written in a hurry checks the happy path only. Happy-path mapping with no checks — missing roles blows up later.",
          "heading": "Problem"
        }
      ]
    }
  ]
}
